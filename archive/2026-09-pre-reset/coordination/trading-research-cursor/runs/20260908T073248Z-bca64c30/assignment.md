# Consolidated review repair — first quote preparation change

Resume the existing assignment and make one focused repair pass in its same four allowed files. The supervisor has reviewed the complete diff. Do not reread the whole repository, redesign the implementation, run commands/tests/imports, change the frozen reference, or start other work. Implement these five corrections and add only the directly necessary assertions. Return a short summary and stop.

1. **Boolean Arrow arrays must allow copying.** `_combine` changes `actions` from a ChunkedArray to an Array. All three action-mask expressions in `PreparedQuoteBatch._prepare` now end in `BooleanArray.to_numpy()` with its `zero_copy_only=True` default. Boolean Arrow arrays cannot be converted zero-copy. Use `to_numpy(zero_copy_only=False)` for updates, clears and known. This would otherwise break every nonempty valid input.

2. **Correct the independently calculated large-clock fixture.** In `test_large_clocks_depths_and_equal_timestamps_keep_integer_path`, the final row moves both bid and ask up. With `depth = 2**32 - 2`, the first two transitions are +2 and -4; the last transition is `(depth - 5) + (depth - 3)`. The highest cumulative OFI is therefore at `base + 40`, source order 3, not `base + 10`. Keep the data and arithmetic unchanged. Assert the actual high and low timestamps/source order and integer values, based on this calculation; the low occurs at the second equal-time update, order 2.

3. **Make the frozen comparator a static dependency.** Replace the importlib file-loader/cache in `tests/test_auction_flow_quotes.py` with `from references.auction_flow_quote_check6.quotes import QuoteWindow as FrozenQuoteWindow`. A small `frozen_quote_window()` returning that alias may remain for callers. The runner discovers dependencies by static AST imports, so an executable reference loaded through a string path is absent from its reuse contract. Do not change any reference bytes. The project root is on the registered worker's Python import path.

4. **Restore the public required-null guard before preparation.** In `QuoteWindow.add`, retain the old explicit required-column null check after the empty/capacity checks and before `prepare_quote_batch`. Otherwise action casts can raise a different error before the required-null `IntegrityError` (for example a required null alongside an uncastable action). Keep the prepared per-slice null mask so a good prefix remains consumable when a later row is null. Add a directly relevant fixture covering valid-prefix consumption followed by null rejection, and the public null-error precedence.

5. **Do not silently truncate slice coordinates.** `add_prepared` currently coerces `start` and `stop` with `int()`, accepting fractions and booleans as row indices. Require integer indices, rejecting booleans and nonintegral types with the existing IntegrityError. Python and NumPy integer indices may be supported. Include explicit rejection checks for a fractional and boolean coordinate, along with out-of-range/reversed indices. The pipeline already supplies valid integer indices.

The optimization and all original acceptance conditions remain the same. Registered runtime checks are the supervisor's next step. Do not claim tested correctness or performance.
