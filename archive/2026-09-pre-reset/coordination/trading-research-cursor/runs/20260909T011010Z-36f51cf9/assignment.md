Complete one consolidated correction of the profile/reference draft. Same selected Cursor Grok 4.6 Extra High Fast, same isolated checkout and ONLY the same three owned files. 1800 seconds. Do not execute imports, tests, shell, raw scans, or install dependencies. Root has now read all three files. The original assignment and frozen AUCTION_FLOW_PROFILE_REFERENCE_STATISTICS_V1.json remain authoritative; this correction fixes violations, it does not narrow the study. Return complete files and a concise report of exact interfaces and retained limitations. No additional worker loops are planned.

Read these canonical files for the real API/schema, not invented fixtures:
/workspace/trading-research/src/trading_research/research/auction_flow_window_links.py (feature_schema, label_schema, join_window_eligibility)
/workspace/trading-research/src/trading_research/research/auction_flow_window_statistics.py (scientific_window_eligibility and vector stage guards)
/workspace/trading-research/src/trading_research/research/auction_flow_anchors.py (named_clock_anchors already returns civil_ny_week/month/quarter/year!)
/workspace/trading-research/src/trading_research/research/auction_flow_anchor_profiles.py (PIN069BarClose)
/workspace/trading-research/src/trading_research/research/auction_flow_anchor_tpo.py and auction_flow_anchor_trades.py
/workspace/trading-research/src/trading_research/measurements/{profiles,vwap,footprint}.py
Read the exact frozen ANCHOR contract and DETAIL bound by the profile contract. The first profile contract has footprint:{}; obtain authoritative parameters from DETAIL and authenticate it, not silently invent defaults. The profile contract does not contain source_collections: derive the exact collection map from its bound observation/window protocol or population identities; root can inject operational identity bindings but the public frozen contract must work without fixture-only assumptions.

1. Streaming and actual performance. Current compute_profile_partition retains all atoms, all cells, all feature/label dictionaries and all output dictionaries for an annual partition; dedup_atoms scans all prior atoms O(N²); it repeatedly filters a year of identity atoms for every cut/label and builds Fraction dense grids/quantiles for every prefix. This cannot fit 4GiB AS or the CPU bound. Implement bounded chronological units/day batches with typed NumPy/Arrow arrays for cells and window rows. Incrementally add/remove sparse histograms for rolling 5/15/60/240, incremental daily/civil masses, unique clocks/calendar lookups cached. Keep only necessary active anchors and 60-minute future support; long civil masses need accumulated cells/moments/provenance, not every atom since January. Dedup exact aliases by indexed coordinate/time/content hash and mark conflicting overlap unavailable; do not scan all previously seen atoms. Consume/release each measurement JSON; no annual cache of 3GB JSON objects. Output typed bounded chunks and statistics accumulators; do not return all output rows or extend all_geometry/all_tpo/all_joins. Do not duplicate original sparse cells into millions of Python rows. A compact authenticated cell-input ledger with exact reconstruction from original measurement/observation receipts is sufficient; retain unpriced-only atoms and per-atom unpriced mass exactly once. Missing cell rows cannot disappear. Production numerical geometry should use NumPy integer/common-denominator arrays and reproduce unchanged Fraction kernels in literal tests, rather than constructing Fraction objects at every cell/cut. All fourteen completed variants, six proxies, baseline developing/rolling, and original estimands remain required.

2. Correct chronological anchors. Current grouping by source_path prevents the authorized adjacent nonoverlapping file carry; neighbor_units only4h cannot support civil periods. Build identity by collection/root/physical contract and compatible raw instrument, with each contributing source path/hash kept as provenance. Monthly and weekly never merge. Include all necessary prior/next units in both computation and authenticated partition identity. Assign selected results to economic collection/root/year once, support-only neighboring rows are not another observation. Process civil week/month/quarter/year from actual period bounds and accumulated mass; no filter that drops these named_clock_anchors. Full period coverage remains false unless constituent intended intervals are proven; retain partial observed geometry and complete/missing counts separately. Do not manufacture venue closures from no atoms.

Current completed_same_day geometry is computed first and then assigned to prior_completed_va used by developing prefixes: this is lookahead. Maintain only genuinely prior completed references whose publication <= decision clock. New current-session final geometry must not affect earlier developing rows. Carry prior RTH across eligible dates/files/year only with the same contract and known publication. Developing prefix complete means all intended prefix atoms through this cut are present/known; it does NOT require the eventual whole session to be complete. A fixed completed anchor is frozen and available for later cut links; do not only emit future links for rolling/developing. Record observed current open and prior observed close with real provenance/known_at; do not label them official settlement.

3. Join the real accepted windows. Actual windows have latency_ns (including integer0), complete, chronological_eligible, stage/stage_boundary, atoms_complete, data_complete, coordinate_complete, source_coverage_complete, contract_stable/transition, event_start_ns/event_end_ns/known_at_ns. Features contain formation_minutes; labels don't. Current int(candidate.get('latency_ns') or -1) loses the zero arm. Use explicit None checks. Current feature_complete uses only atoms_complete and coverage_known tests merely not-None; both can admit incomplete geometry/source. Require the profile's own formation/prefix coverage, exact physical coordinate/contract, stage interval from formation start through scenario publication and label maturity in the same named stage; reuse actual join helper/vector equivalent, not fallback labels' stage. Preserve no-label/no-event/no-priced/censored/stage-boundary rows and reasons. Match exact source identity metadata and cut, not only display path. Keep formation_minutes, latency and horizon as named independent group dimensions. Baseline geometry should be referenced once by geometry_id; label/source/geometry ID links can replace duplicated wide rows with a documented exact public reconstructor. In row counts distinguish link rows from independent events/dates.

Source publication is observed 250ms, but zero/250ms/1000ms are declared counterfactual scenarios from window46; preserve actual known_at and explicit scenario_known separately. Geometry selection known and actual input known must never be erased. Future label starts cut+latency exactly. Future mass is the separately declared whole-minute fixed-cut profile target: compute once per geometry/cut/horizon, not per formation/latency duplicate. Bind following same-contract atoms, maturity including future input publication, overflow for buy/sell/unknown and unpriced, and its own coverage. Preserve geometry and transport estimands in output and statistics. No touch/retest ordering from extrema.

4. Missing or wrong numerical fields. _table_from_rows currently silently drops many calculated VWAP/side/quantile/band/migration metrics because the schema omits them. Every required metric must survive typed output and have a named statistics entry even when undefined. Cover complete POC set, VAL/VAH, nodes/valleys and dominance, side and signed/absolute delta, concentration and same-grid transport, VWAP exact moments, weighted quantiles and MAD, SD and percentage bands, footprint qualifying rows/stacks, TPO/IB/tails/single prints, frozen/developing movement, and future profile/mass. Raw sums use exact ints or rational pairs; source tick/index-point units explicit. Current compact scalar_poc/val_row/vah_row are grid row indices; coarse width/origin comparisons and contact must convert to physical tick coordinates or carry exact row+width+origin with explicit conversion. Check support units and same-grid transport.

exact_sqrt returns None for an irrational rational variance; SD is still defined. Preserve exact variance and a labelled floating sqrt/band approximation or symbolic coefficient, rather than dropping almost every SD band. Do not call it exact numerical rational sqrt.

pin069_reanchor currently does not advance origin on a new high and returns latest close*volume, not the specified accumulated anchored VWAP. Follow PIN069BarClose's exact first/new-high reset: fixed UTC-4 06–09 window, high changes only on reset, origin=selected completed bar start, publication=bar known, accumulated sum_bar_close_volume/bar_volume from reset, retained unknown/high-state and unsupported volume. Do it incrementally; do not scan every prefix. Preserve it as the separate source proxy, not the exact traded-price anchored VWAP.

TPO: verify actual AtomicTrades adapter and AnchorTPO API. Do not catch a ContractError and silently drop every TPO row while declaring success. Retain supported trade visits and range proxy15/30/60, IB30/60 and all missing dispositions. Final single-print designation only after complete anchor and at least two complete brackets; provisional prefix separate. Quiet brackets must stay in intended history. Record bracket provenance compactly.

5. Statistics and reuse. _accumulate_joins groups by unique anchor_id, making each cut its own group/date. Group by anchor_variant/kind and prescribed collection/root/year/stage/session/geometry variant/formation/latency/horizon; anchor_id is only row identity. _accumulate_geometry currently feeds partial values into full quantitative groups, and proxy comparison loop includes proxy versus itself and duplicates other comparisons. Separate observed-partial descriptive populations from eligible-complete; pair exact same anchor/publish clock/source/physical cells and only baseline exact vs each named alternative. Preserve matched and own denominators. Accumulate all prescribed auxiliary and future metrics, including explicit missing/no-event fractions; never replace raw observation counts with date-cell counts. Keep equal-date means and actual value quantiles, dependence-aware CI with frozen weights. Avoid a metric×replicate×allobservations matrix; bounded per-date sufficient sums and exact typed values can be spilled per partition. Full readable tables and concise findings, not selected first20 rows.

Current accepted partition branch authenticates output but does not load/reduce its groups, so an all-reused parallel finalizer produces empty statistics while claiming complete. Store finalized group/paired payloads per partition (groups include year, so concatenate distinct partition keys after authenticating all references), and reducer must reproduce fresh serial logical result. Bind all source members+support neighbors, all relevant window refs, consumer implementation hashes and scientific contract. Unavailable/censor/missing counts and lists must aggregate equally for new and reused partitions. Do not reread all geometry Parquet merely to reduce already authenticated groups. All public authentication must exhaust hashes/row counts appropriately.

6. Decisive fixtures before returning. Preserve original literal checks but correct fixture schemas to actual feature/label schemas and complete identities. Add independent actual-run tests for: zero latency present; future-stage mismatch/own geometry incomplete rejected; prior final VA must not affect any earlier developing output; completed prior anchor produces later links; adjacent same-collection different files form one rolling/calendar mass but other collection never joins; civil week/month/quarter/year rows exist and partial status correct; new-high PIN069 origin and accumulated quotient exact; irrational variance has non-null SD representation; every required computed field survives Parquet; coarse width2/origin1 physical values; unpriced-only atoms retained exactly once; two independent dates for a joined anchor_variant group; no proxy-self pair; accepted partition reuse output/groups/counts equals fresh and corrupted neighbor ref is rejected. Keep tests bounded and genuinely independent. Root will run registered pilot and full projection, no tests here.

Root will supply an operational runner with 4GiB AS per worker, 1800CPU pilot and up to48000CPU/16GiB full under remaining same-family limits. These are caps, not permission to spend them in Python per-cell loops. Exact source-unit/cell/anchor/cut counts and measured segmented CPU/output must support full projection. Report residual uncertainties honestly; do not silently reduce variants, prefix cuts, sources, dates or metrics to pass a pilot.
