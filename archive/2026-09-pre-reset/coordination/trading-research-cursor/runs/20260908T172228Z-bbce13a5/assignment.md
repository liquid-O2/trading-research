# One consolidated production correction to the completed anchor consumer
Continue your existing assignment, with same four owned files and science. The first returned code is preserved. Do NOT reread entire onboarding; inspect the specific defects below and implement the smallest complete correction. No tests/imports/scans/Shell execution. Return within 900 seconds. This is the single consolidated correction; no new framework or further worker review loop. Codex has useful independent cohort/run integration work in parallel.

Named blockers in your actual code:
1. consume_anchor_schedule retains ALL _PreparedWindow measurements/atoms/prepared trades, ALL full results, and serializes only at the end. A full 3589-window cohort exceeds memory. Change to bounded streaming ownership: one current decoded source window, release after consuming; bounded caches may hold compact atoms/sufficient state with explicit limits, never years of decompressed measurement dictionaries or prepared trade tables. prepare currently tuple(read_series_tables(storage)) eagerly materializes full Arrow tables before conversion: iterate directly. Consume source windows once per required chronological request block and reuse additive states for related developing prefixes rather than rereading all earlier history per prefix. Year anchors must add atoms incrementally to AnchorTrades and all 3 TPO accumulators instead of holding all sparse-profile atoms in a tuple. Keep only compact provenance/membership where required. Persist each full result immediately and discard it; return compact refs/stat rows. Fixtures may explicitly request bounded in-memory payloads with a small hard cap; production default returns refs. Do not silently downsample requests/products to meet limits.
2. bar_proxy_atoms rejects the cross-file source-order reset even when AnchorTrades admits the explicit domain. All requested products must work with that admitted membership. Reuse bar_allocation_rows/geometry with compact completed OHLC+volume source records already checked by AnchorTrades, preserving the exact original proxy formula alternatives, all unknown/unpriced mass, members, source flat-bar loss etc. Do not retain full per-atom sparse profiles merely for bar OHLC comparison. Cross-file domain must retain unchanged original addresses. Do not fake same physical lineage or renumber to bypass the old guard. An optional narrow adjustment in owned consumer is preferable to changing unrelated kernels.
3. A reconstructed event/TPO cut fragment is not a completed native bar. bar proxies and PIN069 must only consume the original fully covered native-bar geometry after publication, within the scientific anchor, without including a whole native minute across a causal/mid-minute cut. Explicitly retain unsupported/partial-bar membership; do not pass all source_order=None reconstructed fragments as completed bars. PIN069 native-minute source remains its fixed UTC-4 window.
4. _covering_windows currently selects via domain key but never joins window receipt_sha256/coordinate_identity/span to that domain: enforce that join before composition, not string length alone. No source_path fallback may silently substitute an actual raw source_key for exact trade eligibility. Actual admitted catalog metadata supplies source_key explicitly. Empty retained trade series is different from missing cache: a completed 0-row source atom can still be reconstructed as observed empty with explicit coverage; missing cache stays unavailable.
5. _products expects tuple but production schedules are frozen JSON arrays: accept list/tuple with exact identical validation. Audit other JSON restoration shape assumptions (especially SourceOrderDomain).
6. cpu_components_disjoint currently includes total alongside components, double-counting total. Return total separately. Bind the frozen schedule identity and admitted receipt refs in results, keep raw contract and chronology/date grouping in compact rows; scientific missing cases stay explicit.

Focused tests with independent expected results: streaming full results are written before next request and not retained by default; previous source prepared batches are released before later source windows; two disjoint physical files with all profile/proxy products retain known mass; mid-minute fragment excluded from native-bar proxy with explicit coverage; empty cache vs missing cache; changed receipt/domain rejects; JSON-array products accepted. Keep existing literal source/equal-time/TPO/rolling tests. Tests are written only. Report exact remaining limitations honestly; missing efficiency implementation is pending, not scientifically unsupported.
