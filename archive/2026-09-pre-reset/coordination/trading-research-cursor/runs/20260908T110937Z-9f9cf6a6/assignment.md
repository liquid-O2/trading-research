# Consolidated controller repair: historical memory

Resume the same session; only auction_flow_memory.py and tests/test_auction_flow_memory.py may change. No candidate imports/tests/compilation/scans/execution or other edits. Controller read both complete files. Keep original arithmetic and reference producers unchanged.

1. Definite identity failure: the report id hashes slices/work, so one-pass and split reports get different IDs although tests require equality. Hash only scientific input/definition/window/quality/result content; keep execution counters separately. Preserve complete numeric, member and source lineage comparisons across arbitrary cuts, including ties.

2. Definite fixture error: empty_grid... uses quantities10, unpriced4 and small3 with minimum5. The current reason counters correctly count both4 and3 below the size filter, so expected below_filter_prints=1 and volume3 are wrong: they are2 and7. Reason counts are nonexclusive; do not erase the unpriced print from filter diagnostics. Keep unique excluded total separately. Check other literal expectations against their explicit inputs.

3. Remove full-input Python retention: _in_t/_in_size stage every trade even for time decay. Time decay needs no such history. For volume decay, maintain cumulative total eligible volume and the exact end-of-timestamp-group cumulative volume for each SELECTED print, with a pending selected group carried across reader slices. At finish, age = final all-trade volume minus its timestamp-group end cumulative total. Include every later small/unpriced eligible trade; all equal-time prints are excluded from each other's age, including ties crossing chunks. This preserves the original formula without storing all input timestamps/sizes. Retained input-dependent storage should scale with selected membership, not 50m Python records. Add long mostly-unselected and cross-chunk tie cases with explicit retained-state/work counters. Do not fake ages or use selected-only volume.

4. Remove redundant per-print metadata conversion passes. Validate source_key and raw_action/side correspondence on the bounded owned Arrow slice with vectorized checks, validate quantities in 0<size<2**32-1, flags>=0 <=255 and no snapshot bit, price_valid/domain, exact order/time/delay. Then update primitive counters/membership in one necessary per-print walk (vectorized exact aggregate counters also fine). Do not build per-row keys/actions/raw_side copies and a full selected_mask tuple list before replaying them again. Preserve nullable/unknown raw sides and uint64 addresses; no int64 narrowing or float sums. Add negative/zero/sentinel quantity, signed flags=-256 and forged mapping rejection tests.

5. Bind cluster IDs to exact selected member CONTENT as required, not only source_key/source_row. Current changed prices or sizes at the same supplied addresses produce an unchanged cluster ID. Retain member event/known time, price and quantity as well as address/flags/action/side; hash this frozen content once per cluster with the definition. Keep member ordering deterministic and batch-independent. Add same-address changed-content identity tests. This remains unpublished arithmetic, not source authentication.

6. Keep finite allocation bounds effective. During final grouping stop once distinct completed components exceed max_clusters rather than building every remaining component. Count actual inspected work and the first over-cap component honestly; don't claim a full cluster census after early abort. Avoid materializing an obviously oversized report when a bounded serialized-size accumulation can reject earlier; preserve exact final canonical-byte verification. Distinguish attempted union calls from successful component merges if retaining both counters. Preserve exact field/overflow conservation and original deterministic half-life accumulation. Update capacity fixtures to the actual charged failure work.

Keep full existing literal/original comparison coverage. No new generic framework, visited-clock substitute, future labels or canonical integration. Finish with the two files and a concise unexecuted-check caveat.
