# Consolidated controller repair and missing tests

Resume the same isolated book-recovery assignment. The initial worker reached its 1,800-second implementation timeout with the kernel written but NO tests file. No numerical work ran and no draft is accepted. Finish this coherent two-file task now; do not reread unrelated source/planning bundles. Scope remains ONLY auction_flow_book_recovery.py and tests/test_auction_flow_book_recovery.py, with no imports/tests/compile/scans/execution or edits elsewhere.

The controller read the complete kernel. Preserve the scientific policy and original references. Correct these concrete issues and write the required independent tests before stopping:

1. _project is not sufficiently sparse. np.union1d(inv_idx,rec_idx) loops over every bit4 row and every otherwise ordinary LAST quote in any batch containing an invalidation. A single gap near a 65,536-row ordinary batch can therefore trigger per-row Python work over all its unchanged quotes. Calculate blocked/ever-invalidated masks with vectorized latest invalidation/restart indices or an equivalent exact method, seeded by incoming state. Loop only over actual blocked-state changes (first invalidation opening an episode and first qualifying recovery closing it). Count repeated provider flags via vectorized prefix/group sums. Preserve first invalidation lineage, exact same-time physical order, original bits before the first invalidation, and all later candidate-valid masks. Add a work-count fixture demonstrating sparse Python episode transitions for a long clean/repeated-flag sequence, not a timing claim.

2. Continuation can falsely restore complete flow history after an initial clear followed by a flagged/unknown record in the same unresolved episode: _restore inspects only each episode's FIRST invalidation action. Enforce observed_history_complete against cumulative provider_flagged_records and unknown_action_rows, which cannot become complete again. Carry coverage incompleteness forward too: finishing one interval with coverage_complete=False must not let a later adjacent interval's True value claim complete cumulative history. Preserve book trust separately from history/coverage.

3. Strengthen the existing bounded carry validation without building an authentication framework. Before digesting/copying a carry, bound its episode list and string/address field sizes; reject unbounded/unknown payload shape. Then check internal invariants: all episode addresses use the single bound source_key; invalidation addresses actually have flag4, R or an unknown action; resolved markers have A/M/C, LAST, no SNAPSHOT or flag4; event/order/physical-row sequence is monotone, recovery follows its invalidation, episodes do not overlap, only the final episode may remain unresolved, and no episode address lies after the retained last source address. All cumulative counts are <=rows; episode provider-flag counts sum to the global count and include their flagged first rows. Empty-prefix state cannot contain episodes or nonzero counters. Include previous interval start/end (and original prefix start if counts are cumulative) so next_start==previous actual end==new start is explicit; do not rewrite old clock/lineage to manufacture adjacency. Retain the digest as an integrity check, not a venue/source eligibility certificate. A finite carry-byte limit may complement the existing 50k episode bound; no unbounded serialization before validation.

4. Validate the ORIGINAL book-valid claims too. Currently a trusted original quote with a crossed/zero/sentinel BBO or non-update action can pass through before the first invalidation. Reject original_valid & ~valid_update_domain before projecting. Check categorical raw_action/raw_side/source_key types rather than silently converting arbitrary numeric columns into strings. Preserve legitimate string/binary/dictionary categorical values and nullable unknown action/side; do not alter raw columns. Keep raw flags exact and bounded.

5. Preserve the exact Arrow schema, including book_valid's original integer type and field metadata. Current empty and nonempty returns always replace it with uint8 and a new name-only field. Return a new table with the SAME schema and only candidate book_valid VALUES changed. All other values, row order, multiplicity, field/schema metadata and input buffers remain untouched.

6. Write focused independent tests now. Use literal rows and an independent small scalar policy loop, plus the unchanged QuoteWindow for actual OFI/exposure checks. Cover: repeated flags are one episode; clear then bit4/unknown history remains false across carry; exact completed clean restart; snapshot+LAST/no-LAST/T/N/R/unknown cannot restart; crossed/zero/sentinel/locked BBO; second episode; same timestamps with order; one-pass/arbitrary batches/adjacent cut equality over EVERY column and logical episode/count/lineage field; false coverage then later true remains cumulatively incomplete; corrupted counts/actions/flags/key/order/carry adjacency; exact/one-over limits and poison; malformed original trust, mixed source/delay/nulls; original Arrow buffer/schema and returned-address isolation. In QuoteWindow the first restored quote contributes zero OFI across the gap, the next valid quote contributes the exact expected OFI, and standing time starts at the new ordinary quote. Its original descriptive book_recovery string belongs to the unchanged consumer; the controller will label the wrapped candidate measurement with this candidate policy. Do not edit that consumer just to change its label.

Keep code/test scope compact. No new source kernel, admissions, models, gross refill/hidden depth or exchange-history claims. Report that tests are written but unexecuted; controller will review and use the registered actual-source check.
