Implement the actual formation/window/forward-label/event timing statistics consumer for Deliverable 1. This is necessary because accepted atomic core statistics do not answer formation-length or availability-delay comparisons. Read START_HERE once, then the frozen /workspace/trading-research/validation/AUCTION_FLOW_WINDOW_STATISTICS_V1.json. That contract defines all calculations and comparisons; do not expand into Context, profiles, structure or memory.

Only two new owned files:
/workspace/coordination/trading-research-cursor/worktrees/window-statistics1/src/trading_research/research/auction_flow_window_statistics.py
/workspace/coordination/trading-research-cursor/worktrees/window-statistics1/tests/test_auction_flow_window_statistics.py
No imports, tests, scans, benchmarks or raw data reads here. Root owns registration, runner, full review, integration and execution. Use 1200 seconds to finish complete compact implementation and independent tests. No edits to canonical code.

API: run_window_statistics(*, population, contract, outputs, load_reference, selected_partition_keys=None) -> {passed, refs, counts, partition_measurements, family_complete:False, ...}. population is an authenticated auction_flow_window_population_v1 JSON manifest containing units (current pilot is in canonical reports/auction-flow-runs/bcb1b5c79cdf224e5756457d574e8cb0379b6c6e63fbb6c18f10d90e0179988b/outputs/window-population.json; full45 running). Each unit has source_identity, unit_id, features and labels ParquetSeries refs with schemas, row counts and exact hashes, instrument_count, cut_count, all coverage counts. Root calls your function only inside registered worker after own tests. outputs is existing BoundedOutputs. load_reference authenticates bounded JSON metadata only. Read series through canonical read_series_tables, authenticating exact hashes; no source extraction and no source MBP rescan. Full population ~4.1M feature rows /12.3M label rows, only a few GB compressed. Process unit tables once, vectorized NumPy where possible. Do NOT create 50M dict Cartesian join rows or read an entire multi-year population into memory.

Read canonical auction_flow_window_links.py schemas and join_window_eligibility (identity/stage only). Scientific eligibility additionally requires all feature quality flags, no censor/roll, and label.complete. A feature row has formation length/cut and last price strictly inside formation; labels have absolute future price extrema/last, not formation-relative displacements. Therefore derive displacement from each feature reference; compare formation alternatives only when same actual reference timestamp/order/price. Derive all timestamps as nullable exact int64, never through float. Price units are raw ticks=0.25 index point. No priced future trade is a separate complete outcome, not zero return; no-new-trade counts can be zero/one only on complete coverage. True CVD/OFI path extrema must use high-low, never close extrema. Cohort volumes overlap, not a partition. Standing/pressure use their own eligible masks.

Use the existing core economic_date, stage_name, classify_session and source_collections definition. Primary inference stays separate for monthly_acquisitions and weekly_acquisitions; verify no duplicate root/instrument/cut/formation inside a collection. Partition by collection/root/economic year, filter row years at economic-date boundaries, and record unassigned-stage rows separately. All source windows, unavailable units, missing and censored counts remain in denominators. Do not force the full 111-source intersection. Expected date universe must match the partition year/stage; gaps within that universe remain missing. Reuse core planning/helper patterns where appropriate, but do not import/run full core statistics again.

For statistics, use canonical observed_date_statistics in batches sharing one intended universe and _retain_replicate_estimates=False. Date means have one weight per date; cuts/overlapping windows are dependent. Need actual numeric estimates, date support, event support, missing counts and 5-date-block 95% intervals, plus declared quantiles. Preserve sufficient date sums/counts for all measurements and paired differences as Parquet with exact roundtrip. Report all retained candidates, not an outcome-selected winner. Contract's descriptive directional concordance is just a paired observed frequency, not model accuracy or predictive gain. Both zero past and zero future signs need explicit counts.

Latency comparisons: 0 and1s versus250ms on identical physical source/cut/instrument/raw contract, formation and horizon, equal feature reference, both eligible, with paired terminal/up/down/no-new-trade differences. Formation comparisons: 5/15/60/240 on same cut, target and reference; distinct own-support and common-support counts. Avoid counting each repeated target as independent evidence. Primary eligible-group statistics and paired contrasts must stay separate.

Scheduled events: read only the accepted observed-source-events.parquet referenced by contract inside run. Canonical scheduled_event_measurements.py _event_table schema documents it. Keep primary_population + independent_support; dedup semantic_id retaining all source provenance. Timestamp event_type and precision/status remain separate; date-only cannot enter timed proximity. No historical publication vintage exists: event labels are retrospective, known_at NULL, causal_feature_eligible false. Dates without records are unknown coverage. Keep event/no-eligible-receiver/no-window denominators so missing transmissions are not erased. Produce event-to-cut link table with exact source and interval IDs and relative buckets, and descriptive outcome/date stats per event type/bucket, with paired latency contrasts where supported. Do not infer surprise, actual release value or causal influence.

Outputs: per-partition group statistics JSON, date aggregate Parquet, paired contrast summaries and event links; global exact completeness/coverage manifest; readable results.md with actual numbers and explanations. Scope remains this window/timing block, not full auction family. Bound per-partition memory <2 GiB and whole worker <4GiB, output <4GiB for full result. If a group has no valid observations, emit explicit missing counts/undefined values rather than raising. Pilot operation must accept selected_partition_keys without inventing full completion. Return measured partition CPU/wall/output for root's full resource gate. Reuse accepted partitions by exact source/scientific identity if simple; don't build a framework.

Independent fixtures: literal future100->110 gives+10 terminal ticks and up10/down0; all future above reference clamps down to0; quiet complete has no-new-trade1/nullprice; missing is not zero. Known-at after cut, label before maturity, stage crossing, source hash mismatch, wrong contract, source-boundary censor fail eligibility. 0/250ms/1s matched latency changes use the same cut and reference. Formation comparisons with different prior reference are not paired. Equal-date mean on dates with1vs100 duplicate cuts stays balanced. Monthly/weekly aliases do not double date support. True CVD high7/low-2/close1 has range9. Distinct source cohorts overlap. Event atcut is excluded formation [start,cut); timed relative boundaries half-open; date-only remains date-only; missing event calendar is unknown; event with no receiver retained. Year/stage date universe excludes unrelated years but retains missing dates inside it. Int64 large timestamps survive roundtrip. Do not run the tests; registered root runner does that.

Return a concise report of owned files, API and exact remaining integration requirements. Root will inspect all actual code and execute it; your completion message does not establish acceptance.
