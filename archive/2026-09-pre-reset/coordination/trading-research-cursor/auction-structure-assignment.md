# Bounded implementation: full-source causal structure arithmetic

Use Cursor Grok 4.6 Extra High Fast. Implement only in this isolated checkout. You are the implementation worker; the controller owns science, actual-data eligibility, review, integration and execution. Read AGENTS.md; current phase is validation across all families, followed later by Context then Locations.

Allowed writes ONLY:
- src/trading_research/research/auction_flow_structure.py (new)
- tests/test_auction_flow_structure.py (new)

Everything else is read-only, including original measurements/structure.py, foundations/bars.py, prepared trade helpers, references, configs, validation and tools. No shell, candidate import, compile, tests, raw scans, numerical execution, benchmark, network, new dependencies, protocol changes or editing permissions. Write tests but do not run them. Stop with concise changed-file summary and precise limitations. This is an unpublished arithmetic kernel for a later registered actual-data validation, not a F09 admission certificate, Context model, location-quality claim or completion of all M03/M08. Do not add models, FVG/block heuristics, or unrequested framework abstractions.

## Current need and references

The shared exact source path retains immutable bounded PreparedTradeBatch arrays and actual raw-source lineage. The old scalar DirectionalChanges and pivot_reference in measurements/structure.py already define fixed-threshold trade-path swings and final-bar pivots. They are correct independent comparators but retain per-print objects/seen dictionaries. Implement the same calculations for the full bounded source stream with streaming state and exact output, plus bounded final-bar pivot processing. Reuse these original reference functions unchanged in tests. Test literal independently specified expected cases as well. Do not call a candidate inside a purported independent expected-value implementation.

Read auction_flow_windows.py PreparedTradeBatch/TRADE_FIELDS and existing relevant structure tests. A batch has immutable integer values including t, known, price, price_valid, instrument_id, source_row, source_order, raw_flags; source_key(index) returns the acquired stream identity. Raw side/action are retained, prices valid exactly at positive ticks <2**53, source_row may be uint64 above 2**63 and source_order nonnegative. Do not narrow unsigned addresses. Actual batch size is at most 65,536. All other numerical columns must remain unchanged and inputs must remain immutable.

## A. Streaming fixed-threshold directional changes

Provide a typed frozen definition (version, positive exact reversal_ticks as int or Fraction, frozen_at) and a stream taking raw instrument ID, source_key, half-open [start,end), constant delay_ns, a bounded nonempty tuple of distinct definitions (max32), and explicit finite source-row/emitted-row capacities. Keep positive timestamps and exact types strict (bool is not an int). Fixed Fraction thresholds permit exact source-admitted externally scaled thresholds without this kernel claiming that scale training is authenticated. Report actual threshold and definition identity in every row. Definition frozen_at must be <= start. Do NOT implement adaptive updates of a threshold mid-leg.

Expose add_prepared(prepared,left=0,right=None,*,history_complete=True) -> immutable tuple of newly emitted swing records, record()/snapshot() -> copied immutable/serializable current state and counters. Accept only the actual unreleased PreparedTradeBatch class and proper slice bounds. Validate source/instrument/window/delay, increasing event time with strictly increasing source_order and source_row across and inside calls; same-time prints are legal in known physical order. Reject out-of-order/repeated rows, mixed acquired source, wrong raw instrument, invalid delay/time/order/address or use after failure. Validate before modifying state where feasible; poison on error so partial state cannot be reused. The prepared arrays are private owned source projections, not a license to bypass guardrails for callers.

History completeness is a controller-provided segment mask, not something inferred from clean trade flags. A raw book-gap flag may arise between trades. The controller will split source slices at verified quality boundaries and pass history_complete=False for unsupported segments. False resets provisional state and emits no swing from those rows, exactly like scalar PricePoint.complete=False. Also treat any individual raw_flags & 4 and unpriced trade as an incomplete/reset observation even if segment argument is true. Preserve counts and reason counts for every row, including incomplete/unpriced rows. Unknown aggressor does not invalidate a priced price-path observation. A subsequent explicitly complete segment starts a new leg; never bridge the reset or infer recovered historical trades. Do not call this a venue completeness certificate.

For each definition, implement scalar recurrence literally: initial direction0/high=low=first eligible price; strictly higher/lower updates retain first equal extreme; test high reversal when direction>=0 before low reversal when direction<=0; on confirmation set direction opposite and high=low=current point. Exact >= threshold boundary using integer/rational arithmetic, no float. Confirmation known time=max(extreme.known,current.known). Return side, exact price, extreme event/known/order/source address, confirmation event/known/order/source address, threshold, definition ID, and current segment/leg origin (first point after a reset, then the previous emitted extreme, consistently documented). Retain the selected point records only as live state rather than all input events. Output is append-only; earlier returned records cannot change when later data arrive or mutable record output is altered by a caller.

Keep method output order deterministic by source confirmation order then definition tuple order. Counters cover actual rows/slices, per-definition source visits, resets, confirmed high/low and emitted rows; count before terminal resource error. Permit output streaming: do not retain all previously emitted objects inside the stream. Finite default <=50,000,000 source rows and <=500,000 emitted rows overall (caller may choose smaller; no unlimited setting); reaching exact cap succeeds, one-over fails without silent truncation. Max32 independent scales, one source pass, no per-row source digest/replay or object for each definition when it can share the same selected source point. Do not promise speed before measuring.

Snapshot exposes current direction, provisional high/low/last and complete/incomplete counts plus current threshold, but no generic checkpoint/restore framework is requested. Cross-window continuation can be added by controller only if required; do not invent serialized authority objects.

## B. Bounded causal final-bar pivot stream

Use the existing CausalBar as the input type. Provide typed pivot definition version,left,right,ties,frozen_at; lengths 1..1000; ties strict/earliest/latest/ambiguous. A stream binds one instrument, fixed nonoverlapping cadence/window and definition, and consumes one final available version per bar in increasing interval order. The actual source adapter will create bars from retained exact trade geometry with input provenance. This arithmetic layer must explicitly state it does not authenticate F09 producer recipes.

Match pivot_reference on the supported append-only finalized bar domain, including incomplete and empty bars in the cadence so they invalidate any supporting pivot window instead of becoming a shortened lookback. No pivot before the complete right support has been observed/published. Do not accept revised or duplicate bar identities in this streaming path: raise a clear error that revisions require the separately replayed as-of reference, preserving earlier outputs. Record max actual publication of all supporting bars, center interval, all supporting bar version IDs, and the original scalar definition/output identities wherever exactly reproducible. Reject raw-instrument changes, non-final input, overlapping/backward bars, publication earlier than final end, definition frozen after support start, and capacity errors. A time gap invalidates any window crossing it; do not invent synthetic bars or pivot order. Independent left/right definitions are separate streams; local deque stores at most left+right+1 bars and emits at the arrival of the rightmost bar. Source event and known time differ.

Use original strict/earliest/latest/ambiguous tie semantics for high AND low, including a bar that is both a local high and low. All missing/unpriced/incomplete support yields no pivot and explicit counters. Expose actual bar visits, valid/invalid support windows and outputs. Default bounded <=100,000 bars and <=200,000 emitted records, exact cap success, one over fail. Every result remains tagged unpublished arithmetic with exact input lineage.

## Required independent verification code (do not run)

1. Literal trend/no-confirmation, high then low, initial upward/downward tie priority, exact threshold and fractional threshold, equal extreme first occurrence, same-time source order, unknown aggressor, no priced rows, flag4 and segment-completeness reset. Every swing field, origin/delay and provisional state asserted, not just count.
2. Fixed integer cases compare the entire corresponding Swing geometry and clocks against ORIGINAL DirectionalChanges consuming original PricePoint objects; do not alter the reference or skip it. Fractional thresholds compare a simple independent literal recurrence. Deterministic randomized small paths may complement explicit cases.
3. Batch/slice cuts at every point, a cut within a timestamp tie group, 65,536 boundary, high uint64 source addresses, empty slices, mutable original Arrow buffer independence, released/mixed/forged metadata rejection, exact limits and one-over, frozen rule and constant delay.
4. Future suffix leaves earlier returned results byte-equal; after error every subsequent use rejected. record output mutation cannot mutate retained state.
5. Pivots compare complete records to ORIGINAL pivot_reference on all tie rules, multiple left/right, gaps, incomplete and quiet bars, missing high/low, delayed publication, both-high-and-low center, no early publication and revisions rejected. Exact deque/work and resource boundaries.

Do not fabricate an actual market result, timing, scaled training recipe, tree relation, divergence, bar producer admission or family completion. The controller will review these files, add a complete real-source probe, and execute one combined registered validation. Preserve all original scientific references and current budgets.
