# Validation implementation: prepare exact trade batches once

The user requires all-framework validation first, then Context, then Location. Optimize only the existing auction/flow validation extraction path. Another worker is independently changing structural storage; do not edit that code.

Work in this isolated checkout. Edit only:
- src/trading_research/research/auction_flow_windows.py
- src/trading_research/research/auction_flow_measurements.py
- src/trading_research/research/auction_flow_pipeline.py
- tests/test_auction_flow_prepared_trades.py (new focused tests)

No shell, imports/tests, scans, fits, protocols, budget changes or other files. The controller handles the registered check and integration. Use as many allowed tool calls as needed to deliver the full bounded change, then return a concise patch summary and limitations.

Measured issue: InstrumentWindows.trade_rows passes one source batch to whole_tape, native, and each 60-second TapeWindow. TapeWindow repeats Arrow conversion/source validation for every slice; each of its four OrderedFlow.add calls repeats size/side/order validation and cohort membership. The check7 projection assigns 29,956 CPU seconds to trade_atomic_consumers, versus 3,378 to whole_trade_and_native_consumers. Native arithmetic is already vectorized and should remain unchanged. Source projection is already separately validated, but public arithmetic boundaries must still reject malformed data.

Implement a bounded PreparedTradeBatch in auction_flow_windows.py, analogous in purpose to the accepted prepared-quote path. Prepare one <=65,536-row projected Arrow batch into explicitly owned, immutable integer arrays, original source-key/address access and any useful shared masks. Keep exact int64 clock/order/quantity/price values, raw side/action/flags and unknown side handling. Validate the complete source projection once: fields/nulls/types, eligible quantity, side mapping, T action, non-snapshot flags, instrument, original source order and price-valid domains. Window-specific bounds/delay, cumulative maximum counts and cross-batch continuity must still be checked by each recipient. Preserve exception poisoning and empty-input behavior of existing public TapeWindow.add.

Expose an explicit add_prepared(prepared,left,right) route on TapeWindow. Its numerical work must be the same ordered path, profile mass, marked-size/interarrival samples, exact squared/absolute variation, same-side runs, first/last/priced observations and first-attained extrema as the current _add. Public add(table) remains compatible and checked. Prepared handles must reject use after close and invalid or non-integer slice coordinates. Do not retain a source batch past its bounded consumer lifetime or let caller mutation of the original array alter prepared values.

Share eligibility/array validation across the four flow channels without making unchecked raw-array APIs callable by accident. A narrowly scoped prepared-array/validated-input helper in auction_flow_measurements.py may support this; ordinary OrderedFlow.add and SparseSideMass.add must retain their full existing input checks and exact behavior. Do not weaken accepted integer domains, truncate huge Python opening offsets, use floating histogram weights, rewrite the cumulative kernels or replace observed extrema with closes. If a larger refactor would be needed for one consumer, preserve that consumer and report the remaining cost.

Wire InstrumentWindows.trade_rows to prepare once, feed whole_tape through the same prepared route and send native.add_trades its original table, then iterate exact atomic slices without redoing Arrow conversions. Keep the current trade_atomic_parts count, source/coordinate checks, carry, quote path and all measured populations unchanged. Charge preparation to the existing whole_trade_and_native_consumers region so CPU regions remain disjoint and no new cost disappears from source_cost_projection. Do not move validation cost outside measured regions.

Focused tests compare the complete published records against an independently loaded frozen ORIGINAL windows/measurements implementation, including multiple reader/atomic cuts, equal timestamps, strict source order, unknown sign, unpriced observations breaking price adjacency, repeated same-side runs, source filter boundaries 29/30/60/61/74/75/99/100, huge exact opening/quantity moments, actual null handling, different instrument/delay, snapshots and failures that poison publication. Preserve the original file copies as fixtures only if the supervisor supplies them: do not copy code into a supposed independent reference yourself. You can load the controller-provided references/auction_flow_trades_check7 modules via importlib using their manifest, or independently specified literal expected records if unavailable. Count preparation/conversion calls so two atomic windows share one prepare; do not assert synthetic speed.

All source versions/scientific schemas remain unchanged. The full registered check will compare every value from all nine retained market resource windows and three continuation splits, so there is no representative-only acceptance. No performance claim before that result.
