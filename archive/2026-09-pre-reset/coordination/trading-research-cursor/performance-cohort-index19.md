Implement exact per-instrument interval slice indexing to remove repeated batch searches in current auction cohort preflight. Scope only src/trading_research/research/auction_flow_cohort_preflight.py and tests/test_auction_flow_cohort_preflight.py in this isolated workspace. Parent owns registered execution/integration. No tests/imports/scans, no shell/network, no other writes. Read these supplied current files; parent has read current plan and owns all scientific definitions. Relevant full nine source-window cohort validation retains all eight hard/soft/fitted definitions, complete whole/minute/scalar checks. Baseline hashes in assignment below.
Measured problem: _add_slices loops all batches for each whole/minute/scalar prefix and each definition. ES 23h with 1380 minutes,108 batches,8 definitions causes ~1.19m probes. Existing _decode_current validates monotonic t within/across batches and strict source ordering. Keep its validation.
Build a small private interval index once per instrument after _prepare_groups. Store existing nonempty batch references and tuples of first/last t, no copying data/sorting/concatenation. For [start,end), first=bisect_left(last_timestamps,start), stop=bisect_left(first_timestamps,end), call existing _bounds on only those batches and keep nonempty (batch,left,right) slices. Cache tuples by exact (start,end) per instrument. No moving cursor: requests return to earlier prefixes. Empty/backwards intervals must yield no slices consistent with relevant caller contract. Preserve timestamp ties across batches, include start/exclude end, distinct source/instrument ownership, and availability clipping.
Change _add_slices and its three call sites to consume index; still call window.add_prepared every slice/definition and preserve every existing count and output field exactly. _collect_scalar_probe stays unchanged as independent literal validation. Retain _release ownership, index must stay inside unit and not survive release. No new output instrumentation fields altering complete record parity.
Tests with independent literal [start,end) filtering: split ties, exact boundaries, empty batches/instrument, gaps, clipped minute, whole->minutes->earlier-prefix ordering, isolated instruments, cached/uncached source order and contributions. Instrument _bounds in tests only to show repeated intervals reuse and bounds calls scale with intersections. Existing real nine-unit full semantic parity run will validate integration. Keep helper simple, avoid frameworks, no speculative guards.
Return changed files/API, no further proposals or repair loop. Finish within 600s.
{
  "src/trading_research/research/auction_flow_cohort_preflight.py": "48ddf6439b67e7a1461ab366cd09499eaa20cf47ebce957b369cf00a5e57f9c9",
  "tests/test_auction_flow_cohort_preflight.py": "3ac09972788a0a35e3bab9d782b297e27866d528980739bb17b83a9e5d392e5d",
  "src/trading_research/research/auction_flow_cohorts.py": "0052077c0d74282495d3e5d50ff084a1870e2c5f5ed815518a1859076e57ad5b",
  "AGENTS.md": "73f23fe6b1d5e85c21d99d64f932a93196fe91a68f8b9b08d6c6641e963e0c38"
}
