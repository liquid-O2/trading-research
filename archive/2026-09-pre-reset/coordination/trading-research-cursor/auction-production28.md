# Complete a production auction/flow source controller

Implement one coherent production source controller through the EXISTING registered auction family runner. This enables full extraction while downstream statistics are implemented, preserving all science and acquired variants. Read /workspace/AGENTS.md and /workspace/planning/trading-research/START_HERE.md once. Codex owns science, resource allocation, integration and actual registered execution. No shell/imports/tests/scans/network or canonical writes from this worker. Only owned files below may change. Do not modify the clock27 code being implemented separately.

Current reusable pipeline: measure_raw_window returns full source/native/atomic measurements and `measurement_continuation`; next adjacent window receives that full carry. QuoteReplaySeries receives ONLY the same initial carry's `source`, never terminal carry. ParquetSeries writes exact trades/exclusions/native; QuoteReplaySeries preserves exact reconstructible quotes, finishing against ORIGINAL producer manifest. BoundedOutputs guards files/total and verifies compressed measurement equality. plan_source_windows() supplies the complete frozen 3589 source/day windows across167 distinct physical files (including quiet/partial days and all overlapping acquisitions separately). Reuse frozen inventory/schedule evidence instead of rereading all source identities. measured_unit() is a verification wrapper with literal prefixes and duplicate plain JSON: DO NOT call it for production. Its underlying numerical pipeline and storage are reusable.

Parallel source semantics: one child task per physical source file, processing that file's scheduled days in chronological order with actual carry. Different files run concurrently, up to17 under existing hardware helper and live17.85-core/83GB allocation. A calendar cut never creates book recovery or a new observed prefix. File starts are explicitly observed prefixes as in the existing registered schedule; this is not a cross-file continuity claim. Preserve NQ/ES raw-contract coordinate boundaries, all missing/quiet/failed/no-event cases and overlapping variants. No sample substitution or arbitrary source filtering.

Required implementation:
1. New auction_flow_production.py: construct and validate exact file chains from a supplied frozen complete schedule; produce a full source window with required exact trade/excluded/native files, quote replay and compressed full measurements. Source unit identities and source_metadata hashes must join protocol/index/schedule. Bind full clock policy, grid/delay, runtime and relevant producer dependencies. Retain counts, source manifests, artifact hashes and actual initial/final continuation identities. Persistent per-window completion receipt is published only after all required artifacts/replay finish successfully. Keep terminal measurement carry for the next day. Avoid duplicate full plain JSON, literal prefixes, repeated scientific comparators or downstream-fitting here.
2. Resume only complete contiguous leading windows whose original producer receipt/code/runtime/source/definition and artifact hashes still match. Read original immutable artifacts in place; do not copy/recompute accepted windows. Authenticate carry chain, parameter identity, schedule membership and actual source coordinates. A partial window is pending and its output/CPU remains charged by its old attempt. Do not overwrite old attempt outputs or reset budgets. Reused windows must have evidence of a registered prior attempt (even if its overall file later failed); no naked arbitrary JSON receipt is sufficient. A changed unrelated consumer should not invalidate a producer: use relevant source dependency identity, not a blanket repo hash.
3. Add `extract` and `extract-check` dispatch to tools/run_auction_flow_study.py, including a production file child entry that authenticates the existing live parent/supervisor/code snapshot/runtime/registered attempt and compiled library before any candidate processing. Reuse supervise_source_children, process-group cleanup, wait4/live CPU accounting and hardware admission. Each file child validates its complete frozen job reconstructed from the parent plan. Keep per-window receipt files and a parent pool checkpoint even on failure. A file failure blocks its later carry-dependent days; healthy other files continue. Report all failures and pending units; full source completion only if every required window has a new or authenticated reused complete receipt. No family-statistics or three-hour claims from source completion.
4. Production allocation is supplied prospectively by Codex in check extension `production_limits` and a frozen production plan/reference, not invented by the worker. Validate caps against SAME family total192000 CPU seconds,32attempts,256GiB output less retained consumption; never reset/rename family or alter frozen protocol bytes. The original planned14 extraction partitions may be packaged in one source pool within unchanged TOTAL allowances. `extract-check` uses a bounded explicit small fixture/schedule check and registered tests; `extract` requires the complete declared schedule. Fail closed if the required production allocation/plan is absent. Codex will freeze these before execution. Do not launch or claim authorization from code alone.
5. Per-file output reservations may differ; the frozen plan supplies a bounded maximum_output_bytes for each file, and their sum plus coordinator reserve must fit the current attempt allowance. Do not reserve512MiB*3589 independently or split every day into a fresh cold child. Reuse BoundedOutputs inside each child, file<=512MiB and memory<=4GiB. Join actual output bytes from all completed/failed children once; detect overruns. Quote/native retention and final report count against reservations. Parent total CPU includes all live/reaped children and compiler; inherited rlimits must not be raised above authenticated family budget. Existing bootstrap hard CPU1210 prevents full production after authentication: adjust this carefully to allow authenticated extract caps within the unchanged family total while retaining a small pre-authentication SOFT limit and inherited tighter parent bounds for normal tests/benchmarks. Preserve existing mode behavior and tests.
6. Pending clock27 will add `source_clock_policy='redundant_backward_snapshot_v1'` options to measure_raw_window and QuoteReplaySeries. Default strict remains usable. This named policy retains every raw field/address/count and only separates exact BBO-redundant backward A/N168 snapshots following adjacent same-instrument A/M/C128 equal valid BBO; it adds no clock shifts/recovery. Pass chosen policy through production, receipts and source/measurement/quote-replay continuation. Do not implement the policy here. Source-clock mode currently in your baseline runner is Codex's pending verification wrapper; preserve it.

Tests: meaningful literal two-adjacent-window source fixture verifies second window seeded with true first-window carry (including prior invalidation/age), exact cached trades/native/quotes/counts, output receipt completeness and no duplicate plain measurement artifacts. A restart reuses completed first window without source computation and executes only the remaining window, preserving prior bytes. Tampered carry/receipt/artifact/source/policy, noncontiguous reuse, missing required source/day and overlapping file lineages reject. Complete schedule file grouping must conserve all windows. Test per-file reservations and multi-child success/partial-failure accounting using bounded subprocess fixtures under the registered test mode; avoid starting the full actual data. No implementation-mirroring test volume, no new generic execution framework.

Stop condition: source production controller, registered parent/child dispatch, exact per-window artifacts/carry/reuse and concise independent tests ready for Codex integration. Return changed files, interface/config schema and known limitations. Do not invent downstream findings or implement anchor/cohort/statistics controllers in this assignment. Time allowance1500seconds; return coherent code, not another planning document.

Owned files and canonical baseline SHA256:
- src/trading_research/research/auction_flow_production.py: None
- tests/test_auction_flow_production.py: None
- tools/run_auction_flow_study.py: 907e45e5c5583052de69d4ce1f64d792552377c91c1300434bc1423e17556a1c
- tools/auction_flow_parallel.py: 8b7ec23067f588c7a2836848971ea67f5656caff874ce36983ec58b65e122b73
- tests/test_auction_flow_parallel.py: 374c8bf08c9a5ca36f15b8414597a1166cc0e0c13a835b6b82db7e47833f409e
