# M12 complete reference measurement engineering v1

Scope: M12 and M12.REFERENCE_TYPES, all sixteen P0–P7 phase IDs. This adds the entire remaining measurement design to the already frozen M12/L02/L03 implementation family without altering its original goldens/protocol. Retain all 81 finding rows (66 directly routed, three PIN019 parent additions, twelve INV) and exact completed original-reading evidence. Forty-one new MR cases are independently hand-derived before this extension's candidate implementation. Every original M12/L02/L03 case remains mandatory.

Implement all M01–M13 in one batch, then one whole-batch static review, one consolidated defect repair, one shared bounded verification. No candidate import, compile, test, model fit, market scan or benchmark before that shared review/repair. Registration hashes and retains existing bytes only. Resource limits: 180 CPU seconds reserved per attempt, 190 hard CPU, 240 wall seconds, 4 GiB address space; maximum three attempts and 600 CPU seconds per family. One shared worker has one physical identity; family telemetry is not summed as separate physical work. Failures and interruptions remain retained.

The full adjacent design defines authentic early/session/period opens, period calendar traversal, supported prior scales, typed reference graph/algebra, actual F02/F04 bridge decomposition, source interpolation/floor pivots, actual F10 early-open publication and typed fixed-end gap outcomes. Raw coordinates, open/close/official-settlement/source-specific conventions, clock versions and observation/publication dates remain separate. No missing official reference is replaced by a trade close. No caller-created receipt or support flag authenticates a producer. Existing legacy wire schemas remain unchanged.

The child target set includes every 5/15/60-minute and session-remainder endpoint, frozen at the original cut. Gap held, actual traded fill, traversal across the gap, strict terminal close through and hold after contact are separately defined in the design and MR41. Complete quiet no-fill stays in the denominator. Pending/censored outcomes are unavailable, partial observations remain labeled partial facts, unknown order retains bounded compatible outcomes, and future results cannot enter opening features. Corrections append later reference/target versions and preserve prior target/TTL birth.

Limits are retained in the design/gold: references256, edges4096, calendar days366, source observations4096, identities4096, path points4096, ambiguous batch8, states4096, transitions65536, identity UTF-8 bytes256, total retained bytes8 MiB. Validate before copying/encoding/replay. Exact capacity plus one-over must use the same frozen configuration and reject before mutation. Rational arithmetic is exact; independent references operate on primitive values without invoking candidate reducers. All forty-one MR IDs bind to full named unittest assertions, and all source rows have narrowly scoped implementation/evidence dispositions and remaining requirements.

Run the complete earlier 563-test baseline plus current C01/M12/B00/V01 and every new M family once under the shared supervisor after the combined review and repair. Synthetic correctness supports exact engineering claims only. Actual data certification, calibrated conditional gap/return probability or survival heads, comparisons on common dates/candidates, downstream forecast usefulness, full one-account/one-mini policy economics with costs/execution stress, and actual future captures remain pending their evidence. No source-wide/phase-wide completion or profitability claim follows from case coverage. No account, purchase or order action is authorized by this protocol.
