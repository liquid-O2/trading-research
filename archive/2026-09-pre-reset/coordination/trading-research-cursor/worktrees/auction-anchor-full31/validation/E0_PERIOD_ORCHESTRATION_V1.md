# Frozen new-family resource contract

This engineering family binds six independent literal case groups to the V03/V08 resource requirements. Golden vectors are fixed before candidate implementation or imports. Subsequent primary-period and E0 integration families may use explicit `engineering_batch_contract_v2` with 180 CPU seconds, 190 hard CPU seconds, 4 GiB address space and 360 wall seconds per physical full-suite attempt, maximum three attempts and 600 CPU seconds per family. The completed 851-assertion run used 97.725895 CPU and 217.32050153892487 wall seconds; growth motivates elapsed-time headroom. Original v1 registrations remain 240 wall seconds and cannot be reclassified.

All and only frozen family bindings plus all 851 prior passing baseline assertions must run in one complete supervised suite. One physical worker is charged conservatively to each participating new family, never summed as physical telemetry. Inspect full implementation and tests once, collect findings, make consolidated static repairs, then execute a registered attempt. Failures consume attempts and retained observed resources. No private candidate imports, compilation, isolated tests or reruns. Resource-limit child processes appear only inside the registered test worker. Actual metrics must remain strictly below each accepted limit; types, missing values, nonfinite values, mixed registrations and forged result contracts fail closed.

The six cases cover legacy preservation; explicit v2 schema; final exact boundaries; immutable family preflight; actual child-process OS limits; and final worker/supervisor/attachment agreement with all existing assertions. Successful measured evidence must name exact assertion IDs and source bindings, retain failed receipts and immutable versions, and claim only finite engineering support. Recovery resumes the same lifecycle and does not spend an extra attempt or start another worker. No new raw data, market fit, economic trial, order or account activity is part of this family.
