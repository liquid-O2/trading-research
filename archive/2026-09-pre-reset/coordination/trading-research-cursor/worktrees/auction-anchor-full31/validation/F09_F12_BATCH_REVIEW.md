# F09–F12 shared engineering batch

All four source preparations, protocols and 125 hand-derived golden cases were
registered before candidate implementation. The seven complete initial reviews
collected 92 distinct findings before any repair. One consolidated repair pass
then addressed every finding, with exact dispositions and final source identities
in [the batch record](../reports/f09-f12-batch-review.json).

The batch covers shared trade bars and activity clocks (F09), immutable object
lineage and archived history (F10), committed artifact/input/fit closure (F11),
and deterministic arrival-trace adapters with local raw-journal boundaries (F12).
The mapping preserves every frozen case, all 102 prepared source cases and every
actual test method. Root and delegated static confirmations preserve their exact
scope; no execution success is inferred from review.

The upcoming single full-suite run is registered under four families. Its one
physical resource observation is charged conservatively to each family and is
never summed across the duplicated family records. The worker has a 180-second
aggregate CPU guard, a 190-second per-process hard CPU limit, 4 GiB per-process
address-space bounds and a 240-second wall limit. Durable lifecycle recovery
does not rerun assertions. Metrics are accepted only with their exact successful
assertions and frozen finite schemas.

At this pre-execution checkpoint there have been no candidate imports, compiles,
test runs, market-data reads, model fits, orders, external captures or economic
evaluations in this batch. Exact source-specific measurements, native/cohort
admission, complete consumer integration, calibration and future-observation
gates remain separate work. These engineering assertions do not close an entire
parent, child or phase.
