# Current trading research instructions

Read and apply [the workspace working rules](/workspace/AGENTS.md) first. This file supplements those rules with research requirements; it grants no exception to them. If project guidance appears to conflict with the workspace rules on scope, necessity, efficiency, verification or continuation, follow the workspace rules. Later explicit user instructions take precedence over both files.

Read `/workspace/planning/trading-research/START_HERE.md` once on entry for the current task, relevant specification, evidence/budgets and Cursor workflow. `/workspace/trading-research` is the canonical implementation; do not create a replacement project.

The user requires **validation across all intended families/frameworks, then Context, then Location**, retaining full scientific quality and actual algorithms/source alternatives. Inventories, fixtures, proxies and small pilots do not establish complete family findings. E0/account/broker/Response/live systems are later work, not current research gates. Use 2020 onward; older Jumbo/OHLC work remains separately scoped.

Reuse admitted data; convert columns once, use vectorized/compiled computation where measured, and verify realistic throughput before scale. Preserve the complete scientific acceptance criteria when optimizing execution.

Use Cursor Grok 4.6 Extra High Fast (`cursor-grok-4.6-xhigh-fast`) for coherent bounded delegated implementation in isolated checkouts. Codex retains scientific definitions, task selection, review, integration, registered execution and acceptance. The exact process is `/workspace/coordination/trading-research-cursor/HANDOFF.md`; stale worktrees and stopped probes must not be resumed automatically. Necessary small direct corrections are permitted to avoid another worker loop.

All candidate imports/tests/scans/fits/benchmarks execute through their applicable registered family runner. Preserve all failures, attempts, CPU and output; do not reset a budget or rename the same workload to evade it. New scientific questions may receive their own honest bounded registration under current task authority. Pure organization, static AST, metadata and hash/link verification are not research execution.

Use the user's 21-vCPU/83-GB/RTX-A4000 allocation subject to effective 17.85-core and 82,999,996,416-byte limits and tighter study/process caps. Preserve explicit approved budgets in the current state and live registries. Do not infer this allocation from host-wide hardware totals.

Do not modify original `/workspace/data`, `/workspace/sources`, frozen historical plan bytes or immutable accepted/failed evidence. `/workspace/archive` is excluded. No paid expansion, external trades, messages or live deployment is authorized. Later user instructions supersede this file.

The source timestamp/performance repair is complete and verified in attempt36. The latest user instruction at September8 20:41UTC explicitly resumes full Deliverable1 scientific work, then Context. Follow CURRENT.json for its acceptance conditions; the earlier performance-only boundary is superseded. Reuse accepted source extraction and preserve prior elapsed time, failures and budgets.
