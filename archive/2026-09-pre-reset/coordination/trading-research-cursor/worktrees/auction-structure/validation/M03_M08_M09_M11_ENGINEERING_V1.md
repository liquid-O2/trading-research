# M03/M08/M09/M11 engineering v1

Scope is all nine listed parent/refinement units and all 72 P0–P7 slots. This packet registers source-faithful deterministic measurement implementations plus explicit substantive fitted ports; it does not close empirical or future phases.

Before any family candidate execution, retain this protocol, the 70 independent hand-derived cases, all 185 source findings and their completed original-reading evidence, the full source-derived design and common/anchor interfaces. Original PDFs, embedded code, conversation exports, planning documents and the excluded archive remain unchanged. Hash-verified prior completed readings are reused without claiming another reading.

Implement all remaining M01–M13 measurement families as one batch, then perform one whole-batch static review, one consolidated defect repair, and one shared bounded verification. Existing initial C01/M12/B00/V01 review reports remain evidence for that same consolidated repair; there is no new per-family test loop. No candidate import, compile, test, model fit, tape scan or benchmark is allowed before the shared reviewed execution. Preparation/registration only hashes and retains bytes.

Hard runtime limits: one shared execution identity; 180 CPU seconds reserved per attempt, 190 CPU hard stop, 240 seconds wall, 4 GiB address-space limit. Each family has at most 3 attempts and 600 CPU seconds. Repeated family charges for one shared worker are not separate physical runs. Every actual failure/interruption remains recorded with measured resources and retained artifacts; no silent retry. This engineering run reads synthetic fixtures only and performs zero market-tape/economic evaluations.

Implementation API and semantics are pinned in the complete adjacent design. Actual F09 time-bar publications and exact coverage authenticate swings; shared actual F05/legacy captures authenticate flow and memory; original normalize_mbp recipes and actual BookReducer replay authenticate quotes. Actual F10 geometry/lineage and actual scheduler completions gate consumers. Arithmetic equality or a copied dataclass/certificate string does not prove source eligibility. Raw/contracts/ticks/reset/cohort/date identities remain distinct, unknown side/order/history are explicit, changes append subsequent versions, and earlier cuts remain immutable. One account, one mini, no order/account/purchase actions.

MS fixed-bar and directional-change variants retain their registered tie/confirmation/leg-birth scale rules. MD endpoint comparison variants, event-versus-knowledge lag, complete observed nonbreach and cohort shared evidence stay explicit. MQ exact OFI, duration versus update denominators, fresh versus copied quote age, constant-price execution-adjusted net recovery and masked unidentifiable gross flows remain separate. MM whole-print connected clusters, forward merge/split, normalized sparse spatial fields, elapsed/activity/visit decay, retests, independent matured markouts and strict preceding-episode retrieval retain actual source versions.

Default finite bounds: 4096 input events, 8 MiB recipe bytes, 32 swing scales, 1024 retained swing/cluster versions, 4096 sparse cells, 1024 training rows, 16 fitted basis columns, 128 retrieval neighbors. Exact-capacity cases must succeed under the same configuration used for one-over rejection. Bound sequences before copying/replaying/sorting, stage mutations atomically, and count actual work. No silent eviction or invented constant-time claim.

Every learned consumer reopens an actual SemanticArtifactStore CommitRef containing the complete TemporalFold population, exact training IDs/date groups, matured target LabelEvidence and actual training ReadSession manifests. It reconstructs state from the retained admitted rows and recipe, compares exact payload bytes, and validates original OOF/downstream routes before serving. MX08 applies the same real-store discipline through M03 ridge residuals, M09 lag transforms and M11 distance scaling/retrieval. Pure numerical kernels return unpublished results and cannot certify training by themselves.

All 70 case IDs retain their literal inputs and expected values before implementation. Rational results compare exactly; exponential results use absolute tolerance 1e-12. The literal reference uses primitive tuples/dicts and arithmetic and does not call candidate reduction/selection/fit functions. Tests must materialize the actual producers and consumers, including causal suffix/correction/restart, malformed/forged inputs, source-unit/clock mismatch and resource boundaries. The case map binds every case to concrete full unittest assertion IDs. Every source finding gets narrowly scoped implementation/assertion dispositions and explicit remaining requirements; string presence and broad test names do not close sources.

P0/P1 correctness and P2/P4 deterministic representation comparisons require the shared execution. Empirical calibration/support, fitted challenger comparisons and interactions, downstream fixed-opportunity usefulness, complete policy economics with costs and execution stress, and genuine future captures remain pending their actual data and chronological prerequisites. No synthetic correctness result establishes profitability, institutional identity, observed inventory or future trading success.
