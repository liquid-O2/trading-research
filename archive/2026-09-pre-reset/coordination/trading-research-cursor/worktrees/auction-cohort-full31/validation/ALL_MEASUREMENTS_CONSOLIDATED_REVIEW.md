All measurements: consolidated review and repair

The one combined static review completed against the immutable original implementation before repairs. Its six retained review reports contain 76 findings. One consolidated repair pass addresses those findings across C01/L01, M12/L02/L03, B00 decision contracts, V01/V02, and all new M01–M13 measurement families.

The batch contains eight immutable registrations, 377 golden cases and 1,402 source-clause rows with overlap. The prior successful complete suite contains 563 assertions. Raw maps bind exact finite mechanisms and preserve every broader source-closure flag as false.

Repairs bind authentic source and formula receipts, actual calendar and publication clocks, original F10 birth/expiry, explicit finite capacities, and retained F11 query/training evidence. Tests use actual public producers, correction/restart histories, source membership and independent primitive arithmetic/relations. Typed label ledger outcomes replay exact retained input bytes; incomplete full-horizon values remain unavailable.

Original golden files, registrations, preparation documents and initial review bytes are unchanged. Pre-execution clarifications preserve the original CL-T03 fidelity vector, distinguish fee identity versus input occurrence capacity, and fix the 4096-total-quote-input convention. A separate binding clarification preserves the originally reviewed tool artifacts while binding their repaired sources independently.

No candidate import, compilation or test execution occurred during this review/repair pass. The next step is static mapping normalization, then one complete supervised worker with 180 CPU seconds reserved, a 190-second CPU hard limit, a 4-GiB address-space limit and a 240-second wall limit. The same observed physical run will be attributed to every participating family. Failed, skipped, omitted, substituted or over-limit evidence cannot be attached as success.

Finite synthetic engineering does not close native admitted-source behavior, all routed source models, predictive or economic validation, or future receipts. Those requirements retain their existing owners and phase states.
