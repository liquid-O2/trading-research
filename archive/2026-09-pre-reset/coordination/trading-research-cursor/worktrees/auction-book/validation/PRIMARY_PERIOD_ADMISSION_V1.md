# Frozen primary-period admission engineering

The primary model population begins2020-01-01T00:00:00Z inclusive (1577836800000000000 UTC nanoseconds) and ends at actual eligible observations available at each frozen cut. Seven independent literal case groups cover pre-boundary/coincident-boundary/full-range cohort admission, explicit diagnostic versus primary fold/model/calibrator/measurement-fit paths, immutable evaluation registration, in-range missing ES MBP, and independent contract/calendar/universe blockers. Older raw material is optional only for separately declared Jumbo/OHLC context or comparisons; raw storage and older static definitions/calendars are not globally clipped. The historical2022–25 E0 harness is separate from the main2020-onward cohort.

Preserve legacy diagnostic wire/hash contracts. Omission of purpose cannot create primary admission; primary consumers carry the immutable policy hash, exact complete population and actual-cut evidence. Direct Fold/TemporalFold/model/calibration declarations do not bypass full retained-population revalidation. Existing source, maturity, OOF, F11 actual-read, accounting and endpoint gates stay in force. A future protocol may be declared before its future endpoint; actual future observations/results stay unavailable until seen/matured. Registration time can precede2020. Root corrected the prepared future protocol's registered_at toB-1 before freezing because its structural gate requires registration before start; no candidate was executed to select this correction.

Implementation follows this freeze and registration. Collect one complete independent source/code/test integration review, make a consolidated static repair and run all tests together with the E0 and orchestration families. Retain all851 existing successful assertion IDs and all seven new literal groups. No private candidate import/compile/test or unregistered retry. Each new family permits three attempts and600 CPU seconds total; per physical attempt180 CPU seconds,190 hard CPU seconds,4 GiB address space and360 wall seconds. Record actual process-group resource use, all failures, immutable reviewed code/mappings and successful attachment without summing duplicated family telemetry.

These are finite engineering assertions, with no new raw market data, actual market fit, economic run, orders or full parent/phase closure. Missing source/calendar/terms/complete parent-volume evidence remains explicit and does not become a period error.
