# Auction/flow performance checkpoint 23

Accepted at 2026-09-08T15:31:42.832735+00:00. [Execution](/workspace/trading-research/reports/auction-flow-runs/541e27aac8fd7ce9a91be85d5c53a3654e7583559caed957fd0ff5ab337500e8/execution.json); [worker](/workspace/trading-research/reports/auction-flow-runs/541e27aac8fd7ce9a91be85d5c53a3654e7583559caed957fd0ff5ab337500e8/worker.json). This is engineering/source equivalence evidence, not completion of annual family validation, Context or Location.

121 relevant tests, all nine complete retained source-value comparisons against benchmark18 and all three adjacent-window continuation comparisons passed. C++ projection, quote-slice fusion and native quote updates executed on every actual unit. Complete logical quote/measurement values remain equal; original native floating tolerances remain unchanged. Compilation used -O3, no fast math and disabled contraction.

The exact quote replay representation retains original immutable sources plus bounded descriptors. Each completed writer independently replays every original quote column and the entire producer source manifest except timing. That includes raw all-field identity, physical row addresses, source selection and terminal carry. Initial carry remains distinct. Readers pin source implementation/runtime and verify identity again when exhausted. Trade/native Parquet and every source alternative remain retained. Descriptor and required replay CPU are charged; later rereads are not free.

Measured source-only allowance: **152,641.745 CPU seconds**, down from160,866.336 in18; **261,158,243,987 output bytes**, down from419,007,169,666. Both retain the original1.5 margin and separate failure-output reserves. At17 continuously busy workers this CPU allowance is2.494 hours; this is not an observed full-run time or full-project ETA.

A separately frozen, empirically calibrated quote-cost model accounts for both slice overhead and quote rows. It uses the two original quiet units for fixed slice cost and all nine units for residual row cost, with one quote per raw row as the prospective upper population. Its source allowance is134,722.304 CPU seconds (2.201 hours at17busy workers). This is a costing correction, not an additional implementation speedup. It must be tested on the independent17-window holdout before use; V14 records its exact coefficients before that run.

The full correctness benchmark consumed **380.605659 CPU seconds / 398.110 elapsed seconds / 187,721,396 bytes**. It is slightly slower than18 because replay integrity is exercised repeatedly during value and continuation comparisons. Busy-window quote CPU fell roughly15–23%; quiet-window quote CPU rose modestly. Do not describe this benchmark itself as an end-to-end speedup.

The downstream source-reuse path already passed attempt20:70 tests,18 complete anchor/cohort payloads across9source populations unchanged,122.657CPU seconds /130.973elapsed seconds. It authenticates and reuses accepted source/trade artifacts. Profile extrema hoisting and cohort interval indexing remain verified by17/19; the complete anchor/cohort catalogue is not removed.

Attempts21 and22 remain charged:21 stopped on test-harness compatibility failures before source scans (8.134236CPU s,223,680bytes);22 passed120tests but was deliberately stopped after real16-bit flags exposed unnecessary fallback (123.815261CPU s,96,548,410bytes).23 corrects that conversion and requires per-unit compiled execution. No budgets or failures were reset.

The hardware plan now permits17single-thread children plus a4GiB coordinator within72GiB aggregate address space, below the actual17.85core/82,999,996,416-byte allocation. Its tests passed. Actual throughput/utilization is pending the registered17-distinct-file full-UTC-day holdout. Each independent holdout begins with explicitly declared observed-prefix state; it does not infer pre-window coverage or replace production's continuous same-source carry.

Full remaining annual/anchor/causal/cohort statistics, all other validation families, Context fitting and Location studies are uncosted and unfinished. Complete end-to-end under-three-hour feasibility is **not established**. Full production extraction has not started.
