# Jumbo check dispatch error and retained evidence

Attempt 14 passed **435 tests** and the historical 60-date extraction/statistics comparison, using **55.529881 CPU seconds**. Its registered successful receipt remains unchanged. The intended complete model-resource check did **not** execute: the dispatcher recognized annual schemas through v7, and the new v8 schema fell through to the legacy branch. This was an implementation error. The [complete deviation review](jumbo-check14-dispatch-deviation-review.json) retains the exact receipt, worker log, measured scope and consumed resources.

The dispatcher now explicitly routes the stable `jumbo-complete-model-workload-v8` schema to the complete annual/model check and rejects unknown or missing schemas. Two added cases verify the requested route and prevent a silent fallback. These changes are written and await registered verification. The 435 earlier tests and the complete extraction remain applicable to their unchanged code; no model-resource or predictive-quality result is inferred from them.

Fourteen attempts have consumed **1,366.469830 of 12,000 CPU seconds**. Two attempts remain under the approved sixteen-attempt allowance, while corrected verification, fitting and confirmation require three. The [concrete proposal](../validation/JUMBO_CHECK_DISPATCH_AMENDMENT_V1_PROPOSED.json) adds **one attempt, raising the total to seventeen**, with no increase in total CPU, phase CPU, memory or output limits. Every prior attempt and cost remains counted. No extra attempt is authorized until the user approves this resource-only amendment.
