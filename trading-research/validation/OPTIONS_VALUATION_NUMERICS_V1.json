{
  "american": {
    "bounds": "intrinsic lower; call<=S undernonnegative dividends q0; put<=K*max(1,exp(-r*T)); pricing at intrinsic can imply a nonunique zero/positive IV range",
    "dividend_model": "Backward jump V_before(S)=max(intrinsic(S),V_after(max(S-Dcash,0))); explicit limited-liability jump assumption. Unknown expected dividends are a separate model-input scenario, not invented zero evidence.",
    "greeks": "finite differences at two perturbation sizes and grid refinement; unstable exercise-boundary derivatives unavailable with reason, no clipping into desired sign",
    "maximum_iterations": 80,
    "model": "American spot finite-difference obstacle problem with nonnegative discrete cash dividend jumps, q=0, constant annual continuously compounded r, exact fractional times",
    "numerical_policy": "Grid refinement128/256/512 space andtime base; retain grid-price difference, residual/convergence. Bounded domain must include spot,strike and dividends, report domain errors. No silently labelled exact tree using PV-adjusted European approximation.",
    "pde": "V_t + 0.5*sigma^2*S^2*V_SS + r*S*V_S-r*V=0, V>=intrinsic; exercise allowed throughout; dividend times inserted exactly into time grid",
    "price_tolerance": "max(0.005,1e-5*S) for grid consistency, independent root residual separate",
    "sigma_bracket": [
      0.0,
      10.0
    ]
  },
  "authority": "Latest user requests all Deliverable1 definitions/measurements/statistics/timing. These deterministic O02/O05 numerical definitions are necessary to the pending acquired quote valuation, not family scientific completion.",
  "european": {
    "bounds": "D*max(cp*(F-K),0) <= price < D*(F ifcall else K); zeroT only payoff and no finite IV identification",
    "formula": "D*(cp*F*N(cp*d1)-cp*K*N(cp*d2)), d1=(log(F/K)+0.5*sigma^2*T)/(sigma*sqrt(T)), d2=d1-sigma*sqrt(T)",
    "greeks": "partialF keeping other input conventions explicit; calendar derivative changes discount at provided constant r with F fixed. Spot total derivative requires a separate explicit mapping.",
    "maximum_iterations": 100,
    "model": "Black forward, distinct European contract class",
    "price_tolerance": "max(1e-8,1e-8*abs(price)); retain residual and weak-vega conditioning; this is numerical error, not market uncertainty",
    "sigma_bracket": [
      0.0,
      10.0
    ]
  },
  "full_family_complete": false,
  "implementation": "Batch arrays for Black and native C++ American pricing, explicitly root-built and hash-bound binary; no import-time compilation or autonomous subprocess; pinned existing NumPy/SciPy only, no extra installed dependencies.",
  "intervals": "Distinct endpoint price admissibility, solver convergence and IV identification. Bid/mid/ask intervals conditional on sameinputs; missingbid may give only upperIV, atbound lower0, absent finite bracket leaves sideunbounded. Crossed prices and unsupported styles explicit unavailable.",
  "kind": "options_valuation_numerics_contract_v1",
  "risk_scenarios": {
    "calendar_seconds": [
      0,
      60
    ],
    "coordinate_relative_shocks": [
      -0.01,
      0,
      0.01
    ],
    "policy": "Full joint repricing with nonpositivevol/time domain explicitly marked; analytic local approximations and residual separately. No favorable scenario recombination acrosscontracts.",
    "sigma_additive_shocks": [
      -0.01,
      0,
      0.01
    ]
  },
  "scope_ids": [
    "O02",
    "O02.BIDASK_IV",
    "O05",
    "O05.HIGHER_GREEKS"
  ],
  "sources": [
    "/workspace/planning/trading-research/reference/trading-model/components/OPTIONS.md",
    "/workspace/planning/trading-research/reference/trading-model/experiments/O.md"
  ],
  "tests": "Independent exact European prices/parity/finite differences, American no-dividend call vsBlack, independent high-step CRR put, deterministic discrete-dividend early-exercise, intrinsic/zeroT/tinyvega/negative-rate/failedbracket and inputshape faults. Root registered execution only.",
  "units": {
    "greeks": "delta/gamma with respect to labelled coordinate; vega/vanna/volga per1.0volatility fraction; charm derivative in increasing calendar time at fixed coordinate and vol, negative derivative in remaining time; per-contract multiply separately by certified/scenario multiplier",
    "price": "option premium per underlying unit",
    "time": "exact nanosecond difference converted to Actual365Fixed years; never round0DTE to civil days",
    "volatility": "fraction per sqrt(365-day year)"
  },
  "version": 1
}
