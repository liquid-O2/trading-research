# Consolidated review repair: exact cohort validation probe

Repair only the same two files from the prior assignment. All kernels, budgets, contracts, protocol, evidence and other files stay read-only. Do not run imports, tests, shell, scans or fits. The controller has read the complete helper and tests. The controller will wire the helper and change the test's production_contract() reader to the canonical V4 extension after integration; keep controller_inputs unchanged in your checkout.

1. **Bounded scalar reference must retain complete timestamp groups.** `_prefix_rows(..., PREFIX_LIMIT)` currently truncates an arbitrary final group, yet the generated window is marked complete through last t+1. Collect at most PREFIX_PROBE=4097 and trim exactly as the training prefix; if all first 4097 share one timestamp the scalar reference is explicitly empty/unavailable, with its reason. Main whole/minute paths still use every print. Do not silently claim a successful nonempty scalar comparison for an empty truncated prefix. The reference record must contain selected count/bounds, original first/last source addresses, exact prefix digest and whether truncation removed the final group.

2. **Never fabricate raw fields for prefix re-preparation.** `_rows_to_table` fills raw_flags=0 and generates raw_side/raw_action even for real flagged records. Remove this reconstruction. Populate the scalar prefix CohortWindow from the existing immutable prepared batches, slicing the exact complete reference interval, and feed the literal Trade objects from those same original addressed values. The main current cache is still decoded/prepared only once. `_row_tuple` may retain raw_flags/raw_side/raw_action if needed for complete prefix evidence, using existing prepared values/references; do not invent replacements. Current retained series refs already bind all original raw values. Count prefix slices/preparation accurately.

3. **Complete disjoint CPU accounting.** Training prefix series validation/selection is currently charged to histogram fit. Move `_collect_training_prefix` and member selection into training_cache_validation_and_selection; histogram arithmetic only then receives its own stage. Record total helper CPU from entry through completed serialization and a separate nonnegative orchestration_and_report_assembly residual (or explicit timed equivalent) that closes total CPU against the sum of every disjoint stage. This must include definition/support planning, dictionary/report construction, release/cleanup and other previously untimed sections. Preserve the per-stage costs already requested and do not count serialization twice. Add separated actual training/current decoded row/batch counts if needed to make the existing aggregate interpretable.

4. **Strict training time/identity joins.** `_load_training` currently defaults missing known_at/end to zero and can silently accept a missing delay. Require actual integer start/end/known_at, exact measurement bounds equal training_unit bounds, known_at>=end and explicit retained source_manifest event_latency_scenario_ns exactly equal known_at-end. Reject missing/changed fields instead of a fallback. Preserve exact referenced root/date/source identity, the complete coordinate, and exhaustion of all trade-series rows.

5. **Complete current instrument population.** Reject duplicate current instrument IDs. During `_decode_current`, prove every decoded retained trade belongs to exactly one of the declared current raw IDs, and that total retained/partitioned counts reconcile. Do not silently ignore an unknown raw ID. Preserve all local incomplete/quiet instruments.

6. **Partial preparation cleanup.** `_prepare_groups` can allocate batches and raise before returning, leaving `_release(prepared)` with the caller's empty dict. Populate a local tracked mapping incrementally and release every created batch on error. Ordinary final release remains unchanged.

7. **Report honest comparison counts.** `exact_original_source_fields_compared` currently counts channels (return 1 or len(channels)), not fields. Either count the actually checked individual fields/population identities, or rename consistently to channels and add an actual field count. Do not publish a channel count labeled exact field comparisons.

8. **Fixture repairs and targeted additions.** `test_rejects_incomplete_training` creates two Harness instances under the same existing folder; the second fails with FileExistsError rather than the required IntegrityError. Use distinct child directories. Add focused checks for scalar timestamp trimming (full main paths retained), preserved flagged-prefix source evidence, missing/changed training delay/window fields, duplicate/unknown current instrument IDs, and CPU total closure. Keep the valid original fixtures. Exact probability pairs/knots in contract validation must not silently coerce fractions or booleans into integers.

Do not reduce any whole/minute/current-instrument population or omit numeric channel fields. No new historical fit, F11 admission, Context or Location work. Return the two-file repair and concrete remaining issues only.
