Continue SAME owned physical-volatility1 module/tests. Root reviewed all numerical execution; one consolidated correction pass BEFORE any numerical execution. Do not run imports/tests/data. 900 seconds; root concurrently integrates auction windows. Root owns runner (do not edit runner). Current API stays, root will put actual pilot payload in configuration.accepted_pilot, explicit refs + computed_partitions. Complete only these concrete contract violations and add independent regression fixtures. Do not revisit catalogue/docs.
1. Domain bug: true_range_ticks passes prevclose as OHLC close requiring it INSIDE H/L, falsely discarding true gaps. overnight_log_gap and close_log_return and YZ opening validity construct (O,O,prev,prev), falsely reject ALL NEGATIVE RETURNS. Use independent positive finite numeric checks for ratios; H>=L and allpositive for true range, no prev enclosure. YZ previous positive independent of O. Fixtures negative opening/closing gap, prevabove/belowrange true range, negativeopening YZ.
2. history_window_row uses CURRENT session formation/version/contract and known_at even role feature_prior, hiding actual20-120session input interval, source boundaries and leaking timestamps. Derive formation_start=min first intended row start, formation_end=lastrowend, known_at=max actual input knownats (NULL if missing), contract_key from history only, window_version digest of each intended window/version/source/contract. Add source lineage (JSON compact string column listing DISTINCT source/canonical/admission hashes and variants used; include previous_close provenance since first session gap depends on one further preceding close). Current session date is decision date label only. feature_prior must be available strictly no later than current session start (with no current information); expose decision_cut_ns and causal_feature_eligible. measurement_including_current is own completed measurement available late. Full formation/maturity stages: stage_purge if formationstart throughknownat crosses frozen date boundaries; preserve raw stats but keep purge explicit; do not silently relabel past history as current inputs. Missing or roll rows retained; mean_true_range denominator MUST declaredn orNULL, never shrink availabletrue ranges. Add lineage provenance also session previous close/year normal carry (currently only original variant tagged). Crossyear sources need exact list. Tests mutate currentprices/timestamps and show featurepriorunchanged exceptdecision identifiers, and assert true interval/inputhashes.
3. Remaining causal bug: incompleteprefix trailingmissingminute makes actualmaxknown BEFORE cut, target can overlap formation. Use planned known_at=max(prefix_end+MINUTE,actualobservedmaxknown) for source assumed60s; keep observed_input_known_at separately. prefix incomplete stays ineligible and status cannotcomplete. lastreference must be from valid published input and its timestamp included. Remove dependence on FUTURE fullsession contract for eligibility/window_version; use prefixversion/contract and targetcontract. Add join_eligible requiring prefixcomplete+targetcomplete+samecontract+stagewholeinterval+applicable. Keep observed labels if partial as explicitobserved masks; no padding missing aszeros. Future up/down excursions should max(0, high-ref), max(0, ref-low), logs max0 as MFE/MAE baseline includes reference. If intended semantics signed distances, rename to signed highdistance (root wants excursions consistent other families). Fixed5/15/60 horizon must NOT silently clip to cashclose and call complete60min. Keep nominal_target_end and actual_observedend plus horizon_censored when exceeds cashclose, fulltargetcompleteFalse; 'actual_cash_close' separately complete. cut_after_close ineligible. All source/contract/window lineage must be feature-only; target info keptlabel. Test prefixmissinglastminute, fullsessionfuturemutationdoesnotchangefeatureid/version, fixed60nearclosecensor, allfutureabove reference down=0.
4. Scale final30min bar currently counted as complete60min and mixed into equal60min mean. Frozen contract excludes truncated lastbar from complete equal-scale comparison. Keep disposition counts/actualduration plus observed aggregate separately if wanted; primary mean/sum/RV use ONLY FULL length scale bars, keep finalshortcount/shortbar observed diagnostics. Full5/15/1 fine. 60minRTH fullsixbars +30short. RV regular pairs mustnotsilentlyinclude short interval. Statistics must split fullcoverage primary vs partialobserved; current _date_map scale usespartial asfull. Add require complete for primary scale stats and explicitpartialcounts/observedsubset ifneeded. Include GK/RS and semivariancestatistics (current only Parkinson/RV, omits intendedGK/RScomparison). Denominators n_expected/complete/scalefull. Tests6full+30short andmissingmidbar no fullcomparison.
5. Seasonal contract_key/window_version currently entire completed dayfuture; derive from actualcell; prefixversion forremaining. Seasonalcells missingnull okay. Originalvsprimary matched difference requires same rawcontract and fullvalid pair; report changedcontract separately rather than compute mismatchedprice.
6. execute_own_suite raises generic exception discards report! Preserve full failure text before raise in registered worker log (print or exceptionpayload), actual data MUST NOT run after failure. Tests fixture _columns defaultknown=(start0+offset+1)*M wrongwhenstart0 nanoseconds; should start0+(offset+1)*M, primary fixtures source lag60 known=start0+(offset+2)*M exceptexplicit tests. Check all fixtures relevant independently.
7. Full payload API: return computed_partitions exactcomputed selection inclsource refs so reuse supported; source_versions result includeacceptedpilot source_versions (orderive fromprotocol). assertfullintendeddates1676perroot+252original (metadata/counts) and all15partitions seen, pilot252dates. No all-data re-read outside owned registered run; yearlyonce. Resource use existingarrays okay. Testno disksearch.
Keep source originalfrozencontract unchanged. Rootwill check remaining potentialadmissionAPI and constraints before registeredpilot. Return concise modifications and test names, no scientificcompletionclaim.
