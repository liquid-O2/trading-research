# Bounded implementation: actual full-source reported-BBO comparison

Use Cursor Grok 4.6 Extra High Fast. Read AGENTS.md. The controller owns science, review, integration and every numerical execution. Validation across all families remains the current phase.

Write ONLY two new files:
- src/trading_research/research/auction_flow_book_preflight.py
- tests/test_auction_flow_book_preflight.py

All existing kernels/tests/references/configs/validation/tools/permissions are read-only. No shell, imports/tests/compilation, raw scan, benchmark, network, new dependency or worker. Write tests but do not run them. The controller will integrate/wire the entry into the registered source check afterward. Read validation/AUCTION_FLOW_BOOK_REINITIALIZATION_PROBE_CONTRACT_V1.json and the COMPLETE auction_flow_book_recovery.py plus its tests, auction_flow_quotes.py and auction_flow_storage.py. Preserve the accepted strict source projection and every old comparison. This is a separate unpublished candidate, not an accepted global replacement or a full family study.

## Entry and complete source domain

Implement `check_book_unit(measured, unit, *, measurement_reference, quote_storage, outputs, contract)` returning `passed`, output artifact refs, current counts, disjoint CPU/bytes/work and limitations. Validate the frozen contract exactly and join measured root/start/end/known-at, all listed raw instruments and source manifest counts. Every acquired source variant remains independent. Use `read_series_tables(quote_storage)` and EXHAUST the iterator for its final row/byte validation. Process <=65,536-row batches, retaining only bounded prepared arrays, first4096 scalar-reference rows and sparse episode/state metadata. Never retain every full quote row as Python objects. Validate one source key and the actual raw instrument/time/delay/order/physical row for each stream. Preserve uint64 addresses. Reject malformed/mixed/unlisted domains, do not silently skip them. Empty source/instrument windows stay explicit. Count full decoded rows/read batches and actual instrument slices separately.

## Three complete mask computations

For each instrument instantiate the unchanged ReportedBBOReinitializer over the full unit and run it on every original quote/invalidation row. Compare every returned column/type/null/value other than derived book_valid exactly with the original; original input buffers/records remain unmodified. Compare all candidate book_valid values to an INDEPENDENT vector recurrence over the same original fields. Do not call the candidate's private functions in the expected implementation.

Independent recurrence: invalidation = F4 OR action R OR action outside A/M/C/R/T/N. Qualifying restart = A/M/C, F4 clear, SNAPSHOT clear, LAST set, positive exact noncrossed BBO and positive sizes below uint32 sentinel. Track last invalidation and last qualifying restart position with vector cumulative maxima and incoming state. Before the first invalidation, output original book_valid. Thereafter, a row can be trusted only when a qualifying restart occurred strictly after the latest invalidation, and the current row is a valid A/M/C full-BBO update (including the original snapshot handling after trust exists). T/N/R never become trusted updates. Same-time order follows original source_order, not a made-up timestamp grouping. Invalidation wins at its row. Missing/incomplete history stays distinct from current trust. Validate these expected masks against an independent literal scalar loop on the complete first4096 retained rows per instrument (physical rows, ties do not need truncation for this ordered recurrence), including complete episode/address/cumulative-history comparison. Keep full-source vector-mask equality separate from bounded scalar evidence.

Also run a full adjacent-cut candidate per instrument in the SAME source traversal. For a unit spanning UTC midnight, use that interior midnight; otherwise use the exact interval midpoint. Split rows half-open at the cut. Finish the left candidate and restore its actual `carry()` into the right; preserve original source order and first invalidation. Empty left/right intervals must work. Compare every row's candidate book_valid and every complete logical terminal episode/address/count/history field with the full unpartitioned candidate. Carry hash itself differs with the interval; do not falsely demand byte-equal interval-specific reports. Counter scope is cumulative from prefix_start_ns; current_interval_rows/empty_observed_window describe only the current interval. Do not reset trust or economic age at a reader/cut boundary. Validate exact positive adjacency and source continuity.

Persist only a compact derived-validity series containing source_key/instrument_id/source_row/source_order/t/known_at_ns/book_valid, linked to the original complete quote series. Do not duplicate every unchanged BBO/source column in another full cache. Use ParquetSeries/BoundedOutputs with their byte accounting. Retain every mask row, not just changed examples. Summarize changed/original/candidate valid rows and complete source/episode lineage. Attribute provider flagged RECORDS separately from invalidation episodes; never call them missing downloads or a count of lost trades.

## Actual unchanged quote measurements

Feed each full candidate-projected batch into the existing QuoteWindow without modifying its code. Prepare the candidate batch ONCE and reuse its views for a whole-unit quote accumulator and every one-minute atom, with actual adjacent QuoteWindow continuation through empty bins. Keep original event/known/economic times. Current source quality/coordinate coverage is taken from the original measured unit/atoms at finalization, never promoted because local BBO trust recovers. Initialize the source unit with the same original initial measurement state if present; current nine full units normally have none. Clearly reject an unsupported initial-state domain rather than silently resetting it.

Produce complete whole and minute candidate quote measurements, linked to the unchanged original minute records and original full measurement reference. Preserve original core records in their reports; the candidate wrapper may COPY the static `book_recovery` explanation and attach the candidate policy/version so it does not falsely repeat the original 'no inferred recovery' label. Do not change numeric consumer behavior or original report bytes. For each minute retain original versus candidate rows, fresh updates, pressure transitions, OFI, invalid/snapshot/gap/clear counts, standing duration, quote means and terminal projection, plus the full candidate record. Compare every source-population/flag/action-side field that must remain equal, while recording true trust/pressure/exposure differences.

Check whole candidate integer sums against the complete carried minute composition: event/flag/action populations, OFI and components, fresh/pressure counts, standing exposure and midpoint occupancy mass. Compare full OFI open/high/low/close and actual extreme clocks by composing each minute's ordered path with the running opening value, including quiet starts and ties, not maxima of independent closes. Floating sufficient sums/means require the declared absolute1e-6/relative1e-12 tolerance and meaningful denominator-weighted composition; do not average averages. If a quantity is not composable from published sufficient statistics, say so and compare it by the existing independently specified bounded scalar quote loop instead of inventing a sum. Do not use the same candidate function as an independent expected value.

Independently verify that every first recovered quote is backed by a qualifying original completed ordinary row and the preceding state is invalid for pressure. Retain its exact source address and adjacent original/current BBO. A first recovery may initialize standing exposure but contributes ZERO OFI from the missing interval; later eligible ordinary transitions contribute the literal four-indicator OFI. Use sparse episode transitions and bounded scalar reference neighborhoods, keeping full vector checks for all rows. Do not create a Python record per row to test this. Same-time and snapshot boundaries must retain age/freshness rules. No claim of gross replenishment, venue sequence, participant intent or restored trade history follows.

## Resource accounting and focused verification

Close helper CPU accounting across entry validation, complete retained-cache decoding, filtering/preparation, candidate masks, independent full masks, adjacent carry, scalar reference, whole/atomic quote work, finalization/comparisons, serialization and release, with a named nonnegative orchestration residual. Stage timers must be disjoint and sum to measured helper CPU including cleanup/report overhead. Count actual compared cells, emitted mask rows/bytes, quote rows per consumer and Python transition/reference visits; do not report theoretical work as measured. All outputs go through BoundedOutputs; retain output delta, peak RSS, wall/CPU and exact refs. No annual projection or full native/Context/Location claim. Abort partial Parquet writers and release preparations on every exception.

Write focused tests using real bounded ParquetSeries/BoundedOutputs and original QuoteWindow/reference arithmetic, with a minimal measured-unit fixture whose original quote records are actually computed. Exercise full masks against independently hand-calculated sequences, equal timestamps, repeated F4, non-LAST and snapshot nonrecoveries, recovery followed by a genuine OFI update, clear-only versus gap/unknown history, quiet bins, empty/cut intervals and carry parity. Verify complete schema/source identity, no mutation, original/candidate comparison fields, contract and clock/source/count/schema rejection, cleanup and precise CPU/output counters. A test that only asserts `passed` or compares two candidate calls is insufficient. Keep the full-source vector and bounded independent scalar validations distinguishable. No numerical execution by the worker.
