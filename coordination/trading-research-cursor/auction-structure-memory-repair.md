# Consolidated controller review: structure/memory preflight

Continue in the same isolated checkout and same two-file scope. The initial assignment and frozen contract remain binding. No shell/import/compile/tests/scans/numerical execution, other files, dependencies or permissions changes. The controller read both complete files. Repair the following together; do not change the candidate kernels or original reference mathematics.

1. `_scalar_structure` has an unexpected indentation before `emitted = _add_stream` immediately after constructing the stream. Correct the source. Recheck the complete written text for truncation/indentation without executing it.

2. The CPU stages currently overlap. `_write` adds serialization time while its call is also inside the interval later charged as `full_memory_accumulation_and_finalization`. The residual therefore can be negative. Use truly disjoint measured intervals, excluding every write interval from algorithm time. Charge the actual whole `_write` operation consistently, retain the storage library's narrower timer separately if useful, and close the total with the real nonnegative orchestration residual. Do not clamp negative residuals or remove the closure assertion. Keep per-instrument/report-assembly and release costs accounted. Add a test that forces a substantial serialization interval and proves it is counted once; timing/resource instrumentation can be mocked, candidate arithmetic cannot.

3. Retention and limits: `_decode_current` retains all Arrow trade tables, then `_prepare_groups` duplicates all of them, retaining both for the entire helper. Prepare once during streamed decoding, release each Arrow batch promptly, and keep only the bounded prepared collection. Separate decode and preparation CPU intervals. Enforce row/prepared-byte limits before retaining another batch; never keep appending after setting an overflow flag. If a limit has already failed, consume/discard remaining iterator items only when bounded and needed for its accounting, then fail without publication. Cleanup must cover a failure during decoding/preparation as well as later algorithms. Add explicit resource/cleanup tests, including a failure after the first prepared batch and an early retained-byte bound. Do not add a full-tape Python row copy.

4. Source identity and validity: trade, quote and excluded source keys are validated separately but never joined; `_excluded_keys` is ignored and `_source_key_for` can silently prefer a different trade key. Require all nonempty stream keys for the current single-source unit to agree with each other and with `source_manifest.sources`/the explicit current source path when provided. An excluded-only instrument must retain its true source key. Validate all source rows/orders as exact nonnegative integers, t monotonic within batches as well as between batches, raw flags as the actual finite nonnegative flag domain, and no null numeric clocks before using them for first-invalid masks. Validate excluded action/reason bits against actual flags/size (read source data exclusions); do not trust arbitrary exclusion bits. Match the complete F4/unknown/clear and exclusion counts against available actual manifest/atom quality evidence. Manifest fixture helpers should supply realistic source/action/flag/quality metadata for these checks. Add cross-projection key mismatch, bad earliest-row domain/order, unknown-action, and malformed exclusion-reason cases.

5. Whole continuous structural coverage is incorrectly admitted when one atom lacks source ownership but some other atom has it. `_structural_unavailability` currently checks a special all-empty/all-false case. Follow the contract: any structural archive/ownership/coordinate unavailability over this whole raw-instrument path makes that continuous path unavailable, with every print still consumed/countable. Use each actual atom's `source_instrument_presence`, `supplied_raw_coordinate_stable` and coordinate evidence when present. Do not confuse an F4-only bad atom with a structural ownership failure; the former still preserves confirmations before the precise bad source address. Source interval coverage must support adjacent covered spans, not assume one span contains the whole interval. Keep local final-bar eligibility separate. Add a nonempty multi-instrument ownership-switch fixture and archive-span/gap fixture. For scalar memory inherited completeness, apply all relevant structural/first-invalid history consistently to both literal and original views.

6. `_causal_bar` fabricates `width - 1` observed duration for an incomplete atom, which implies a measured one-nanosecond gap. Use a clearly declared conservative admitted-duration policy: full width only for completely eligible priced atoms, otherwise zero admitted duration; actual missing duration is unknown. Retain this basis alongside the bar/comparison evidence. Validate the entire atom cadence covers exactly unit start through end in order and the atom instrument/known-at clocks match; do not permit omitted or shifted quiet atoms. Keep all existing original pivot comparisons and retain every bad/quiet atom.

7. Fix the definite expected-value errors in the F4-between-trades and invalid-size tests. The prefix 400 -> 416 at threshold16 emits LOW400, extreme order0, confirmation order1; it does not emit HIGH416. The later gap prevents a new confirmation. Preserve the clear-only 400 -> 392 -> 416 expectation LOW392 confirmed at order3. `_compare_original_memory` must not reject an actually retained null raw side merely because it is null: null/unknown side is supported observed content, and must be compared without fabricating an aggressor. Add a null-side and an unpriced later-print case so original/literal volume-clock comparisons include all later quantity and exact retained raw fields.

Keep every full-grid, full-source, all-definition calculation and bounded scalar reference required by the initial assignment. Report only the two changed files and an unexecuted-check caveat when done.
