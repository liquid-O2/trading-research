# Combined measurements: final registered attempt

The [second shared attempt](../reports/all-measurements-location-label-decision-v1-verification-0ebbc49eb449.json) executed all 851 tests with no skips: 845 passed, six errored, and none failed an assertion. The six errors have two common causes. Its failed receipt, source snapshot and family charges remain intact: 91.770063 CPU seconds, 200.745897 wall seconds and 237,465,600 bytes peak RSS.

Both fitted-measurement fixtures now bind each actual read to its exact direct dependency slot and singleton source column. The F11 validation remains unchanged. The five affected fit, query, reconstruction and restore paths received a bounded static follow-up.

The location adapter now distinguishes the start of a source formation from its actual confirmation. Immutable formation-anchor evidence is separate from independently retained accumulation receipts. A corrected accumulation is explicit current F10 object evidence, so the prior object becomes unavailable until corrected measurement publication. The original birth, anchor, expiry and frozen targets remain unchanged. The actual integration case exercises this state transition and the combined evidence-capacity boundary. Default anchor captures retain their prior schema and hashes.

The [third-attempt repair report](../reports/all-measurements-consolidated-repairs-retry-03.json) preserves all earlier findings and failed evidence, maps these six errors to their repairs, and includes the final static integration check. This is the second post-execution repair; it does not replace or relabel the original combined review or initial consolidated repair. No candidate imports, compilation, tests or private retries occurred during the static repairs.

This is the final attempt within the original three-attempt family registrations. All eight families retain the same 600-CPU-second budget and per-run resource limits. New coverage maps bind the current code; all earlier maps and receipts remain immutable. The complete suite, including all 563 previously passing baseline assertions, must pass before this batch can be attached as successful engineering evidence.

```bash
PYTHONPATH=src /tmp/trading-research-venv/bin/python tools/verify_engineering_batch.py reports/all-measurements-batch-retry-03.json
```

Native-source completeness, the remaining B00–B10 implementation, market quality and economic outcomes remain separate work. Subsequent primary-model cohorts follow the user's [2020-onward policy](../RESEARCH_PERIOD.md).
