Correct the same three owned files only. Root read all three; do not compile/import/test/run commands (registered execution remains root). Make one consolidated correction pass. This is necessary for O02/O05 style-correct IV/Greeks, before scientific quote valuation. Do not change the frozen contract, dependencies, root canonical files, or weaken tests. Return concise exact changes and limitations.

Observed static defects:
1. Native obstacle policy marks V<H exercised, then pins V=H forever (release condition V>H can never arise after pinned row solve). step_residual completely ignores A V-rhs on exercised nodes, so wrong exercise sets can report convergence. Implement a correct complementarity solver (simplest consistent PSOR with residual max of feasibility errors and complementarity min) with both V-H>=0 and A V-rhs>=0 checked, and min(...)≈0. Retain max residual over every step; damping half-steps need their own actual remaining time/boundary PV, not final full-step tau for both. Fail status if any step fails. Avoid unnecessary vector allocations per time step.
2. Sigma0 currently repeated grid interpolation, yet tests require exact deterministic exercise extrema. Implement deterministic forward path with discounted payoff extrema at time0, every dividend just before/after, maturity; between jumps discounted call/put payoff monotone for constant r, so endpoints suffice. Include limited-liability spot jump. This removes interpolation bias for exact sigma0 and provides exact IV lower price. Reject invalid dividend times outside (0,T], nonfinite amounts, unsorted/duplicate handling explicitly deterministic. Future beyond maturity cannot affect pv_divs/boundaries. Do not silently treat unknown dividend row None as known zero.
3. American IV _batch_core fixes domain using sigma10, so entire inversion at128 nodes on enormous log-domain cannot price sigma.2 accurately. iv_one never uses solve_refined and ignores nonconverged endpoint/mid solves. Need every accepted IV result evaluated with numerically adequate refined price and reported grid error, root residual separate; adaptive bracket within fixed [0,10] may avoid evaluating sigma10 until needed, but no untested domain shrinking. Require valid status for endpoints/iterates, unavailable if refinement inconsistent, never accept only a tight volatility bracket with excessive price residual. Do not silently finite-return IV on failed bracket. Intrinsic flat American price can imply range, not a point0; return weak/unidentified interval or status. Preserve all numerical diagnostics and show independently repriced IV result. Ensure price/IV support scalar and broadcast shape including price-only vector and rights-only vector; currently np.ascontiguousarray promotes scalars to shape1 and IV cannot broadcast price with scalars. Bounds/error arrays must initialize NaN/0 on ABI failure, not np.empty garbage. Native row CSR lengths must be bounded before int64->int cast.
4. Native refinement if one grid is nonconverged cannot declare a finer grid valid without its own full residual criterion; record actual max grid difference and all used iteration counts. Tests currently allow arbitrary large self-reported error and ordinary put inversion may incorrectly pass endpoint statuses. Strengthen ordinary ATM call/put fixtures to finite correctly converged or within frozen .005 grid requirement, error <=.005; independent CRR error tolerance based independent reference convergence, not unlimited reported production error. Require recovered .2 within justified grid-derived IV interval and independent repricing. No-dividend call analytic price test should use frozen .005 tolerance where grid passes. Add exercise complementarity/negative-rate/intrinsic set-identity tests. Root will capture failure if insufficient.
5. Quote interval _side discards all primary inversion statuses and copies failed finite IV; point_defined then may report false valid. Retain status_bid/mid/ask explicitly; only certified sides finite, proper interval lower0 if bid below mathematical lower bound, no upper if ask above available finite bracket. An infeasible ask below model lower makes conditional set empty. Missingbid leaves upper only (frozen fixture expectation); zero bid is a valid no-tightening lower0, not an equality inversion. Reject negative quotes and mid outside finite bid/ask; crossed and invalid inputs explicit. Do not overwrite failed primary statuses with generic undefined. Full-market interval distinct numerical tolerance, point midpoint requires all appropriate inversion statuses, ordered contained endpoints, nonweak mid; finite bracket endpoints remain endpoint/nonunique rather than converged. BlackIV leftover currently allows10x frozen tolerance and over-high finite10 failed value; honor frozen tol and NaN on failure. Numeric rights1.5 must not cast to1 silently. Empty arrays shouldn't break nditer. ZeroT inconsistent market price should retain diagnostic no IV.
6. American Greeks: linear interpolation at bumps .01/.02 can give gamma0 on same cell across all bumps falsely stable; need derivative evaluation with spatial stencil appropriate to grid or genuinely convergent interpolation/spot stencils. Two requested grids128/256 both native_refined ultimately512 => not independent grid comparison! Expose single grid internal result or use distinct finest grids128/256/512 as stated. Retain all 2step and grid differences per derivative; existing u_d0/u_g0/e_v grid differences discarded. Do not take max of dimensional delta/gamma/vega/charm errors into one unlabeled error; provide per-Greek error/status with units. Propagate EVERY underlying price failure to derivatives. sigma-h<0 may not be clipped then divided by2h (wrong derivative); derivative domain unavailable/one-sided correctly stated. Near maturity steps must respect actual subday T; negative T unavailable rather than coarse1day masking all0DTE. Avoid calendar steps crossing cash dividend events silently: choose within nearest event or mark unstable/nonexistent. Unstable derivatives must remain unavailable as frozen contract says; generated diagnostics may remain separately, never be silently treated valid. Add no-dividend American Greeks vs analytic spot derivatives including nonzero gamma/vega, not just finite(error). Price sigma0 exercise boundaries may have undefined Greeks.
7. response_cube uses core.charm*dcalendar in PRICE Taylor; charm is derivative of delta, not theta. Add price calendar theta (analytic for Black with F fixed and evolving D) and correct expansion theta*dt + charm*dF*dt (other higher time terms only if calculated). Add zero-coordinate/zero-vol60s finite difference check. Implement conditional interval and response cube for American too (distinct named helpers acceptable), with exact shift of dividends, grid/status diagnostics, no accidental European application. Required full product classes include ETF American. Do not claim this task completed with European-only interval/repricing.

Keep code focused; no new framework or source scan. Existing tests should remain genuine independent numerical assertions. Root writes registered runner while you correct. Target completion within 15 minutes.
