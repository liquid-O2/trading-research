Finish the concrete remaining defects in the same three owned files only, Grok4.6ExtraHighFast, 900 seconds. No commands/imports/tests/scans. Root read all relevant changes and will run registered verification. Do not repeat already-corrected code or claim fixes absent from the actual files. Necessary defects, not scope additions:

1. test helper _window_rows lines361ff STILL creates only four features without latency_ns, complete, chronological_eligible, data_complete, contract_stable. You added actual Arrow schema but left those fields NULL. Fix helper to generate actual 4x3=12 features, 12 labels, exact arm clocks and IDs. test_join_rows... currently asserts len(features)==4: change to12 and verify all48 actual pairs, eligibility and specific source/scenario clock values. _feature_for_arm must require exact non-NULL latency; its fallback currently accepts missing latency. _scenario_publication maxes with unshifted input_known so zero-latency never differs when input_known=cut+250ms. Required input clock must be shifted only for inputs from the declared source latency; independent selection remains fixed. Retain actual published source250ms and explicit scenario0/250/1000ms. Test literal actual/source vs scenario and at least one eligible zero arm using real features.

2. Stats _release_geometry_group calls _pair_variants when final variant arrives AND on each proxy arrival, duplicating paired observations four times. Call once after complete variant+proxy group, or track bounded pair completion. Developing and rolling have baseline only; they are currently inserted into geometry_index/exact_index forever because len<allvariants. Do not index those non-comparison rows. Incomplete proxies also prevent release: release by explicit end-of-anchor callback or observed emission count, not only eligible rows. A fixture must assert exact ONE observation per contrast per anchor and bounded empty indexes after hundreds of developing/rolling cuts plus completed anchors. write_partition_groups final loop must not repeat already emitted pairs.

3. Integer exactness incomplete: only six numerators have text escape; concentration_den, side_* num/den, variance num/den, sum_price_squared_volume and other accumulated moments remain i64 and can exceed it. Stats _ratio ignores every *_text escape. Use one central geometry serialization normalization over declared exact integer fields, encode both num+den and accumulated moments consistently; decode in metrics. A REAL Parquet write/read fixture with two unequal large masses (e.g. 2**32 and2**32+1 at price100000 and100002) must prove exact concentration rational (num ANDden>i64), variance and sum_p2q preserved and statistics finite correct. The current test just encodes2**64 by itself and then a one-row concentration1, so it doesn't exercise this. Assert sideWasserstein exactly2ticks for100buy/102sell.

4. Fresh/reuse finalizer still builds group_parts/paired_parts across all partitions, outputs.json huge full dictionaries and report full string. Your report claimed boundrefs but actual finalizer still concatenates allvalues. Fix actual code: retain perpartition group/paired references in compact top-level manifest and provide exported authenticated iterator/reconstructor; stream full READABLE metric values (units, means, support, uncertainty/undefined) partition bypartition to report. Replacing metric table with index-only lost readable findings. Root runner can accept refs['groups'] manifest kind if reported clearly; tests must use public iterator. Reused authenticate should load onlyonepartition groups at a time; do not retain allpayloads. Preserve same semantic counts. Perpartition groups can stay currentkind initially if within128MiB, but full reducer cannot multiply memory bypartitions.

5. Civil output identity uses new day/economic/stage/sample when _cross_civil_periods emits previousperiod. Assign output economicdate/stage to anchor end (civil end minus1ns), not nextperiod firstobserved day. completed_id staysNULL for variants precedingbaseline becauseassigned duringloop; compute baselineID once before allvariants. Warm support flush_day returns before emitting/publishing fixed priorreferences: calculate compact prior cash/futures completed geometry internally without emittingoutcohort, so firstselectedday has priorcompleted VA. Test Dec31fullcash->Janfirstday earlierreference literal. Civil week spanningDecJan currently ownedbyoriginyear and warmstate can neveremit inendyear; choose enddate ownership consistently for crossed week and avoidduplicate. Preserve carry without outcohort numerical emissions. Test Dec/Jan spanningweek mass. This is the existing warmhistory requirement.

6. Partial observed groups: accumulate_geometry_row discards every geometryvalue exceptvolume if notcomplete; partial valuesneed separate partial observed distribution without being eligiblefull. Join futurepartial likewise explicitly retained but noteligible. Don't silently substitute zero unpriced overflow: unpriced has unknown sideofgrid, so future_low/high_overflow_unpriced must beNULL with unavailable reason; side channelsremainknown. Future_val/vah are physical ticks fromnewcode yet metricunits saygrid_rows: correct dimension. transport_wasserstein is currently just duplicated sideWasserstein; retain accurately named buy/sell distance and don't invent time-transport measurement. Implement prescribed future-vs-current normalizedprice transport on commonfrozengrid if frozencontract requiresit, with overflowunidentified distinctly.

Only report fixes actually present. Preserve existing APIs except explicitly documented compactgroupmanifest iterator. All original definitions and sourcecontracts remain. Root has not imported or tested this draft; fix these statically evident failures in this one pass, then root will verify.
