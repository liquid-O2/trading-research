One consolidated correction after full root review of both owned files. Continue SAME two ownedfiles only. No imports/tests/data execution. 900sec finish. Original assignment /workspace/coordination/trading-research-cursor/daily-volatility1.md and frozen protocol still authoritative. Actual admission successful bf2387e52d112c076b777a3e8a1c7382e0228c079ee87948725b47e2e48d955c execution under canonical reports/daily-volatility-runs; read retained JSON metadata only if needed. Root needs actual daily-branch complete validated result, not pooled invalid distributions.
Corrections necessary BEFORE registered execution:
1. _vx_changes silently last-writes duplicate identity/date/basis and allows nonpositive levels. Retain explicit conflict/domain disposition; no same-contract change if either intended date conflicted/nonpositive/nonfinite. Identical aliases dedup and carrycount. Conflicting maturity prices also cannot supply a front via survivor promotion without explicit partial_curve coverage; retain candidate maturity dispositions and alias identities as output JSON/table, not only aggregatecounts. Exact contract changes don't require rankedmaturity but DO require exactidentity unique valid level. Include tests conflictcurrent/previous and nonpositive exclusion. Front source key must include path+hash so no hiddencrosssource join. Unknown duration originals must stay separate or explicitly excluded with reason, not all mixed as one unexpectedcurve.
2. _statistics uses full1676 date universe for year/stage subset metrics: wrong missing denominators. Batch metrics per matching year/stage intended universe, then combine compact results. Read date_statistics API schema: preserve actual estimate/count/missing/support/CI fields correctly; _compact_statistics currently appears to ask wrong config/support keys, inspect actualreturn. Use _retain_replicate_estimates=False to avoid retained1000arrays. Write numeric date-mean+95%CI and actualsupport into results.md, split training/development/confirmation; currently report only pooledquantiles. Existing date_statistics supports batch same intended universe. No false independent support fromdatesoutsideyear/stage.
3. _distributions pools monthly+weekly, settlement+close, alltenors, individual+combined FREDaliases. Replace with same dimensions as statisticgroups (source/duration/basis/year/stage/tenor). Do not poolaliases. Use equal-date mean values for the primary distribution if multiplecontractchanges perdate; optionally expose separate eventweighted declared distribution. Front-roll gap repeats on everycontractrow: dedup bydatecurve identity. Expose per-group intended/observed/missing denominators; rawinvalid/expired/conflicts retained. Mainreport can show all-period and chronological aggregatevalues withfulltablesJSON forrest, but meaningful readable findings needed.
4. Actual index source metadata: individual VIX/VXN/VIX3M are FRED (NOT independentCBOE), same2026-09-03 realtime_start/end as combinedFRED; VVIX is Cboe. Retain these truths explicitly in report; compare individualvscombined exactvalues within symbol without treating themindependent. All known_atNULL causalfalse correct. index domain require finite positive levels; duplicateinvalid+valid notalias unless explicit disposition. Index observations/relations currently lose sourcepath+sha; include all contributing source refs (JSONlineage) especially relationratios; retain allvintagerefs insteadfirstonly. CombinedFRED is comparison copy, not independenthistory.
5. _write_parquet checks onlyrowcount; verify table.equals on readback including nullableint64 ns fields. No stringify fallback silently changes numericdomains; fail invalid schema or explicit JSONencode structuredidentity. Define empty outputs with intendedschema if actually empty relevant.
6. test_unequal_tenors uses assertEqual for 1-2/3 vs1/3, willfailfloatroundoff. Use assertAlmostEqual for weights/price. Add compact literal fixtures for year/stage intendeddate denominators and separate distributions, sourcealias no duplicatedate support and duplicatelevelconflicts. No large testframework.
Do not expand other families. Report exact corrections and remaining actual checks. Root will integrate after physicalvolatilityfull snapshot execution ends, so edit only isolatedfiles. Full C16 still false until othermechanisms; daily branch only afterrunaccepted.
