# One consolidated controller repair: causal structure draft

Resume the same isolated assignment/session. Scope remains ONLY auction_flow_structure.py and tests/test_auction_flow_structure.py. No execution/imports/tests/raw scans or other edits. Controller read the whole kernel and test file. Keep original measurements/structure.py and all other references unchanged.

1. Correct a definite independent fixture error. In test_objects_reference_path_matches_entire_swing_geometry_and_clocks, prices (100,106,110,109,106), threshold4 emit TWO swings in the original recurrence: low100 at event1 confirmed event2+delay, then high110 at event3 confirmed event5+delay. The current expected length1 will fail even against the untouched original. Assert both complete geometry/clock/address comparisons; never suppress the initial low in the reference or candidate.

2. Definition IDs must bind numerical science, not just the human version label. FixedThresholdDefinition.id currently equals version even when threshold/frozen_at differs; FinalBarPivotDefinition has the same issue for left/right/ties/frozen_at. Use deterministic content IDs of the full frozen definition, keep the human version separately, and keep the original pivot reference_definition_id/output reference_swing unchanged. Ensure same parameters with normalized equivalent exact int/Fraction threshold yield a consistent declared identity. Distinct-definition validation must use the actual definition identity (or explicitly reject conflicting human version reuse without treating IDs as identical). Update expected tests and add parameter-change/frozen-time identity cases without asserting a candidate property against itself as the only check.

3. Enforce the emitted cap while processing, before materializing the rest of a large batch. Currently an output cap1 can still build up to 65,536*32 StreamedSwing objects before raising. Stop at the first prospective over-cap emission and poison; preserve the already emitted earlier-call immutable results. Retain actual attempt/processing work counters before errors, separately from atomically installed scientific state if needed. A small read-only diagnostics/work-count method may remain usable after failure, but record/snapshot/add must still reject failed scientific state. Test a long oscillating input with a tiny cap and multiple scales to prove processing stops at the bound rather than computing the whole batch.

4. Remove avoidable full-batch Python materialization before the actual recurrence. Current code first calls Arrow source_key(index) for every row, then builds an admitted dict for every print with repeated local numpy imports/type checks, then makes another per-print SourcePoint. Validate integer array domains and source_key membership once with vectorized Arrow/numpy checks (bounded slice only), including flags>=0 and flags<=255 and side in {-1,0,1}. Then iterate primitive array values once for the literal recurrence, sharing one live SourcePoint across definitions. Do not retain a list of per-row dictionaries or per-row source-key copies. Do not change threshold mathematics or source order. After vector validation, Python int conversion preserves uint64 addresses; do not force int64. Validate the original PreparedTradeBatch-owned source column on the chosen slice rather than accepting a caller string as evidence.

5. Strengthen negative-domain tests: signed raw_flags=-256 (snapshot bit clear), forged prepared side outside -1/0/+1, nonpositive valid price, duplicate/backward row/order and future/early time/delay. Preserve empty slices, 65,536 boundary, same-time cuts, original buffer independence and poison behavior. Add all record fields to reference comparisons where the original carries an equivalent clock/address; keep extra origin fields under independent literal checks.

6. Final-bar pivot arithmetic and its full original reference equality look consistent. Preserve them. Keep bound cadence, incomplete/quiet/gapped support, no early confirmation, immutable output and revision rejection. Add missing field/domain tests only where needed; don't build an unrelated revision framework.

Write these coherent corrections, then stop with a concise changed-file summary and unexecuted-check caveat. Controller will review, integrate and run the registered full-source check. No candidate execution by the worker.
