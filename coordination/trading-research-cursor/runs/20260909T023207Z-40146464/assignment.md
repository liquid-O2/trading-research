Apply focused corrections to the same three owned quote files. Grok 4.6 Extra High Fast, 1800 seconds, no commands/imports/tests/raw scans or changes outside these files. Root reviewed the entire rewritten measurement/statistics modules and changed tests. Preserve correct existing code; do not rewrite the whole modules again. No claimed fix is accepted unless present in actual code. Original frozen contract and prior correction remain authoritative. Root owns registered execution and runs the pilot after this pass.

Concrete remaining defects:

1. Immediate failures: `_table` line619 iterates `for field in schema` inside each column rather than `for row in rows`, so `row` is undefined. `_add_actions` uses np without defining it. `prepare_day` unconditionally projects `last_eq_first`, which is absent in the logical report schema used by fixtures; listing-only report reading does not need first/last OI at all. Static-check every renamed helper/call/schema reference. `parse_quote_typed` builds row_index from zero on each Parquet batch; pass a per-file offset and increment across batches so exceptions, board and run-start source addresses are exact. Call canonical `ClockMaps.begin_request_date(label)` once each day; full OI9 failed because day-spanning seconds cache grew without bound, root has fixed it in canonical OI module (read that tiny method only).

2. Local contract IDs are 30-bit truncated hashes with no collision detection. Across millions of contracts this corrupts identity/time grouping. Use int64 full larger digest with a checked reverse identity map, or explicit partition namespace + local sequential IDs and compare exact identity in reducer. Keep unmatched and OI IDs disjoint. Do not claim same ID cross-shard equality unless guaranteed; preserve exact OSI tuple. Invalid/unresolved sort keys must include chain and original invalid evidence (original symbol/OSI mismatch should not alias distinct malformed identities). OI output int32 remains read-only; quote ID schema may be int64. Fixture force a digest collision or test explicit namespace disambiguation.

3. Listing denominator is STILL derived from `reports.listed`; reports only contain contracts with OI, so listed-without-OI disappear. Canonical OI lifecycle also joins only OI report IDs; it cannot reconstruct all listings. Use authenticated OI `source_manifest` + `membership` (role contracts, chain/date, nvalid) to reconstruct the projected original listing identities for selected day only, through accepted identity parser/lookup. This is reuse of the retained reconstruction contract, not remeasurement of OI. Read only listing identity columns for the relevant day and bind/count these support reads. Do not scan original open_interest rows. Preserve original source/hash, invalid exclusions, marker/missing distinction; reconcile actual valid unique listed count to accepted OI coverage.listing_count. Tests need a listing-only second contract with no report, and one OI-only contract with no listing, and missing/empty listing.

4. OI memory/CPU: `_chain_intervals` still loads EVERY interval for all years of a chain plus giant Python object-string req/exp lists; `prepare_day` copies all history up to each date, regardless of valid_to. `_read_filtered` loops all report parts each date and constructs new dataset objects. Keep a prebuilt projected dataset/index per artifact family; filter selected chain AND validity intersection with the selected shard's date horizon (retain intervals originating earlier if still active), parse unique repeated dates once. For each day retain only intervals whose [valid_from, valid_to) intersects its cuts and eligibility request date. Do not duplicate entire chain history per day. Prefer daily partition statistics/range metadata cached once so irrelevant files are not opened each day. Quote runner uses 49 chain×year shards. Both pilot and full must fit 8GiBAS, and full selected-day OI support cost must be separately measured. `_validate_full` must require explicit authenticated full mode, not merely missing mode !=pilot; root binder supplies actual mode. Full joined status cannot infer from non-NULL object.

5. Board completeness: listing-only rows are emitted only inside `if len(chosen)` currently. Emit the complete listed + eligible OI + quoted universe for each applicable cut even when quote file missing, empty or zero prior samples. Represent quote unavailable with NULL price and exact cut status; retain OI-only contracts and missing listing separately. Union `missing_file` is always false; derive union source status honestly from near/broad status (both missing vs one present, all empty vs no sample). Conflict canonical bid/ask is hidden, but invalid_numeric/nonfinite canonical values currently can escape; retain invalid original diagnostic fields while pricing fields are NULL. OI coverage ratios must use the explicitly declared available-OI universe; include missing OI count and amount-unknown, and keep zero OI different from missing. Listing-only or OI-only rows must not inflate quoted observations.

6. Histograms/raw statistics: the run loops EVERY board row and mergesorts the full cumulative histogram for EACH single value. Replace with vector batch/group aggregation per source/day or cut; no per-sample histogram merges. Current raw histograms are actually cut boards weighted by alias multiplicity, not raw quotation events. Compute exact source raw event and unique source/union event distributions from frame arrays before dropping them. Keep cut-board distributions as a separate population. Histogram keys must bind metric + population + source_family + cut (where applicable) + right + DTE; current `_histograms_for` ignores ALL dimensions and returns identical raw quantiles for every group. Retain exact raw and equal-date quantiles, metric support counts, missing and units. Alias multiplicity pertains raw source events, not a fictitious many-times sampled cut price. Spill sorted frequency parts/references if needed, then use exact rank merges for final groups. Never hold all49shard raw histograms at once.

7. `_bundle_for` unconditionally overrides mean_bid/event counts in every group from `_date_aggregate`'s 10:00 bid. It also casts raw_bid_sum to int, truncating fractional prices. Remove this override and derive per-group values from correct sufficient sums/counts/populations. `_merge_maps` must accumulate sum/count across every date (inspect current handling). Source raw, unique and cut means must have separate names/populations; don't mix counts. Paired_near_broad is aggregated over all cuts then reused in each cut group: retain per-cut/right/DTE exact-time pairs. Full year/stage groups need declared source/cut/right/DTE splits, not only all/all for non-all_period. Metric denominators/events are actual observations. The synthetic `known_unit_fraction` and `known_half_fraction` were incorrectly promoted from a request for literal fixture expectations into production hardcodes (1.0/0.5 regardless data). REMOVE them from production PRESPECIFIED/maps and tests; create a real two-contract example with one usable quote giving usable_fraction=0.5 and an independent all-observed coverage=1.0. Tests must distinguish two cuts with different actual prices, CALL vsPUT, two DTEs, near vsbroad, raw vsunique and unequal date supports/quantiles. No fixture whose expected value is a constant inserted by implementation.

8. Full reduction/output: `_concat_parquet_refs` reads all cut boards into RAM and rewrites them into one file. Preserve flattened authenticated part references with total row count; no full board decode/copy. `_auth_output_ref` hash+Parquet row/schema metadata suffices after acceptance, use reads only for coverage ownership and needed statistical sufficient data. `aggregate_rows.extend`/groups dictionaries/date-aggregates.json/distributions.json/report lines also multiply memory: process one chain or logical group partition at a time, retain authenticated group refs with a public iterator/reconstructor, stream readable complete metric tables. `write_results_md` currently only prints means/quantiles/sparse, omitting bootstrap intervals/ratios; include actual uncertainty and reasons for undefined. Current run_statistics duplicates all groups in three full JSON structures. Do not return huge `groups` inline in worker result; runner expects refs/counts/resources, fixtures can use iterator.

9. Reducer ownership/count integrity: bind exact chain/date ownership to coverage plus date aggregates; reject unexpected or missing keys, duplicated chains/dates, duplicate source file IDs with conflicting metadata. If partial combined selection is allowed, do not claim full based on child flags unless union==prescribed dates×chains. Preserve all part refs for subsequent valuation. Current source/support manifests duplicate full support files across 49shards and sums count duplicates; report unique semantic source counts separately from physical read counts. Reconcile alias_conflict_rows and identity_rows (currently omitted), intended_chain_date_units, coverage rows, cut rows, unique/listed/quoted/OI totals. Compare all semantic groups/counts (excluding documented physical I/O) between fresh two-day/two-chain run and combined disjoint shards. Authenticate chain_group_refs too if preserved. Reconstructable compact refs are acceptable, dropped metrics are not.

Return concise exact API/ref/schema changes and actual remaining limits. Root will integrate the reviewed code, run registered fixtures plus 8-date pilot, then apply the measured full gate. Preserve all original sources, clock unknowns and full cohort. Avoid additional features or undocumented source semantics.
