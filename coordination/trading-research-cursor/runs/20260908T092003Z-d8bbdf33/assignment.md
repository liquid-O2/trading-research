# Parallel execution of independent validation source units

Implement this complete bounded change in the isolated checkout. The user selected Cursor Grok 4.6 Extra High Fast for implementation, with Codex retaining scientific definitions, independent review, registered execution and integration. Use as many allowed tool calls as needed. Do not run shell commands, imports/tests, data scans or fits. Do not recruit another worker. Validation across all families precedes Context and then Location.

Edit only:

- tools/run_auction_flow_study.py
- tools/auction_flow_parallel.py (new, standard-library supervision/hardware support)
- src/trading_research/research/auction_flow_preflight.py
- tests/test_auction_flow_parallel.py (new)

Do not edit frozen protocol/extension/evidence/reference/config files or budgets. The controller will supply the exact hardware instruction document and an additive check-extension version when integrating. The canonical combined storage/trade check is currently running; this isolated draft cannot modify it.

## Purpose and exact scope

The nine existing `measured_unit` calls in `run_preflight` are independent full source-window computations. The current check runs them serially. Every unit includes its exact source pipeline, complete original-value reference, all implemented anchor/profile comparisons and storage. Run those same complete units concurrently in fresh Python subprocesses, retaining input order in the resulting `results` and `refs`. Keep prior-value comparisons, all three continuation comparisons, identity inventory and source schedule afterwards, with their current complete populations and semantics. Do not parallelize a dependent within-lineage carry chain, native kernels, fixture suite, or individual per-unit arithmetic. Leave the numerical code and process-time instrumentation inside `measured_unit` unchanged.

`run_preflight` should retain a sequential default for ordinary callers/fixtures and accept an explicit optional executor callback for the registered runner's complete units. This avoids importing supervisor machinery into scientific calculations. Expose parallel execution evidence separately from scientific measurements. No Context or Location computation is added.

## Hardware and limits

Use the supplied hardware: 21 vCPUs, 83,000,000,000 bytes RAM, RTX A4000 / 16 GB VRAM. Current cgroup v1 is CPU quota 1,785,000 us per 100,000 us (17.85 cores), memory 82,999,996,416 bytes. Read current cgroup v1 or v2 limits; NEVER use host `os.cpu_count()` or `/proc/meminfo` as the allocation. Bound maximum concurrent jobs by the supplied CPUs, floored positive quota, explicit ceiling 16, and available memory.

Use at most 64 GiB aggregate address-space allocation, also limited by supplied/cgroup RAM. Keep the coordinator at its existing 4 GiB RLIMIT_AS and each source child at 4 GiB. Allocate coordinator plus children before launching: with 64 GiB total this permits at most 15 concurrent children. Nine actual check units therefore run at most nine at once. A tighter live allocation must reduce concurrency or fail before launching if it cannot support one child plus coordinator. Native OMP/BLAS/Arrow/NumExpr limits remain exactly one per process. No GPU kernel is relevant here. Never multiply the scientific CPU or output allowance by the number of children.

The controller will create `validation/AUCTION_FLOW_HARDWARE_EXECUTION_V1.json` with kind `auction_flow_user_hardware_execution_v1`, exact family and `parent_protocol_sha256`, `declared_hardware` fields (`vcpus`, `memory_bytes`, `gpu`, `vram_bytes`), an `approval` with source `explicit_user_instruction` and original messages, a hashed `hardware_specification` reference, and `execution` fields `maximum_workers:16`, `aggregate_memory_bytes:68719476736`, `coordinator_memory_bytes:4294967296`, `child_memory_bytes:4294967296`, `native_threads_per_worker:1`, `gpu_workers:0`, `maximum_child_output_bytes:536870912`. Join this file and exact source hardware bytes into the configuration/snapshots in supervisor AND worker. Resource-only execution settings must leave maximum attempts 32, cumulative CPU 192000, check CPU 1200 + hard margin 10, output 16 GiB per attempt / 256 GiB family and all past use unchanged. Preserve existing protocol identity and registration.

## Process and aggregate accounting design

Use `subprocess.Popen` into the same runner's explicit source-unit entry point and the current interpreter. Avoid fork-based callable pools after Arrow/native libraries were initialized. Source children stay in the outer registered worker's process group, so the existing supervisor timeout/kill removes them too. Retain exact frozen native environment and Python hash seed. Do not create detached children or accept arbitrary shell commands.

Each source child receives the original registered packet path plus an exact bounded unit ordinal/identity and coordinator PID. It must check the current running same-family attempt, code/config snapshots, parent/process-group chain, native environment and declared limits before importing source-dependent modules or reading source data. Reuse existing checks rather than creating a second registry. Reconstruct the exact `resource_windows` unit from frozen protocol/index/calendar metadata, and reject changed ordinal/identity. Each child then performs the unchanged `measured_unit` using the correct coordinate index and calendar. Preserve all required reference bytes and scopes.

Give each unit a distinct subdirectory under the registered attempt's `outputs`, created once. Reserve at most 512 MiB derived output per source child before launching all units; this is much larger than prior actual 1–58 MB per unit and the aggregate reservation fits the 16 GiB check. Use existing `BoundedOutputs` and file bounds inside each child, including its result/receipt. Do not share a mutable BoundedOutputs across processes. The coordinator joins all completed/partial child bytes exactly once into its output accounting before continuing or failing. Child packets and reports are retained; inherited stdout/stderr can go to the registered worker log. No temporary market artifacts outside the attempt. Never count a skipped/failed unit as a result.

An individual process CPU RLIMIT does NOT enforce the aggregate 1200-second budget. Supervise all live source children while they run: combine the coordinator's total CPU since launch, already reaped child CPU, and current live child process CPU from `/proc/<pid>/stat`, with no double counting. Include prior fixture/check work in this same attempt. Use wait4 accounting and retain per-child CPU/wall/peak evidence; all source children must be reaped before the coordinator exits so the outer existing wait4 receipt includes their CPU. Apply the soft aggregate threshold early enough that the existing 10-second hard margin bounds sampling/termination overhead. On any unit failure, resource exhaustion, identity mismatch, timeout or interruption, stop remaining source children, reap every child, retain partial files and fail the attempt. Do not leave an untracked descendant consuming resources.

Record sampled aggregate RSS and enforce the already allocated coordinator+child address-space bound. The outer receipt must clearly distinguish per-process wait4 peak from aggregate allocation / observed aggregate RSS; do not report the largest child as simultaneous total RAM. Preserve the outer registry's actual summed CPU and attempt count. The CPU and memory monitor must parse cgroup/process files without assuming host resources or accidentally counting unrelated user processes. Wall watchdog remains finite and unchanged.

## Acceptance and tests

The controller runs the relevant suite and every real-data unit via one later registered check after complete diff review. Add focused stdlib/temp fixtures and literal expectations for cgroup v1/v2/unlimited handling with supplied ceilings, stricter quota/memory concurrency, zero/invalid quota/period/domain, unchanged scientific budget fields, child-ordinal and parent-identity rejection, aggregate CPU including completed and live children without double count, aggregate output reservation/accounting, deterministic result order despite differing finish order, and failure/interruption cleanup. Use tiny non-market subprocess fixtures only inside those registered tests. Ensure existing sequential callers and every preflight field/value remain compatible. Do not mock away the actual coordinator logic for all tests. No synthetic speed threshold and no performance claim before the actual registered parallel run.

Read the current runner, preflight and storage interfaces plus `tools/jumbo_resources.py` only as a hardware-input example. Do not import or couple Jumbo numerical code into this source-family runner. Keep this a bounded source-unit execution change, not a new general experiment framework. Return a concise complete patch summary and any unresolved concrete issue.
