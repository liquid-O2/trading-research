# Bounded implementation: complete source structure/memory arithmetic probe

Use Cursor Grok 4.6 Extra High Fast in this isolated checkout. Read AGENTS.md. Controller owns scientific definitions, review, integration and execution. Current phase remains all-family validation; no Context fit or Location-quality study.

Write ONLY two new files:
- src/trading_research/research/auction_flow_structure_memory_preflight.py
- tests/test_auction_flow_structure_memory_preflight.py

Everything else is read-only. No shell, imports/tests/compile, source scans, numerical execution, benchmark, network, dependencies, permission edits or worker spawning. Write tests but do not run them. Stop with concise summary and unexecuted-check caveat. Read the complete controller contract in validation/AUCTION_FLOW_STRUCTURE_MEMORY_PROBE_CONTRACT_V1.json. Do not invent alternative thresholds or weaken the original source checks. The controller will wire this helper into the registered runner afterward.

## Public entry and existing helpers

Implement `check_structure_memory_unit(measured, unit, *, measurement_reference, trade_storage, quote_storage, excluded_storage, outputs, contract)` returning a compact summary with `passed`, an output artifact reference, disjoint CPU stages, actual input/processing/emitted/serialized work and explicit limitations. Match the surrounding check_cohort_unit/check_anchor_unit calling style. Bind the exact contract kind/values and measured root/start/end/known-at/raw-instrument identities. Use current auction_flow_structure.py and auction_flow_memory.py as the candidate kernels, unchanged. Original measurements/structure.py and measurements/memory.py and the literal reference remain unchanged and independent. No tests package imported by production code.

Read auction_flow_storage.py, pipeline/data/windows helpers and the cohort probe for context, but do not repeat its unnecessary conversions. Retained Parquet projections carry actual original raw fields and uint64-capable physical source addresses. Decode with existing `read_series_tables` only and EXHAUST each iterator so its final row/byte accounting executes. Reuse one bounded set of owned PreparedTradeBatch projections per raw instrument; release all prepared storage in finally, including partly prepared collections on failure. No whole tape converted into Python row dicts. At most 50m input rows, actual batches <=65,536, current source and all instrument populations must match. Enforce cumulative retained prepared-array bytes <=1GiB before retaining the next batch. Existing child address limit is4GiB. Counter decoding, preparation and algorithm work separately; do not conflate reader batches, instrument batches, source rows and definition contributions.

## Source quality without looking ahead

For a continuous whole-unit swing path, start with the conditional acquired-source prefix, then permanently mark it incomplete from the FIRST actual source address with F4, unknown action, or invalid-size non-snapshot trade. Find the first F4/unknown address by streaming the complete retained quote/invalidation cache, which includes invalidations between ordinary trades. Find invalid-size non-snapshot trades in the complete excluded cache (read the real schema/reason bits in auction_flow_data.py). A clear R alone invalidates the book, not the observed flow history. Preserve source key, raw instrument, source order, physical row, event/known time, reason and all counts; no all-flagged-row Python lists. Validate domains/order before trusting an earliest address. Join against source_manifest projection/quality/exclusion counts and fail closed on disagreement. A trade at that raw address or later is incomplete; use physical source order as the tie breaker, not timestamp alone. This avoids allowing a nontrade gap between clean trades to connect their extremes. Do not use a whole minute's future gap count to discard earlier otherwise eligible confirmations.

Source ownership, archive presence and coordinates are separate. Check atomic coverage and source ownership evidence. If a whole raw-instrument path has structural source/ownership/coordinate unavailability beyond the precise flagged/unknown/invalid-size reasons above, report it unavailable for continuous swings and still count/consume all its rows as incomplete. This conservative continuous-prefix scope is distinct from finalized local minute bars. No post-gap venue-history recovery is inferred. Memory retains every observed selected print even when history is incomplete, but volume-clock decay is unavailable when the complete relevant window has missing history. Never label an observed arithmetic population venue-certified.

## Full real-window directional changes and finalized bar pivots

Run all five fixed exact threshold definitions from the contract in one DirectionalChangeStream over ALL current retained trades, using slices split at the precise first-invalid source address. Keep all emitted records and final provisional state/counters. Emit order is confirmation source order, then definition tuple order. Unknown aggressor keeps a priced price path; unpriced/individual flag4 inputs reset according to the kernel. No fitted scale or multiscale tree is claimed by these independent fixed scales.

Construct one actual CausalBar per retained one-minute atom, including quiet and unsupported atoms; exact start/end, publication=end+retained delay, instrument/definition and coverage provenance, observed raw OHLC/quantity/unpriced volume. Reconcile each bar's raw geometry/counts to a complete prepared replay and the retained atom. Do not fabricate per-event identity lists for prints you have not retained; the adapter may use an explicit input artifact+atom digest as a compact lineage identity, and label these bars as unpublished arithmetic rather than F09 admissions. Preserve high/low missingness, all quiet bars and source/coordinate status. Do not shorten lookbacks by dropping bad bars. Use all two length pairs × four equality rules from the contract. Compare FULL emitted `reference_swing()` records to original pivot_reference on every supporting bar, including quiet/gapped/bad cases, plus the complete delayed confirmation clocks. Preserve original definition/record IDs from the reference.

At most the first4096 complete timestamp-group prints per instrument enter scalar swing references; collect4097 to detect/truncate a final tie group. If all rows fit, use the true full group. If no complete bounded group remains, record explicit unavailable reference scope; never split a tie silently. On the ORIGINAL actual selected prices/time/orders/history, compare integer scales to original DirectionalChanges/PricePoint, every equivalent side/price/extreme/confirmation/address field; fractional scale uses an independent literal recurrence. Compare the candidate restricted to exactly that prefix, not later whole-unit extrema. Preserve every extra origin field under independent literal recurrence checks. No future suffix may change an earlier emitted record.

## Full real-window memory

For every raw instrument run ALL five memory recipes in the controller contract on the full retained unit, one definition at a time to bound peak memory. Use the fixed inclusive raw-tick grid for that root, frozen_at0, max_age24h and exact declared parameters. This wide fixed query grid is not chosen using a future outcome. Do not narrow it using selected prices; retain every grid cell and exact out-of-grid overflow. Keep all cluster members/content and raw/decayed fields, unselected/unknown/unpriced diagnostics and full first/last source lineage. Use max_selected_prints2m, max_clusters1m, max_result_bytes512MiB; do not raise limits if a real unit exceeds them. Preserve the explicit failure and charged counts. Whole-window source/coordinate completeness is passed separately, never repaired by local book reinitialization.

Reconcile full input count/volume/unknown/unpriced and selected masses against actual retained ordinary source totals and the matching >=75/>=100 source cohorts where applicable. The >100 comparator remains distinct. All-point/time-box24h raw field mass must match the entire ordinary sparse trade profile on the fixed grid with overflow accounted for; compare cells/side channels, not only total volume. The other four fields retain their actual values even when a source filter selects zero prints. Do not drop empty definitions or unknown-side clusters.

For independent scalar memory comparisons, collect at most256 complete timestamp-group actual prints with a257th lookahead. Build a separate candidate on exactly that prefix and its closed event interval, preserving raw source content and inherited quality. Compare full quantitative clusters/members/side/gross/count/center/bounds/times, raw/decayed cells/overflow/masses to an independent simple all-pairs and literal decay loop. Prefix volume age counts EVERY later prefix print, including unpriced/small prints; same-time groups do not age each other. Integer/box arithmetic exact; half-life absolute1e-12/relative1e-12 with maximum observed error reported.

Also compare to original build_memory on real small captures. Its API ages at its information `cut`, whereas this unpublished kernel explicitly ages at event_end and publishes after delay. Do NOT silently change that science or claim raw receipt parity. Use a separately named zero-delay event-clock arithmetic view for this original scalar comparator: actual event/price/quantity/side/history/order/member address values unchanged, original view's known_at=event_at and cut=closed prefix event end, with the original raw known-at retained beside the mapping. This named comparator validates the old numerical recurrence, not observed latency or serving admission. Production full-unit candidate retains the actual current named delay in all record availability. Compare its full raw known-at/address fields independently. Do not fabricate zero raw flags, sides or actions. Spell out the clock-view limitation in every scalar-reference report. Do not infer F05/F10/F11 source authentication from a small captured comparator.

## Evidence, costs, and tests

Use BoundedOutputs to write complete compressed reports with schema/definition/source/measurement refs, all emitted swings/pivots, all memory reports (each can be a separate compressed artifact) and independent comparison evidence. A compact top summary links exact output refs and includes measured counts/CPU/bytes plus `passed` only after all available required comparisons pass. A reference unavailable solely due to bounded all-one-timestamp selection must stay explicit; full source calculations still run. Distinguish absent data, no emissions and true negative comparisons.

CPU must close from helper entry through decoding, quality selection, preparation, full swing work, bar assembly/reference parity, full memory accumulation/finalization, bounded scalar reference, serialization, release and report assembly, with a separately named nonnegative orchestration residual. No overlapping timer sums. Serialize large object populations once where possible; do not repeatedly reread/decode a full source per definition. Counts must describe performed work, not theoretical fields/rows. Return actual total wall/CPU and output delta, without a projected annual/model number or a full-family claim. Keep limitations explicit: rolling/lifetime memory, visit/protection/markout, volatility-scaled swings, multiscale tree, divergence, FVG/block definitions and complete annual statistics remain separate pending work.

Write focused independent fixture tests exercising the public probe via real bounded retained Parquet series/BoundedOutputs and original reference producers, not mocks of the candidate math. Cover complete data and same-time cuts; quote F4 between two clean trades with no future-prefix rewrite; invalid-size exclusion; clear-only distinct from gap; empty source/quiet minute; multiple instruments/source rejection; wrong clocks/contract/schema/row totals; all source memberships/masses and complete pivot reference equality; limits/finalization/cleanup and closed CPU accounting. Full fixed grids need one production-contract shape test; smaller explicitly fixture-local grid contracts may be injected only through a clearly separate private fixture construction helper, never by weakening the public frozen contract check. No imports/tests/scans executed by this worker.
