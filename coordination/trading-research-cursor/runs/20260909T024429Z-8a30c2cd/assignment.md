Implement only the three new owned files in this isolated checkout: src/trading_research/research/options_valuation_numerics.py, options_valuation_native.cpp in the same directory, and tests/test_options_valuation_numerics.py. Cursor Grok4.6ExtraHighFast,1800seconds. No shell/import/test/raw-data/web/dependencies or otherfiles. Root owns compilation, frozen definition, source joins, study registration and actual cohort execution. This bounded kernel work proceeds while accepted OI/cross-market full runs and quote correction run independently. Required O02/O05 outcome fails without style-correct inversion/sensitivities.

Read /workspace/trading-research/validation/OPTIONS_VALUATION_NUMERICS_V1.json fully and /workspace/planning/trading-research/reference/trading-model/components/OPTIONS.md lines18-72. Do not read the entire project catalogue. Implement this frozen numerical contract, not source adapters/statistics/surface fits. There is no valuation engine yet. Existing NumPy2.3.3/SciPy1.16.2 are pinned, Python3.12; use ctypes for an explicitly enabled root-built C++sharedlibrary. Follow simple native pattern in canonical data/compact_native.py enable_compiled_projection: hash-check externally built library, bind signatures. Do not compile on import, execute commands, create folders or claim verification.

Public interface: vectorized black_price, black_iv, black_greeks with broadcast-compatible explicit F,K,T,sigma,discount,right arrays; retain ndarray result values/status/residual/iterations/brackets, scalar convenient wrapper optional. IV inversion bracketed, endpoint classification distinct from numerical convergence, near-zero-vega weak-identification flag. Use stable scipy normal CDF/erfc and put-call parity for deep-wing numerical cancellation where appropriate. Wrong units/styles/nonfinite/null inputs must be unavailable/rejected, never silently invent values. Fractions sigma notpercent; F-partials must never be labelled spot Greeks. Calendar charm holds F/vol fixed, evolves D=e^-rT; use explicit r to define the derivative. Include delta,gamma,vega,vanna,charm,volga and price. D and r are independently stated inputs; at each perturbation D_new=D*exp(-r*dT). Per-contract multiplication is a separate unit-tagged operation, not inside solver premium.

Native C++ American engine: batch independent parameter sets, shared finite-difference solve helper with exact fractional years and per-row discrete dividend CSR dates/amounts. No opaque Python per-node loops. Solve spot Black-Scholes obstacle with q0 using implicit/CN plus damping and projected tridiagonal/PSOR or correct equivalent; cash jump interpolates V_after(max(S-D,0)) and allows exercise immediately before dividend. Insert dividend times exactly, not rounded to uniform-grid days. Boundary conditions and domain include correct call/put limits at negative rates. Refinement at128/256/512 or caller-specified declaredgrid, with numerical-price error/status; preserve deterministic sigma0 optimalexercise separately and zeroTpayoff, never hide nonconvergence. Errors per row do not corrupt other rows. C ABI integer status and output arrays with checked sizes; no threads/processes. Don't let C++exceptions cross ABI. `enable_native(path,sha256)` authenticates file; expose native_price_batch and american_iv (batch C++ iterations preferable). AmericanGreeks finite differences at two independent steps/grid levels; report error and unstable flags, no arbitrary clipping. Use fixed-space grid while comparing bumps to reduce false derivative oscillation. No analytic approximation pretending to be discrete-dividend American.

Expose conditional bid/mid/ask inversion helper and full repricing response_cube at frozen joint shocks; absent/zero bid does not automatically remove ask upper bound, crossing and lower/upper price bounds explicit. Market quote interval distinct from solver tolerance and grid error. Weak identification/failed finite bounds returns undefined point with interval/status. No midpoint-only accuracy claim.

Independent tests: European ATM S=F100,r0,D1,T1,sigma0.2 hascall=put7.965567455405804; Black-Scholes spot100K100r.05T1vol.2 -> F100exp(.05),Dexp(-.05),call10.450583572185565,put5.573526022256971. Put-call parity, bid/mid/ask sorted IV and repricing, tinyT positive vszeroT, zero/ask-only/crossed, failed bracket/tinyvega, invalidshape/nonfinite, percent/fraction,multiplier100 explicit unit check. AnalyticBlackGreeks against independent central finite differences (charm calendar direction!), vanna mixed derivative andvolga. American no-dividend q0 call r>=0 agrees withEuropean; Americanput>=European/intrinsic with independent purePython high-step CRR reference used only for smallfixtures. Discrete dividend zero-vol r0,S110,K100,D20halfway,T.1: AmericanCall10 bypredivexercise while EuropeanpostdivCall0; negative rates use independent deterministic exercise extrema. Positivevol dividend sensitivity and multiplediv jumps via independently stated monotonic/lowerbound or separate refinedreference. Refinedprice/Greekerror reported, notjust same implementationroundtrip. Native fixture must call publicenable_native using root-provided env path/hash, fail visibly if registerednative prerequisite absent. Root will compile within trial then settheseenvvars; no skip toclaimallpassed. Avoid BoundedOutputs here (previous fixture bugs from using existingtempdir). Use tempfilechild when needed.

Primary reference reviewed by root: QuantLib fdblackscholesvanillaengine.cpp uses separate dividend model, exercise stepconditions and explicit maturity; https://raw.githubusercontent.com/lballabio/QuantLib/master/ql/pricingengines/vanilla/fdblackscholesvanillaengine.cpp . Do not copy code or dependon guessed QuantLib Pythoninterfaces; standard wheel Date resolution cannot silently round 0DTE. Root implements exact source/model maturity semantics separately. No acquired-data inputs in this assignment.

Keep implementation small and complete, focused on this API. Return exact C ABI/build flags, public signatures, numerical domains/status meanings, fixture env requirements and unverifiedlimitations. No full-family or performance claim from code alone. If FD inverse/derivative stability cannot be guaranteed, make it explicit in status; do not weaken validation assertions to fit generated values.
