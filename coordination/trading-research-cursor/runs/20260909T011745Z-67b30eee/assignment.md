One measured performance repair of the scientifically corrected cross-market implementation. 1800seconds, selected Cursor Grok4.6ExtraHighFast, same worktree crossmarket1, ONLY cross_market_alignment.py, cross_market_descriptive.py and tests/test_cross_market_descriptive.py. Root refreshed these from canonical AFTER all root scientific fixes. Read root's crossmarket1-root-integrate.py and crossmarket1-correction.md if needed; do not restore the older draft. No shell/imports/tests/scans. Root owns registered execution and acceptance. Return complete corrected files and exact change/verification design report.

Why required: registered 2020 pilot4 passed all37 tests, then exhausted900CPU seconds (exitSIGXCPU -24) with167,312,081 outputbytes and1.63GB peakRSS. Full is blocked by measured resource cost, not a new scientific issue. Evidence: /workspace/trading-research/reports/cross-market-runs/3224d8b35c2b190d3367073659864677f29ef2ca339e75da8f2c3d71e678fac4/execution.json and /workspace/coordination/trading-research-cursor/crossmarket4-diagnosis.json. Root registered a120CPU profile of unchanged2020 calculation: /workspace/trading-research/reports/cross-market-runs/b4e66d6268c9183e6a801990882fce483ce5c9f678eaa997258c42fc9607e055/outputs/profile.json (full function records), profile.pstats also retained. It includes37fixtures8CPU; actual110CPU. Do not treat profiler cost as throughput, no full pilot yet completed.

Measured dominant costs: process_year99.83s, _emit_source_events49.756s (9calls), prepare_unit35.885s, confirmed_pivots32.187s/26calls with16.45s own and~4million np.all reductions; Writer.add804232calls15.575s; _record_event_metrics227170calls10.226s; _past_receiver136302calls9.942s / last_available9.558s / backward_join_indices9.115s; _add_cell4,908,625calls4.505s;28m dict.get,20m list.append. run_length_and_id3.57s. Source parsing6.89s and annualcalendar4.85s are secondary. Optimize those measured dominant paths coherently, preserve full scientific population and output contracts. Aim actual2020pilot<400CPU, full5500CPU/4GiBAS/4GiB output with1.5margin under unchanged15kCPU family. Don't spend this assignment on unrelated cleanup or add a smaller science subset.

Required repair:
1. confirmed_pivots fixed2left/2right: vector shifted arrays or sliding-window view, validity/monotonic run/contract masks once. No np.all/min/max on tiny slice for every one of millions of bars. Preserve exact strict/weak tie policy, right2 confirmation known at center+3minutes plus delay, proper reset at gap and roll, centerbar high/low and reference formation end, low excursion againstpriorLOW. Running extrema can use compiled/vector run segments; don't alter policy. Add literal reference loop on small independent gap/roll/tie data that agrees everyfield with vector output.
2. _emit_source_events: prepare all backward joins for each source-event vector/receiver/lag at once. Per-scalar last_available currently revalidates sorted known arrays and creates arrays136k times; precompute indices/status/freshness once per event vector. Cache receiver known arrays, contract keys and futurelookup references. Form5horizon results in typed arrays, preserve source event / receiver label / event link identity and ordered deterministic rowIDs. Reference bar ending at event known is allowed; eventstart/future contract/stage valid, never break root scientific fixes. All0/60/120seconds,5/15/60/240/remaining horizons, 15/60 lookbacks, source variants remain.
3. Writer.add validates/converts every field in Python per row. Add a bounded typed Arrow/NumPy batch path for large aligned/lookback/event/link/contrast outputs, leaving scalar add for fixtures/small classes if useful. Declare schema once, handleNULL explicitly, nevercastnanosecondclocks viafloat. Emit compact same logical3table eventrepresentation and retain exact reconstruct_joint_event_rows. Table.from_pylist of millions is not sufficient optimization. Don't retain every year widePythondicts.
4. Grouping: replace millions per-row group_key/dict.get/_add_cell calls with vector/group reductions on a day/batch per fixed group/metric. Preserve exact raw metric event count, metric missing values, sums and independently supported date mean, plus path/no-event/stale/roll/censor/missing fractions and concordance. Do not omit raw quantiles if frozen originalrequires them; existing datesums CIsemantics remain unchanged. Bootstrap1000x5 block weights may be cached for identical date universe and reduced arrays; no repeated giantperrowmatrix. Profile did not reach fullfinalizer, statically ensure it cannot explode100k sameweightcalls.
5. Preserve accepted37 tests and root schemas. Add fieldwise literal regression for typedbatchwriter(NULL,>2^53 ns), vectorpivots and joins; numeric grouping reference sums/counts with multiple dates and missingmetric. Root will run all changed37+newfixtures and a complete2020 pilot, independently compare previousfailedrun's fully emitted prefixes and old reference formulas. No codeimport here.

This is a consolidated performance pass following the new measured failure. No changes to scientific protocol, no price/date/contract simplifications, no delay/horizon/variant removal, no compressed gap calendars, no silent missing=>zero, no source raw rewrites, no claimed completion from fixtures. Retain the same run(protocol,admitted,store,outputs,selected_years) API and all previous logical table fields; exact compact physical storage allowed with complete documented reconstruction if demonstrated output benefit, but avoid gratuitous schema redesign. Root runner is not owned. Stop with three complete files and concise report, no follow-on worker launch.
