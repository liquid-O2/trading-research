Complete the same profile/reference Deliverable 1 draft in one bounded implementation pass. Grok 4.6 Extra High Fast, 1800 seconds. Own ONLY the same three files in this isolated checkout. No imports, tests, commands, source scans or installs; root runs registered verification. The earlier frozen contract and /workspace/coordination/trading-research-cursor/profile-reference1-correction.md remain authoritative. Root has read the entire live implementation/statistics and all tests; the following are concrete remaining defects, not optional expansion. Do not drop any variants/cuts/metrics or weaken eligibility to pass. Correct the implementation and decisive literal tests, remove the large _DEAD_LEGACY_LOOP string (unexecuted duplication), and report exact interfaces, changes and residual limitations. This is a reassessment after static review, not a performance run failure or permission for scope growth.

1. Chronological state still leaks memory and loses history. AliasIndex.by_slot/by_content retain full atom dictionaries/cells for the year after pending is trimmed; future_cache keeps every future result; statistics geometry_index/exact_index/proxy_index and TPO index retain all wide rows. Bound alias state by the next input unit start: once all possible overlapping units are consumed, prune identities older than that watermark. Preserve conflicts through the last possibly overlapping source. Pair variants at a single anchor/cut and release the group after all expected variants/proxies; TPO after the two representations. Release future_cache after all forms/lags/horizons for that cut are consumed. Avoid retaining every Python float in _DateStore.raw: typed arrays, exact frequency maps where naturally discrete, or bounded spills with exact quantile reductions. Do not drop raw-value quantiles. IdentityEngine.pending retains only four hours, losing prior_cash_rth and Monday/Tuesday history. Retain required fixed-clock history (a bounded eight-day active atom buffer is acceptable), then prune by watermark. Civil periods retain accumulated cells/moments/compact provenance; avoid redundant all-year atom dictionaries.

2. Civil resets are wrong. civil_mass/civil_ends/civil_tpo never reset by week/month/quarter key, so June appears in July and TPO gets old origins. Track actual period identity separately for each variant. Emit the previous period's partial or complete result when crossing a period boundary even if its last day has no atoms, then reset to the new origin. Do not require observing Sunday/the final day to emit a previous partial period. flush_ready currently skips every out-of-selected-year day, so the 370-day support selection does not warm state at all. Warm needed prior civil/fixed state without emitting an out-of-cohort observation; selected year determines output ownership only. Future support must also be consumed without emitting next-year observations. Add actual two-week, two-month, two-quarter and Dec/Jan tests with literal expected disjoint volumes and origins; include missing period-end day.

3. Completed reference clocks: _emit_completed stores completed_id from the FIRST 68% variant but later treats it as the baseline 70%; bind the baseline ID explicitly. published_completed appends oldest first while developing next() selects the oldest matching anchor, not latest already published. Select max publication <= cut. A mutable prior_close can be updated by cash_rth before an earlier anchor is emitted because fixed anchors are unsorted; select prior close from a publication-indexed history with known_at <= anchor origin/decision. PIN069 computed for today's 06-09 is currently attached to prior-day or earlier completed anchors; attach only when its real known_at <= that geometry's publication and applicable identity. Preserve actual selection/input known times and the required source provenance. Tests must change later data and prove earlier numerical VA/reference values and PIN/close fields unchanged, not just assert geometry IDs differ.

4. Real window API and joins: test _window_rows STILL fabricates a feature without latency_ns, complete, chronological_eligible, data_complete/contract_stable. Read and use the actual feature_schema/label_schema and actual four formations × three latency arms. emit_join_rows iterates all 12 features THEN all three latencies: it should use only the matching feature latency per arm, giving 4×3×4=48 intended rows for a geometry cut, not 144 mismatched combinations. Zero must be explicit, never `or -1`. actual_known_at_ns is copied from scenario feature. Preserve actual own geometry input publication (250ms source) separately; calculate counterfactual scenario publication explicitly from source publication minus declared source delay plus arm, with selection and required input bounds. Do not substitute scenario for actual. own_ready currently `prefix_ok and (coverage_ok or prefix_ok)` ignores geometry source/price coverage. Require the profile's own eligible/complete priced population, contract coordinate and same stage, plus feature eligibility and mature label within the same named stage. Test COMPLETE feature + INCOMPLETE geometry, rather than setting both false. Geometry identity uses first day's source atom, so a cut in a later adjacent file looks up the wrong acquired/source metadata. Resolve the exact owning window source identity by cut and provenance; support atoms from the other file remain contributors. Match exact metadata hash/window bounds, never path-only. Future frozen-grid output must use max future input known_at as label maturity, not original cut. Retain separate future profile coverage; do not count partial future metrics as eligible complete.

5. Numerical completeness/exactness: integer_profile_geometry uses int64 dot/square expressions whose intermediate can overflow on annual civil mass. Keep exact Python integer moments/rationals or proved-safe NumPy path with fallback. Raw rational numerators/denominators also may exceed int64; preserve exact typed representation (decimal256 or decimal strings with explicit decoder) rather than silent overflow or approximate sums. Add a large-volume literal beyond int64 squared total. compact_side_geometry currently always side_wasserstein=None and baseline passes no view, so this required metric never exists. Compute normalized buy/sell CDF distance on their exact same physical tick grid, undefined only when a side has no mass. Check dimension of dominance_margin against original profile kernel definition; current Fraction(max-second) is mass but metric says dimensionless. Preserve physical tick conversion for coarse grids and transport.

6. Statistics omit many surviving fields: SD/percentage bands, weighted quantiles/MAD, TPO IB high/low, tails/provisional/final counts, POC traverse/terminal-outside, future VA/overflow for all six side/direction fields, unpriced and transport. Add each required named field with its unit and undefined denominator; do not just include schema columns with no statistics. Join no-event rates grouped by measurement_status are always 0 or1; retain an intended-link denominator group across statuses so rates convey actual missing/censor/no-event prevalence. Conditional descriptive groups may coexist. Distinguish link rows, independent geometry/events, and dates. Separate partial observed geometry/future from full eligible estimates.

7. Reuse finalizer: fresh adds all completeness counters; reuse adds only13 and loses link_rows/units_selected/atoms_scanned/cells etc. Aggregate the identical semantic completeness payload for both; operational new/reused counts intentionally differ. authenticate_reused_partition reads every Parquet value again only to finalize already authenticated groups; use exhaustive output hashes/size/schema/row-count certificates as applicable, don't recompute all geometry. Current reducer loads all partition groups and joins strings for a huge full JSON+Markdown; stream partition group refs and a complete readable table/report, preserving public reconstruction via bound refs. Root's runner currently expects run_profile_statistics returns a summary dict, selected_partition_keys accepts encoded list keys, profile_partition_source_refs(members, role='selected') + (...neighbors,role='support'), and partition measurement/refs/counts. Preserve these APIs or report every exact change. Fix tests incorrectly `first, _ = run_profile_statistics(...)`: it returns a dict. Build actual support neighbor (not just same unit called support), prove corrupt neighbor rejected. Compare all logical statistics/counts for fresh vs reuse except intentionally operational fields.

8. Decisive tests: correct actual feature/label schema and count48, eligible zero latency with actual/source vs scenario clock distinct, own geometry rejected despite valid feature, adjacent-file cut links admitted to correct owning identity, cross-year prior carry and each calendar period reset, earlier numeric references unchanged after future data mutation, >int64 raw numerator roundtrip, nonzero literal side Wasserstein, every required schema field has named statistic, mixed observed/no-event link population rate e.g.1/2, all semantic fresh/reuse counts and groups equal. Preserve previous original kernel/reference fixtures. Existing tests often only assert field exists or any reason exists, which does not establish numerical behavior. Keep fixtures bounded and independent of implementation formulas. Do not execute them.

Scientific scope remains all prescribed profile/reference measurement, timing, links and statistics. Root is independently reviewing quote-quality and verifying OI/cross-market; your work is solely these three profile files. 4GiB per-process address space, pilot1800CPU, full48000CPU/16GiB physical output remain bounds requiring measured projection after correct draft. Return complete honest code; no invented successful measurements.
