Correct the quote-quality draft after root's full review of both modules and all923test lines. Same three owned files ONLY, selected Grok4.6ExtraHighFast,1800seconds. No commands/imports/tests/rawscans/dependencies/web/otherworkers; root owns registered execution. Original options-quotes1.md and frozen OPTIONS_QUOTE_QUALITY_SUPPORT_V1.json remain authoritative. Do not shrink sources/dates/cuts/groups/metrics. Root's source admission succeeded:17,067,661,037 quote rows,20,029files,86,535,875,010bytes. NDXP near3,477,034,361 plus broad6,297,946,089 (maxsinglefile7,159,992rows, maxnear4,340,882). This is BILLIONS, not millions. Current code will fail memory/runtime and several scientific contracts; complete one consolidated correction.

Read canonical OI columnar/measurement schemas, not the stale copies; OI8 accepted pilot refs are in reports/options-oi-runs/14d06cc09a2cc6b6f24191201b74d8a3c908c79990617f3140583c0d0f24fa02. Full9 now active. Do not mutate OI files. Quote actual admission CAS /workspace/trading-research/evidence/trials/artifacts/74/742c4efadaa948605375a4a238ac08a31e403f2001fdac72e5cd3aa00ceae787 contains schema strings, metadata and admitted sample rows (no raw scans needed); review /workspace/coordination/trading-research-cursor/options-quotes-admission1-review.json.

1. Real columnar implementation. QuoteCodebook.intern loops everyquote creating5tuple, sort_and_dedup loops EVERYunique event, _union_two loops BILLIONS and _merge_union_rows creates dozens of one-element NumPy arrays per overlap. Replace with unique dictionary identity interning and vector typed lexsort/searchsorted/reduceat/masks. Never construct row dictionaries or object tuples for every sample. Read Parquet repeated symbol/OSI/right as dictionary-encoded arrays, keep compact integer identity/flag/date/family codes; don't expand duplicated chain/family/request/OSI/etc to giant objectarrays then copy them for every filter. nparraytypes must fit a10m+ combinedday plus sorting under8GiBAS. Projected one-day arrays are acceptable if bounded; release source tables/intermediates promptly. Codebook should intern valid unique identities once; invalid identities need addressable distinct dispositions without sorting None against str or int(None). Own-family and union can share read-only arrays until mutation required.

Current alias_conflict writes one wide dictionary per common sample (potentialbillions!) contrary to storage contract. Retain exact aggregate alias/conflict counts with compact source-address reconstruction; cut boards keep latest/run-start/all necessary alias provenance. Exceptions can be typed ranges/masks; arbitrary overlap reconstructs from input refs+exact grouping algorithm. Do not duplicate every unchanged sample. Aggregate raw/unique/matched metrics as columnar arrays or sufficient counts/frequency histograms per date, not per-rowPython. Boards also need vector construction: current np.any(listed_ids==cid) for each cid is quadratic and _board_row percontract×15cuts too costly. Use typed membership joins/searchsorted, gather+nullableArrow output. No need one extra interval tape.

2. Dedup/run correctness. take_kept retains run_start indices into the OLD pre-dedup table, so _board_row can index wrong or out-of-bounds after aliases removed. Store actual run-start timestamp/address directly, or remap indices. _union_two currently structured scalar `<` comparison may fail; use vector joins. Union must propagate preexisting within-family conflict even if representatives match, sum alias multiplicities (not hardcode2), preserve all8payloadfields AND their null masks (NULLexchange is not0, NULLsize not0). Conflict has no winning price: retain original addresses/diagnostic payloads separately and make canonical usable price/mid/spread undefined. Quality base precedence remains invalididentity/clock/numeric before conflict as frozen. Raw original classifications must not be overwritten by unique conflict classification. Invalid numeric or conflicted rows break run continuity; gap/invalid boundary is preserved through aliases. Current invalid excludes invalid_numeric, and firstsample continuity alwaysTrue. Define observed run continuity and lower-bound honestly; actual update age stays unknown.

3. Clock/identity guards. select_cut_rows uses declared filename date, ignoring original row request_date even when future; effective availability must require BOTH source acquisition/declared date and original request_date<=cut_date. Do not silently use an invalid requestdate. Preserve requestdate mismatch flag and originalsource date, exactns; avoid float conversion. Latest current invalid/conflicting state must not fallback through older valid. Invalid OSI/right/strike cannot mint a supposedly valid tradable contract; preserve unresolved identity and potential conflicting original evidence with explicit unavailable disposition. A malformed identity cannot reliably suppress an unrelated valid identity merely because OSI text matches. Preserve original null bid/ask/size/exchange/condition and numeric invalidity; _int64_column currently truncates floating integers and loses nulls. Reject/schema-disposition inexact integers; native inputtypes are int64. _source_quality_rows omits invalid right/DTE rows whenever any valid group exists; retain explicit unknown group so rawgroupcounts reconcile exactlytoallinputrows. Use session close boundary consistently with cut inclusion; unknown actualknown remainsNULL.

4. OIJoinAdapter is neither bounded nor date-specific: it loads allreports(~93m) and allintervals as Python dicts, asof_oi scans allintervals for everycut and caches allchain/date/cut indices forever. Replace with projected typed arrays partitioned/filter-selected chain/date horizon, exact vector interval lookup; release day state. Authenticate each artifact hash/size/rowcount and required schema, preserve compact first/last report encoding from canonical reader. Load selected chain identities once, selected reports/date and intersecting asofintervals once or bounded index. Root full sharding will use CHAIN×YEAR (or month chunks if necessary), not7imbalancedchain-onlychildren; source rows and OI support must remain available around each selected cut. The actual OI report has listed flags, and coverage table has listing_denominator_known/listing_file_present/listing_status. Current code declares every nonVIXday listing_known simply because OIobject exists, turning missinglisting intozero. Bind daily OIcoverage and preserve unknown/empty/missing. Include union of observed listings, quoted contracts and relevant OIcontracts as prescribed, with explicit identities and OI missing/ambiguous/stale/expired dispositions. OI-weighted quoted fraction uses one well-defined universe and numerator subset; no incompatible listing-only numerator/allquotedOI denominator. For zero sum undefined. Namespace locally-unmatched quote IDs acrossshards or make them deterministic; don't collide two different contracts withsame integer0 onreduction. Full joined completion must validate actual OIfullpopulation, not justnonNULLdict (root operationalbinder alsochecks).

5. Actual support metadata: ETF role is qqq_minute/spy_minute; dataset quantpad/nasdaq__qqq-etf__ohlcv-1m and quantpad/nyse-arca__spy-etf__ohlcv-1m; source has NOchain. instrument_id is int32 6424(qqq sample)/15144(spy sample), notstring"QQQ"/"SPY". Bind symbol from exact admitted dataset/role and retain rawinstrument ID; no guessednumericmap. Current _add_etf keys6424then etf_at('QQQ') findsnothing. t=1536154200000 epochms bar START (see accepted cross-market bar semantics); an eligible complete minute has bar_end=t+60s<=cut. Current t<=cut leaks currentbarOHLC. Preserve barstart/end/scenario known, source ref+row index and age fromcompletion, and gapflags. Use typed arrays not annualmillionfloatPythonlists reconstructed everycut. Support records currently never join consumed/manifest/counts, so authenticated reduction doesn't bind them: include allsupport sources and declaredscope with exact hash/schema/rowcounts, quote vs support counts separately. FRED currently chooses rows[0], usuallyDFF, silently dropping every tenor; retain EACH declared series/tenor/realtimestart/end and missing rate, last eligible observation independently with nofuturefill. Cash earlierclose behavior remains retrospective and actualpublicationunknown. Corporateactions retain each actualexdate/dividend/split and source, not only latestaction presence as a substitute forsource support. NULL announcement clock distinct fromfalse. Support per chain/cut should emit once, not duplicate for everyquotefamily. Keep unknown/unavailableVIXcash perprotocol.

6. Statistics are currently wrong/incomplete. _date_maps divides usable/conflict/crossed etc BOARDrows over unique dailycontractcount, so fractions exceed1; near,broad,union and5cutpopulations are mixed. Every fraction must use same namedpopulation fornum/den. Quoted/listed numerator is intersection, notallquoted. Exclude nonapplicableearlyclosecuts fromeligibledenominators. Preserve ownfamilies/union/matched source events and cutobservations separately. build_group_statistics only buildsallfamilies/right/DTE despite required chain/sourcefamily/year/stage/cut/right/DTE. Its cutgroups replacecoverage butstill useALLcutprice/supportmeans. Rebuild from grouped metric sufficient sums/counts bytheactualdimensions. Allsix stale metrics are explicitlyNone now despite data; compute them. Add everyclass/flag fraction, numericprice/spread/age, listing/OI support, rate/underlier/action with metric-specificn/dates/missing andunits. Rawvaluequantiles are required: currentexact_quantiles_from_values isunused and onlyquantilesofdatemeans arewritten. Use exact typed frequency histograms or boundedspill and exactrankinterpolation; include rawanddatequantiles separately, not approximation. For source events distributefloats with exactrawmultiplicity vsuniquededupweight separately. Preserve alias mass. Shared bootstrapweights overexactdateuniverse, fixed1000replicates5day95% seed20260908; no eventmatrix. Sparse support counts must be actualmetricobservations notdefault1perdate. _paired_stats currently matchescontract/cut withoutsame ts andcomparesonly4fields; requireexact commoncontract+cut+sampletimestamp andall8payloads/nulls; report unmatchedsampleclocks and ownsupport separately. Add independent knownexpected fractions1/2 andrawvsdatequantiles thatdiffer. Readablereport mustinclude allmetrics/fulltables via streamedreferences, no selected10metriconlysummaryclaimingfull.

7. Reducer authentication/sharding. Current combine readscoverageParquetwithout hash andneverauthenticatesboards/source manifests/groups; bind and exhaust alloutput refs andsource/science/implementation/OIidentities beforeacceptance, compare declaredrowcounts. A partition cannot passed=false, claims completedsubsetmustmatchactualchain/dateownership, no duplicate/missingfullownership hidden. selected_dates=None must be expanded toactualintendeddates, notemptyownership. Current group_refs forbidsrepeat chain, preventingCHAIΝ×YEAR shards; support disjointdates for samechain, merge sufficientdate/rawdistribution stats orauthdistinctgroups with proper all-periodmerge. Keep completeParquetmanifest/boards/identities andsource refs reachable fromcombinedresult; currentonlyreturnscoverage/stats losingcutboards needednextvaluation. Combine allsemanticcounts correctly (currentomitsuniqueevents/listed/quoted/OImatch etc). Same admitted corpus identity mustbindsources, notjustfilecount; root caninject authenticatedadmissionref operationally. No allchainfullpassflag frompartialOIpilot. Exhaustivelycheckfreshvscombinedlogicalgroups, counts andcutrows includinglocallyunmatchedIDs, disjointdatesonechain andtwochains; corruptcoverage/board/manifest andmissingdateownership rejected. Document namespace/reconstructor ifIDs scopedpartition.

8. Fixtures: severalboard tests expectlen1 butbothnearandunion emitrow; filter sourcepopulation explicitly. test"sort"inINTERVAL_RECONSTRUCTION currentlyfailsbecausepredicate lacksword; testactualreconstruction notword. Add realETFint32/startclockfixture, alltenorFREDwithNULL/latestdifferentdates, actualOIcoverage missingvszero, mutatedsupporthash, samefamilyalias removal withlaterdifferentcontract runindex, samefamilyconflict+equalcrossfamilyrepresentative staysconflict, nullvs0payload conflict,future originalrequestdate, unknownrightcountreconciliation, >2multiplicity, exactsamecutdifferenttsnotpaired, allfractionranges, rawquantileswithdifferentdatecounts, anddisjointdate-samechainreducer+corruptartifact. Use actualschemas, publicrun/writtenartifacts, independentliteralexpectations; do notweakenassertionstopass. No execution here.

Return complete threefiles andconcise report ofexactAPI/schema/references/remaininglimitations. Root thenreviewsandintegratesonce, registeredpilot andmeasuredfullgate. Originalscientificscope remainsunchanged. Eliminate knownbillions-rowPython/onealiasoutput-per-sample pathsbeforepassingdraftback.
