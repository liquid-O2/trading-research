# Actual whole-window and one-minute cohort validation

Continue with one coherent implementation, ONLY these NEW files:

- src/trading_research/research/auction_flow_cohort_preflight.py
- tests/test_auction_flow_cohort_preflight.py

Do not edit the cohort kernels, their tests, runner, existing preflight, protocol, budgets or evidence. The controller reviewed and integrated your previous two-file draft and made small corrections: exact Python fallback also covers absolute knots/cutoffs beyond int64; one acquired source key per CohortWindow; returned addresses are copied; training publication cannot precede member end; the frozen test reference is required. Read the current kernel interfaces. They remain unverified until the combined registered check. Do not execute shell, imports/tests, source scans or fits. No other worker. The controller will wire your helper into measured_unit and an additive extension once all review is complete.

Read `controller_inputs/auction-cohort-preflight-contract.json`. The controller will embed that EXACT object as `cohort_validation` in the new check extension. It selects a retained complete 2020-03-16 training resource unit separately for NQ and ES, fixed source filters, soft benchmark knots (1,30,61,75,100), count and volume quartiles, and the 13/20 count quantile for top-35%. This is a bounded exact-arithmetic/resource probe, not the full 2020–2022 fit population, F11 admission or a Context/Location study. Current acquisition variants remain separate.

## Interface and resource discipline

Expose `check_cohort_unit(measured, unit, *, measurement_reference, trade_storage, outputs, contract)` returning a compact summary with report reference(s), passed, disjoint CPU components, exact workload counts and output bytes. Match the style of `auction_flow_anchor_preflight.check_anchor_unit`. Use existing size/hash-bound `read_json_artifact`, `read_series_tables`, `BoundedOutputs`, PreparedTradeBatch, the new cohort kernels, and unchanged scalar CohortDefinition.weights / `_cohort_path`. Do not re-read the raw market source; use only the complete retained trade series and declared training reference.

Every read_series_tables traversal MUST exhaust its iterator so full file and series counts are checked. Even after collecting a bounded reference prefix, continue validating/charging remaining decoded rows without retaining them. Source batches remain <=65536, single acquired source key, original order and full raw field projections. Keep all current raw-instrument populations, including incomplete history. Never turn a gap or incomplete coordinate into a complete empty window. Do not impose arbitrary scalar-fit limits on the full columnar path; whole-day print counts use the existing 50-million ceiling.

Validate the contract's exact kind, limits, fixed filters, soft knots and declared adaptive recipes before doing dependent work. Join the selected training resource unit's exact size/hash, root, 2020-03-16 date, kind, complete source/coordinate flags and actual instrument. The training member must contain one complete coordinate. Fail a corrupt or mismatched declared reference; do not silently substitute another date/source. The training and current source identities remain explicit throughout.

## Complete 2020 prefix used for unpublished arithmetic

From the selected training series, collect at most the first 4097 eligible prints for its raw instrument while exhausting/validating the entire series. Use at most 4096 prints for fitting. If the 4096th and 4097th share a timestamp, trim the whole final timestamp group, so no training event-time boundary falsely claims a partially retained timestamp group is complete. If the entire unit has <=4096 prints, its full terminal group is allowed. Empty resulting training prefix is an explicit unavailable probe, never a fake fit.

Training start is the retained unit start; end is last selected event timestamp + 1 ns; published_at and train_end are end plus the retained source delay; available_at equals train_end. The full retained source window must be complete before asserting that this prefix is complete. Record the original retained unit/series reference, selected count, first/last source row/order/key, exact prefix value digest, end, publication and original data/fold scope. These form the training member identity. Histogram source/fold manifests explicitly say bounded resource arithmetic, not a full historical fit. Use the exact new histogram fitter for each declared adaptive recipe, retaining requested/realized supports and occupancy.

Independently compute requested thresholds by sorting the <=4096 literal sizes and accumulating count or size against exact Fraction targets, following the ORIGINAL size+1 rule; compare every threshold, channel interval and realized count/volume with the histogram result. Do not use the histogram result to manufacture its own expected values. No probabilities or future outcomes are selected here.

## Current unit: eight definitions, complete observed paths

For each original raw instrument, construct fixed all-flow, the three exact fixed source cohorts, the fixed soft benchmark, and all three unpublished adaptive definitions. Adaptive start is max(current unit start, definition.available_at). It must never precede training availability. On the 2020 training resource day, adaptive paths cover only that later suffix; later units use the whole requested window. If no positive interval remains, retain a specific unavailable disposition. Preserve whole and per-minute coverage flags from the existing measured record; adaptively clipped first minutes inherit the conservative flag of their containing original atomic minute. A missing early interval cannot make the parent history complete.

Traverse all current retained trades once. Prepare bounded per-instrument slices once and share them across these complete paths. Compute each definition's whole supported interval and EVERY one-minute atomic interval, including covered quiet and explicitly incomplete empty intervals. Use the existing atom boundaries in measured['instruments'][...]['atomic_windows']; do not shift cadence to training availability, and clip only the first adaptive atom. Check exact original t/delay/source order/instrument. Atomic windows use zero mathematical opening, which preserves their full relative path; compose them in time order with cumulative opening offsets to reproduce the continuous whole interval.

For each whole and atomic source-filter path, compare all available exact common fields (OHLC including opening, first high/low timestamps and orders, buy/sell/unknown, volume, prints, signed bounds and completeness) against the EXISTING unchanged original measurement records where the intervals match. Excluded source channels stay in the new report and must reconcile to all-flow. For adaptive suffixes, do not compare a suffix to an unrelated full-window baseline.

For all eight definitions, compose every atom's close and translated high/low, first attainment clocks/orders, side masses and weighted counts; compare the full resulting path against the separately accumulated whole CohortWindow. Respect ties at an atom opening: retain the already attained earlier extremum instead of assigning a fictitious new event time. Compare every bar population and summed partition/unknown mass. No reference based only on closes can validate highs/lows.

In parallel with the full current traversal, retain a bounded <=4096-print prefix per definition/instrument (after its own available start). Compare its complete path against unchanged `_cohort_path` and exact scalar weights, using original addressed Trade rows, reported side, timestamps and strict original source orders. Limit only this independent scalar reference; the measured whole and ALL minute populations remain complete. Independently recover first high/low source order from literal ordered updates because CohortPath exposes timestamps but not source order. Keep this reference cost separate from candidate arithmetic.

## Reports and measured cost

Retain actual full-window and every atomic cohort path, both occupancies, history flags, origin/availability/definition identities, reference counts, and local unavailable dispositions. Use compressed exact JSON with shared metadata and explicit field columns if needed to avoid repeating thousands of identical identity/key strings. Any compact rows must have a complete named schema and preserve every numeric/path field and each bar's own timing/coverage; no rounding or omitted empty bars. Process/publish per instrument or definition to stay bounded rather than building repeated copies of every tape. There is no new Parquet codec in this assignment.

Measure disjoint CPU for training cache validation/selection, histogram accumulation/fit/reference arithmetic, current cache decoding, preparation/filtering, whole path updates, atomic path updates, whole/atomic finalization, original source comparisons, full atom composition, bounded scalar reference and serialization. Count actual decoded rows/batches, prepared/sliced prints, instantiated/report bars/channels, full path print contributions, scalar reference prints/channels, and serialized bytes. Return these actual counts; do NOT extrapolate annual cohorts, fit/model/statistics or unimplemented native dense cohorts. The controller will use them for complete workload costing later. No synthetic speed assertion or accepted performance claim.

## Tests

Use small complete temp-file ParquetSeries fixtures, existing raw trade builders and actual kernel/reference code within the eventual registered suite. Exercise ordinary signed/unknown data, complete and incomplete empty minutes, cancellation with nonzero intra-minute extremes, same-time/order ties across reader and minute cuts, all fixed cutoff neighbors, soft/interior weights, training group truncation, count/volume/top35 ties, adaptive suffix start at its availability (including a partial first minute), full-vs-atomic/reference equality, two raw instruments with local coverage, changed training root/hash/size, incompatible source key, incomplete training, and a tampered expected measurement failing rather than silently passing. Assert complete population counts and output values by decoding the resulting artifact, not only a passed flag. Keep standalone fixtures small; real nine-unit execution belongs to the registered controller run.

Return only the two new files and a concise summary/remaining concrete issues. All-framework validation remains the active phase.
