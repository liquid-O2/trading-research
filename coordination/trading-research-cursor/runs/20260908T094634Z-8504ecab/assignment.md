# Consolidated review repair: parallel validation executor

Continue the same bounded implementation. Codex has reviewed the entire initial diff. Fix the concrete defects below together, keeping your original four-file scope and all scientific computations unchanged. Do not run shell, tests, imports, data scans or another worker. The controller will review and run the combined registered check. The actual hardware instruction is already present in this checkout at validation/AUCTION_FLOW_HARDWARE_EXECUTION_V1.json. Read it; do not edit it or any frozen document.

1. `approval.messages` in the authoritative hardware document is a list of dictionaries `{at, text}`, not strings. Validate that actual schema and nonempty string values; fix fixtures to match. Compare the document's complete `unchanged_scientific_resources` object to the protocol's resources as well as keeping literal limits. Validate supplied live-limit dictionaries and file reads with strict integer types: quota is exactly -1 or positive; period is positive; RAM is None or a positive integer. Reject quota -2, bools, fractional values, missing/zero periods, zero memory, and malformed domains with ValueError. Do not treat every negative quota as unlimited. The fixture writers currently omit creation of their supplied root (`root/v1/cpu` mkdir and v2 file writes), causing FileNotFoundError; create those fixture parent directories.

2. The outer parent MUST charge its final `wait4` CPU for the registered worker and all reaped descendants. Never replace it with the pool-end aggregate measurement (which omits all later retained-value, continuation, inventory and reporting CPU), and never add already included child CPU again. Pool evidence is a checkpoint, not the final charge. Clearly name the authoritative final receipt as including descendants. Add a tiny independent nested-process fixture proving the final accounting includes CPU before the pool, child CPU and post-pool work; retain ordinary pool measurements separately.

3. The pool monitor currently starts `reaped_cpu = 0` and overlooks fixture subprocesses reaped before pool startup. Use coordinator self CPU plus the coordinator's entire RUSAGE_CHILDREN CPU plus current live unreaped pool-child CPU, with no double count. Keep separate per-pool-child wait4 totals and all-reaped-child totals in evidence. Capture one self CPU value per published report so component sums refer to the same instant. Only this controller thread calls wait4 for these live children, so sample before/after the same-thread reaping consistently. Include prior finished fixture child CPU in tests and prove live-to-reaped transition counts once.

4. Aggregate enforcement must continue after the pool. In the registered runner (not in generic test calls to the helper), after all source children are reaped, lower coordinator RLIMIT_CPU to its remaining SELF ceiling: floor(check soft limit minus ALL RUSAGE_CHILDREN CPU), hard floor(check hard limit minus ALL RUSAGE_CHILDREN CPU). Reject exhausted remaining allowance; do not raise existing hard limits. Preserve the original 1200+10 aggregate budget. Apply this in cleanup as appropriate too. No subsequent source child may be launched after this step. The outer final wait4 receipt remains authoritative. Include focused mathematical-boundary tests and a tiny subprocess enforcement test where practical; do not alter the main testing process's limits in a fixture.

5. Each child currently gets a 512-MiB artifacts allowance PLUS its packet, exceeding the declared per-child reservation. Bound the packet write and subtract its exact retained bytes from the artifacts allowance, including the final receipt in artifacts. Recheck each complete/partial child directory size against its 512-MiB reservation. Join all retained child bytes exactly once even when they exceed a limit: record actual retained bytes before marking failure, never silently omit them. Check result and receipt reads through bounded checked/load_reference helpers, not unbounded read_bytes. Reject oversized files and wrong expected size/hash before JSON admission.

6. Bind child_packet_path and output_directory to the EXACT registered namespace `RUNS / attempt_id / outputs / source-unit-{ordinal:02d} / packet.json`, after strict ordinal validation. Resolving to any other parent is insufficient. Put attempt/trial/protocol/code snapshot/config identity, coordinator PID, actual child PID, ordinal and unit identity into the receipt. The coordinator joins them against its packet and the actual Popen/wait4 child receipt. Require result references to resolve inside that child's artifacts namespace. Keep complete resource-unit identity equality. Add rejection fixtures for a different attempt directory, escaping reference, wrong PID/attempt/config, oversized receipt, and budget overrun accounting. Factor small stdlib validation helpers if that keeps the runner readable; remain within four files.

7. Actual concurrency and allocated address space must use min(planned concurrency, live ceiling, len(windows)); for nine current units report nine children / 40 GiB address-space allocation rather than claiming 15 / 64 GiB. Retain the 64-GiB ceiling separately. Jobs must have a complete unique ordinal sequence before any launch; reject duplicates or invalid ordinals without starting children. The source entry point does not launch grandchildren; do not build a generic execution framework. Existing failure/interruption tests should exercise cleanup and ensure all actually launched direct children are reaped.

8. Handle supervisor timeout/interruption with a bounded cooperative cleanup opportunity: the worker's signal handler should raise so pool cleanup can stop/reap children; the parent should first terminate the coordinator, allow a short finite grace, then kill the process group only if still alive. The normal pool wall watchdog must expire early enough to clean up before the outer watchdog. Retain timed-out/failure disposition and actual CPU evidence; do not claim all descendants were accounted if forced group kill prevented reaping. Any unavoidable accounting uncertainty must remain explicit and conservatively charged, never reported as zero. Keep this narrowly scoped to existing source-worker supervision.

Do not edit the check extension: the controller will create an additive version with your tests and the separately implemented cohort tests. Do not update Jumbo or scientific kernels. Return a concise summary of fixes and remaining concrete issues; no performance claims.
