# Exact complete cohort calculations for the validation phase

Implement the bounded kernels below in this isolated checkout. Edit ONLY:

- src/trading_research/research/auction_flow_cohorts.py (new)
- tests/test_auction_flow_cohorts.py (new)

Use as many allowed tool calls as needed. No shell, imports/tests, scans, fits, protocol/budget/evidence changes or other edited files. The supervisor performs the registered integration and real-source checks. Do not recruit workers or implement Context/Location models. Validation across all frameworks is the current phase. Another independent worker is editing the source runner/preflight; do not edit those files.

## Scientific purpose and existing reference

The current fast source path retains ordinary flow plus >=100, >=75 and inclusive 30–60 trade-size filters. The complete M02 definition also requires equal-count and equal-volume training quantile bins, the source top-35% interpretation, and continuous partition-of-unity size weights with TRUE ordered cumulative OHLC. These are measurement-definition alternatives. Preserve all fixed source filters and excluded mass; do not identify a participant from trade size.

Read the exact existing `measurements/cvd.py` CohortDefinition, CohortChannel, weights, fixed_source_cohort, `_cohort_path` and `fit_cohort_definition` algorithms; existing tests/test_measurement_flow.py; and the accepted prepared trade batch / ordered integer kernels in research/auction_flow_windows.py and auction_flow_measurements.py. Those existing modules remain unchanged. The new code is a bounded columnar mathematical path and an exact sufficient-statistic fitter. It must not weaken or fake the existing F11 fit/serving admission. Its outputs must clearly be **unpublished calculations**, with actual source/fold/fit publication to be joined by the registered study caller later.

## 1. Exact size histogram and unpublished quantile construction

Provide a streaming `TradeSizeHistogram` that consumes validated `PreparedTradeBatch` slices or explicit checked eligible integer sizes in <=65,536-row batches. Keep exact Python-integer count and quantity-weighted mass per observed size. No floating weights/histogram rounding. Track total prints and contracts; enforce an explicit bounded distinct-size capacity (default 250,000; permitted up to 1,000,000), checked integer slice/input domains, close/failure behavior, and no silent truncation. A report contains the complete sorted (size,count) histogram, totals and a deterministic identity; count×size must reconcile exactly. Merge compatible complete histograms without expanding every print, preserving membership identities and rejecting duplicated source-window evidence. Keep source/fold manifests supplied by the caller explicit; kernel state alone is not an admitted historical training sample.

Provide an unpublished quantile-fit function over that complete histogram with explicit `train_end`, `available_at >= train_end`, immutable training member identities, aggregation unit, exact increasing interior Fraction probabilities, and weighting `count` or `volume`. Reproduce the ORIGINAL rule exactly: choose the smallest observed size whose cumulative chosen mass reaches p×total, set the hard boundary to `size + 1`, merge duplicate empty boundaries, and retain requested versus realized count/volume support. Do not approximate a quantile of quantiles or split equal-size prints to force equal buckets. The returned `CohortDefinition` and recipe retain `origin='fitted'`, actual times and recipe identity; `publication_status='unpublished'`.

Quantile bins with (1/4,1/2,3/4) and the 13/20 count quantile for the top-35% SOURCE interpretation must be representable. Actual top-cohort occupancy need not equal 35% because whole-print ties are preserved; report the realized amount. Do not call any fitted threshold an intrinsic fixed source constant. Soft knots can be supplied as an explicit immutable CohortDefinition, including fixed benchmark knots; fitted-knot selection/publication remains outside this kernel assignment.

## 2. Bounded exact ordered cohort paths

Implement `CohortWindow` over one typed frozen CohortDefinition (hard channels or its exact linear soft-knot weights) with explicit raw instrument ID, half-open start/end, source delay, maximum accepted print count and one exact Python int/Fraction opening per channel. Nonzero mathematical openings require a declared opening version. Accept `add_prepared(prepared,left,right)` and a checked compatible public table route; use the existing PreparedTradeBatch without retaining caller buffers after each bounded call. Keep exact original t, source_order/source_row/source_key and unknown side. Enforce window/delay/instrument, cumulative capacity, monotone event time and STRICT original source order across batch cuts, and failure poisoning. A new window/reset has its own opening and definition identity.

For every channel retain exact open/high/low/close INCLUDING opening, first-attained high/low event timestamps AND source orders, buy/sell/unknown mass, weighted print count, nonzero-contribution print count, exclusion/partition role, count- and size-weighted occupancy, observed signed bounds and separate complete-history bounds. Unknown sign contributes mass/print weight but no signed increment. A no-print observed channel remains an empty measured path; missing history does not become quiet. Output each channel independently plus exact partition mass reconciliation and definition/source/window/availability identity. The source's aggregate whole-print ordering is known within this prepared stream; do not manufacture cross-source order or silently join acquisition variants. Explicitly report the output as unpublished mathematical calculations.

Use integer scaled numerators for exact soft paths. For each knot channel its piecewise-linear weights have at most two fixed denominators: compute a common integer denominator, obtain exact nonnegative integer weight numerators, and run cumulative signed weighted-size numerators. Weighted counts/masses are rational only at publication. Preserve original per-event weighted OHLC, not extrema of cohort closes or quantiles of aggregates. Keep Python-integer openings separate from the batch-relative array prefix so openings above 2**63 or arbitrary Fraction denominators do not overflow or truncate.

Use vectorized int64 cumsum/reductions only when a precomputed conservative bound proves EVERY numerator/product/absolute prefix fits int64 for that batch/channel. For extreme legitimate sizes/knots/denominators, fall back to exact Python-integer arithmetic for that bounded input; do not convert to float, reject a previously allowed exact domain or silently overflow. Retain deterministic first-attainment ties across batch cuts. Avoid one raw tape copy per cohort or a full history of per-print objects. Up to the existing 32 channels/knots are allowed. The common scalar CohortDefinition.weights and unchanged `_cohort_path` provide independently implemented reference calculations for tests.

## Acceptance tests

Compare complete numerical path records with unchanged literal/reference arithmetic on independent fixtures, including +10,-20,+10 (zero close, nonzero extrema), opposing cohorts cancelling, all source cutoff neighbors 29/30/60/61/74/75/99/100, hard partitions and excluded channels, exact soft knot hits/interiors/outside support, unknown sign, no-print channels, equal timestamp with strict original orders, multiple reader/window cuts, resets, large integer/Fraction openings, maximum eligible size and large relatively prime knot gaps that force Python fallback. Preserve exact quantity and weighted-count partition identities, actual high/low source order and first attainment.

For histograms, compare exact count- and volume-quantile thresholds and realized support against the existing scalar training recipe on small complete fixtures; add huge counts via merged summaries without allocating those prints, repeated thresholds/ties, empty/incomplete/duplicate source membership, future member/availability rejection, bad sizes/slices, capacity and poisoned state. Make the new test module load all necessary frozen/reference dependencies explicitly. Test public handling of larger valid tables through bounded internal slices and original mutable-buffer isolation through PreparedTradeBatch. No synthetic speed assertions. Do not execute tests in this worker.

Return the complete bounded patch and concise remaining limitations. The supervisor will integrate actual source-probe use and full validation statistics separately, preserving all family scope and prior CPU/output consumption.
