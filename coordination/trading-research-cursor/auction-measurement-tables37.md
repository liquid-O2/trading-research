# Complete reusable auction/flow observation tables
Use selected Cursor Grok 4.6 Extra High Fast. Read /workspace/planning/trading-research/START_HERE.md and RESEARCH_SPEC.md sections2/3. Latest user authorized COMPLETE Deliverable1 statistics/definitions/timing/validation across all families, then Context. Source extraction is accepted: all3589 source/day windows/167 files complete. Your bounded implementation is a necessary input table for full-cohort empirical analysis, not a family-completion claim.

Only create the two files in your isolated workspace listed below. Canonical files, tests, evidence, protocols read-only. No shell, network, imports, tests, raw scans, subprocesses or delegation. Supervisor reviews, integrates, registers and executes. Wall allowance1200seconds; return complete concise report and static concerns.
Owned new files (absent canonical baseline): src/trading_research/research/auction_flow_observation_tables.py and tests/test_auction_flow_observation_tables.py.

Read canonical /workspace/trading-research/src/trading_research/research/auction_flow_storage.py (BoundedOutputs, ParquetSeries, read_json_artifact), auction_flow_pipeline.py, auction_flow_windows.py, auction_flow_measurements.py, auction_flow_production.py receipt definitions, auction_flow_structural_encoding.py, tests for these interfaces. Read /workspace/coordination/trading-research-cursor/measurement-schema-example.json (actual complete first atomic row; larger parts are schema context only) and validation/AUCTION_FLOW_RETAINED_PRODUCTION_RECEIPTS_36.json header/one referenced receipt if useful. Exact frozen science validation/AUCTION_FLOW_STUDY_V1.md.

Implement simple reusable functions:
1. observation_rows(receipt, measured, *, receipt_reference) -> list[dict], bounded to one source/day. Validate success status, root/start/end/known-at/atomic-width/source/coordinate identity, unique instruments and unique ordered atomic coordinates; whole->atomic essential mass conservation. Receipt parameters latency_ns equals measured end->known. Preserve all instruments and every atom, including empty/partial/invalid; no dropped rows or inferred validity.
2. write_observation_unit(receipt, measured, *, receipt_reference, outputs) -> compact summary and Parquet artifact series. Use existing ParquetSeries exact structural storage (inspect API); explicit stable Arrow schema for null/empty fields and int64 clocks/counts. Batch <=65536, same table deterministic across batch sizes. Do not store all research cohort in RAM. Summary has raw/quote/trade counts, atomic rows, eligibility counts, whole mass checks, original refs, output bytes and total CPU. Keep integer sufficient statistics exact; floating derived columns explicitly float64. Fractions decoded by read_json_artifact are Fraction, but tolerate canonical {$fraction:[n,d]} in pure fixture input. Never coerce unknown to0.

Columns: root, source_path, source_metadata_sha256, source_variant, source_window_start/end, receipt_sha256, measurement_sha256, canonical_raw_values_sha256 (address-free comparison identity only), instrument_id, contract_key nullable, event_start/end, known_at_ns, atomic bin; booleans coordinate_complete, supplied_raw_coordinate_stable, source_instrument_presence, source_coverage_complete, flow_history_complete, price_history_complete, empty_observed_window, quote_coverage_complete, full_standing_window_eligible, full_pressure_transition_window_eligible. Source quality raw_rows/non_snapshot_rows/gap_rows/snapshot_rows/unknown_action_rows/invalid_trade_size_rows and clock_uncertain_intervals count (quote field if present). Preserve source_archive_intervals meaning via an explicit archive_window_complete boolean from interval containment (not merely first/last print). No new mapping between different physical sources.

Core tape columns: prints, unpriced_prints, exact first/last priced trade ticks and event_ns/known_at/source_order/source_row (nullable), observed high/low ticks/time/order, price/up/down/squared variation, adjacent priced-pair counts, maximum same-side run, same-time pairs, terminal-run-censored. For EACH exact existing cohort all, ny_ge100, london_ge75, inclusive30_through60: buy/sell/unknown/volume/prints/excluded_prints/excluded_volume/open/high/low/close/high_at/low_at/high_order/low_order, coverage_complete, observed signed bounds and true bounds nullable. Overlapping hard cohorts are NOT a partition; only all mass plus each retained vs excluded identity must reconcile. Independently check each volume=buy+sell+unknown, close=open+buy-sell, high>=max(open,close), low<=min(open,close); each cohort selected+excluded volume/count equals all. TrueOHLC carries opening zero here; do not assume future rolling composition.

Tape weighted moments: priced_volume, sum_price_volume, sum_price_squared_volume exact ints; vwap_ticks/variance_ticks_squared float derived from exact fractions, weighted price quantile05/25/50/75/95; print-size quantiles25/50/75/95 and count/volume intensity nullable. Profile summary from sparse_profile retains priced/unpriced side totals, occupied_price_rows, complete POC maximizer count/first/last (do NOT pick tie winner), max_mass_fraction, side concentration, signed concentration separately if exact meaningful. Do not implement new value-area/topology algorithms in this task; raw profiles remain referenced for the next consumer.

Quote columns: quote_or_invalidation_rows, fresh_quote_updates, pressure_transitions, OFI/price-change/same-price-size components, OFI open/high/low/close and exact extreme clocks/orders, invalid/gap/snapshot/clear counts, standing_duration_ns, duration_mean_spread/imbalance, update_mean_spread/imbalance/microprice residual, normalizedOFI sums. Retain null unavailable means and full standing/pressure masks; partially observed OFI is not promoted to complete. No BBO recovery inference.

In summary validate complete atom count per instrument against declared [start,end) grid; original atom source/instrument id and known-at exactly. Sum trade prints and volume across atoms equals whole_window and total receipt trade/volume counts across instruments; sum quote rows across atoms equals receipt counts quote_rows. Source raw counts across allocated instruments may include ownership semantics, do not invent equality if contract differs: inspect source. Per-instrument whole-window flows vs atom sums (open-relative changes, prints/mass) independently reconcile. Do not concatenate/deduplicate monthly/weekly acquisitions; receipt link remains distinct, root/date economic clustering comes later.

Focused tests with literal numbers: buy3+sell2+unknown1 givesvolume6delta1, cohort excluded mass/count, unknownbounds, trueOHLC with interior reversal not reconstructed fromclose, priced moments, multiplePOCties, zero/quiet observed vs missing, incompleteclock/span masks, null coordinates, two instruments, partial final atom, tampered joins/counts/duplicate atoms, Parquet roundtrip stable explicitschema. Test literal expected columns/denominators not merely self-parity. No candidate execution by you.

Return actualfiles, exactinterface, static scope limitations and any unresolved schema decisions. Do not claim empirical completion or start further tasks.
