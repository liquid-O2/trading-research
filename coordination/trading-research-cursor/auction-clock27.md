# Complete the retained source-clock replay disposition

Goal: remove the common source-clock blocker to complete auction/flow validation without altering original source fields or hiding actual economic events. Implement one coherent source-to-measurement change, not another optimization campaign. Read /workspace/AGENTS.md and /workspace/planning/trading-research/START_HERE.md once. Existing full science and budgets remain. Only edit the owned files below in this isolated checkout. No shell, imports, tests, scans, network, canonical edits or new frameworks. Codex will register combined verification through the existing family runner.

Scientific definition (Codex-owned, implement exactly): default clock policy remains strict. Add an explicit named policy `redundant_backward_snapshot_v1`. Under that policy a raw event with t below the running non-replay highwater may be disposed as a redundant snapshot ONLY when: action A, side N, flags exactly 168; the immediately preceding original physical row is the same instrument, action one of A/M/C, flags exactly128; its bid_px,ask_px,bid_sz,ask_sz equal the snapshot's exact original values; valid finite exact positive quarter-point noncrossed BBO and positive finite sizes below uint32 sentinel; preceding row is genuinely immediately adjacent within the same physical source, including exact batch/carry boundary addresses (no synthetic adjacency after window filtering). All other backward/null/unknown clock failures remain errors. Consecutive snapshots cannot manufacture an ordinary predecessor. No max-timestamp rewrite, sorting, recovery or dropped source rows. Do not generalize flags or allow a prior T event (it might not equal the last projected quote).

Evidence: ./clock-neighbor-records.json contains ALL original fields and exact physical coordinates for the five real failures. All five immediate predecessors are ordinary C/128 with identical BBO. Official Databento common-fields documentation says ts_event can be nonmonotonic and ts_recv is the index timestamp; the acquired eleven fields have no ts_recv. Snapshot documentation says ts_event is unchanged and snapshot ts_recv is generation time. Flags168 = LAST128+SNAPSHOT32+BAD_TS_RECV8. We assert only an explicit no-economic-change replay disposition, not receipt time, exchange sequence, snapshot generation time or repaired history.

Implementation requirements:
- CompactProjector must retain ALL original selected rows, raw hashes, original source_order/row-count and invalidation addresses. Do NOT prefilter the table before projection: its contiguous ordinal allocation refers to every raw row. A narrow optional constructor policy plus validated per-part replay mask/orders can allow highwater ordering and suppress only returned replay quote rows. Existing C++ project_part can operate on the full original table; filter its returned quotes and adjust quote count afterwards only for validated rare replays. Do not modify compact_native.py. No per-row Python loop on ordinary 65536-row batches; vectorize the normal clock check and examine only backward candidates. Keep strict default manifests/outputs unchanged if possible.
- Track terminal original row separately from chronological highwater. Across batches and source continuation, preserve exact prior physical identity + original record needed to authenticate adjacency and the non-replay clock highwater. A terminal replay cannot lower the highwater. Validate policy and all carry fields/hash; reject missing/tampered/mismatched-policy carry. No next-window future data is allowed to classify an earlier event.
- AuctionFlowStream/ObservedBatch: batch.raw stays complete with original t and original addresses. Expose the validated replay records (all original fields/addresses and predecessor identity/reason) and a temporal raw view excluding only those records. Keep all raw counters, flags, source fingerprint and manifest selected-row totals. Bound retained replay evidence/counts honestly by the existing selected-row budget; prefer stream callbacks/exact descriptors over keeping large arbitrary lists in memory. Record policy, count and reconstruction/provenance in manifest; unchanged strict path must preserve existing compatibility.
- Pipeline must pass temporal raw to TimeAtPrice and any raw consumer requiring monotone economic clocks. NativeSourceOwnership raw_count must STILL count every original snapshot in its raw event-time bin; snapshots never become ordinary ownership. Add a narrow way to record authenticated replay raw counts separately if needed. Ordinary ownership/trades/dwell/OFI/exposure/economic quote age are unchanged by disposition. Raw per-minute flags/counts continue to include every source row even if its bin precedes the previous ordinary bin. Do not duplicate ownership or raw counts. Keep original-row inspectors fed batch.raw.
- Provide optional source_clock_policy through measure_raw_window and bind it into continuation parameters. QuoteReplaySeries descriptors must retain the exact chosen policy and reconstruct it via AuctionFlowStream; pin all relevant source code as before. Default descriptor compatibility remains strict; never silently reinterpret an old accepted descriptor. Add an optional replay-record retention callback only if needed to make full evidence addressable; no mandatory extra raw-data copy is required where source addresses+all-field hashes permit exact reconstruction.
- Source counters distinguish original raw rows, projected economic quote rows, and disposed snapshot rows. The strict source alternative must remain usable and its original failures retained.

Independent tests (add into owned existing modules): literal small raw sequence ordinary quote t=100 then identical A/N/168 t=90 then ordinary quote/trade t=110; assert all raw t/fields/orders kept, counts include3, replay emits no fresh quote/OFI/trade/age reset, exact exposure/dwell unchanged vs the known no-replay economic sequence, raw native/minute snapshot count still present. Compare one batch vs split exactly before replay and adjacent-window carry where applicable. Exact highwater failure after replay (next ordinary t95) must reject. Changed BBO/size, previous T, other instrument, nonadjacent physical row/source, wrong flag/side/action, missing/tampered carry must reject. Real five retained neighbor-record sequences should classify the exact original offending addresses without rereading data. Verify raw multiplicity never collapses two genuine equal trades. Add quote descriptor policy round-trip/mismatch test and strict-default unchanged test. Do not execute tests; Codex will combine them with registered verification.

Acceptance: coherent compilable implementation of this policy across original source retention, projection, temporal consumers, native raw counts, carry and quote replay; independent tests and concise changed-files/limitations report. Complete this assignment within 1200 seconds. Return the actual implementation once complete. Do not expand to production scheduling/statistics; Codex is progressing those in parallel.

Owned files and canonical baseline SHA256:
- src/trading_research/data/compact.py: fc99e9f013bf6891153c5de5c25975037f111df4f68a9b1a961bae9e6b016008
- src/trading_research/research/auction_flow_data.py: b391fa4c33ca249a6c1488444cdd6546126099184109f90dac883fa12f3c74a1
- src/trading_research/research/auction_flow_pipeline.py: 6e6c75a2e59a2aee087f15fff9a0a0d0b3b1555f252cc0b00bec90340a97e8bb
- src/trading_research/research/auction_flow_native_bins.py: 43fbb6274c4af6c415fa728bdf74c905c1be90bb0e1aa2e73fa5921aebdcca5d
- src/trading_research/research/auction_flow_quote_replay.py: ee21338e5c1d32c801694e36121c9301133e68603f644464513ebc4e9426e14b
- tests/test_compact.py: ea8c077cdfda7762b6d2de8a44e3596c98a06797d3bba1ca6b2cafce69997c2a
- tests/test_auction_flow_data.py: f043859496f23caa797dfd3d676ae33e80eb4b5f5375d66de68479cdcffed869
- tests/test_auction_flow_pipeline.py: 4f9c4f6faeb3624998a6e479fb693cdbf3c9154e4a5305fe248904c2caadfe92
- tests/test_auction_flow_native_bins.py: f9629d654176667055bb8c7f95096b1d1e34daf5e604996f19b1b01a6d66c36c
- tests/test_auction_flow_quote_replay.py: 5d1a5459053ff97791855510d5f84b8f4f628f6239834c180b10fbe162e11b41
