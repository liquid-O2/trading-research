# Exit policies on the B0.3 executed list (P15-19), judged against E0 on the year folds

See REPORT.md section 14. `VARIANTS.md` is the fold table (`evaluate_variants.py --baseline E0`), `EXITS.json` the study summary (1,721 sessions, one NaN-quote failure), `RECORD_SAMPLE.json` one session's records. The run itself is `/workspace/.scratch/a537e734/exits-b03/rows.jsonl` (session lines with the five policies' per-family net points).
