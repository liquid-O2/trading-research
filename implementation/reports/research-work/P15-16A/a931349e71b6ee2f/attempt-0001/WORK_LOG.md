# Adapter-population full-history work log

- Run id: `1019e6c09ee54609`
- Manifest sha256: `1019e6c09ee54609e69412f852b40213b033b6b99c6ff8e03cfdea7d626dae23`
- Baseline: `B0.2-2026-09-15`
- Dates: 1742 (2020-01-01 .. 2026-09-03)
- Branches: 40
- Jobs: 69680
- Workers requested: 12
- default_workers: 12
- cgroup_worker_count: 17
- cpu quota: {'cpu_quota_us': 1785000, 'cpu_period_us': 100000, 'cpu_quota_cpus': 17.85}
- census_run_id: `20def36e065c13d7`

## Decisions

- Default --baseline B0.1 keeps the frozen BRANCH_RECORDS table and does not recompute B0/B0.1.
- --baseline B0.2 dispatches every family's scan_b02; B0 and B0.1 rows are read from run-1.0.1 and census 20def36e065c13d7.
- gb_fail_0930/overnight redirect to green_failure.scan_b02; golden_pocket redirects to green_vwap_scalp.scan_b02.
- Jobs are jobs/<date>/<coverage_id with ':' replaced by '--'>.json.gz (JOB_PATH_SCHEME=coverage_id--json.gz). Branch names may collide across families.
- cpu_quota reads cgroup v1 cpu.cfs_quota_us/period first, then v2 cpu.max.
- default_workers is floor(quota/period)-5, at least 1 (12 on this machine).
- install_write_guard is on. A cache miss fails the date.
- load_registry(..., check_software=False) because this runner sits beside frozen baseline_repairs.py.
- SIRES and REFILL-STUDY B0.2 use the R3 NativeMarketView array plane attached once per date.
- Resume skips a date with a valid completion.json. Existing job gz files are kept.
- SUMMARY.json and RUN_COMPLETE.json are written only when all 1742 dates have a valid completion.

# P15-16A round 3 work log

- Merge notes: no remaining shared-file conflict. Kept main `runner.py`. `common.peak_rss_bytes` already lazy. Family-qualified job paths already on main.
- Ruling (a): `cascade_stages` now stops on fail only. Removed SIRES gamma `session_unknown` early return. Green `_gate_stages` no longer rewrites later passes after unknown.
- Calendar guard lives in `load_source_market`. `build_market_view` stays open so P15-19 can load classified partial date 2026-09-03. After-tape dates not on the classified calendar (for example 2026-09-11) are refused before `HistoricalFeatures`.
- Verifier memos in `receipts.py`: per-state file digest cache, per-resolved-path verify memo, predecessor cache key includes receipt path.
- B0.2 run launched at `implementation/reports/research-work/P15-16A/1019e6c09ee54609`, PID 2520125, 12 workers, 1742 dates, 40 branches, 69680 jobs.
- Produce/report changes are in `tools/produce_p15_16a.py`. Receipt is not issued until `RUN_COMPLETE.json` exists.
- F1: calendar guard is ISO membership in `_classified_session_dates()` only (`is_native_session` / `require_native_session` in `common.py`). Deleted `NATIVE_CALENDAR_START`/`END` from `native.py` and `b02_saint_track.py`. Adapter tape checks share that guard. Loader test covers 2026-09-03 (accept), 2026-09-11 (refuse), 2022-01-01 (refuse) with `build_event_window` patched to raise.
- F2: `ofm_aggressive` and `balance_failure_fade` keep `contact_at_source_literal_location`. `episodes_per_session` is null (source gives no contact density). Family JSON `observed_rate_justification` cites OFM p.4 and BIG p.14. Plausibility gates assert those two branches are out of bound with that diagnosis.
- F3: serial cProfile of P15-09 `250e5aea146f3516` verify is written to `_work_r3/PROFILE_VERIFY_P15-09.txt` (top 30 cumulative, wall, first-vs-second ok and distinct triples).
- F4: nine `_repair_sires`/`_repair_saint` evidence files restored to committed bytes. Tests write generated PLAUSIBILITY/FUNNEL/REPLAY/ENTRY_TIMES under `_work_r3`. Byte-identity test covers `_repair_*` and `_track_*`.
- F5: `AUTHOR_EXAMPLES_2026-09-15.json` on main marks eight 2026-08-27..2026-09-03 examples inside tape. Replay tool is re-run; rows copy `reached_location`, `detected`, and operands. Produce does not reuse the round-2 replay artifact.
- Corrected B0.2 run root `1019e6c09ee54609` is complete: 1742 dates, 40 branches, 69680 jobs, failed_dates empty, concurrency 12, wall 7413s.

## Round 3 continuation (Opus implementer)

- Reviewed the stopped Grok session's uncommitted diff file by file. Kept: the receipts.py memo structure, the shared frozen-list calendar guard, the nine restored `_repair_*` evidence files and the test redirection, the funnel/entry-histogram aggregation in `run_adapter_populations.py`, the producer's funnel/plausibility rendering, the replay tool's `reached_location`/operand columns and market cache, ruling (a) in `cascade_stages` for SIRES and Saint.
- Replaced (F2): the Grok left `ofm_aggressive` and `balance_failure_fade` at `contact_at_source_literal_location` with `episodes_per_session: null`. A null bound cannot be in or out of anything, so nothing was reported. Reverted both to `episode_kind: session_unknown` with the source-grounded bound `[1, 1]` per session and kept the `observed_rate_justification`, so the observed contact density is reported out of bound with OFM p.4 / BIG p.14 and is not tuned.
- Replaced (receipts.py): the per-state receipt-result memo was keyed by resolved path alone. A receipt's result depends on `pending_ok` (a predecessor assumed ok during a successor search) and on `visiting` (a candidate skipped as an ancestor), so a path-only key can return an optimistic result to a later independent call. Re-keyed to `(resolved path, sorted pending_ok, sorted visiting)` via `_VerifyState.receipt_memo_key`; both are constant across one successor search, which is where the repeated verification is, so the speed-up is kept.
- Replaced (F1): the guard was in `common.load_source_market` only, and `build_market_view` stayed open "so P15-19 can load 2026-09-03". 2026-09-03 is a member of the frozen list, so that exemption had no reason to exist. Moved `is_native_session` / `require_native_session` into `native.py` (the shared market-view loader's module; `common.py` re-exports them) and put the single guard at the top of `build_market_view`, before `load_session_arrays` touches the derived cache. Deleted the residual range constants `TAPE_END`/`TAPE_START` in `sires_b02.py` and `green_b02.py` and `TAPE_LAST` in `jumbo.py`, whose two `day > TAPE_LAST` comparisons became `is_native_session(day)`.
- F4 extended: `test_p15_16a_plausibility_greenbird.py`, `test_p15_16a_jumbo.py` and `test_p15_16a_plausibility_jumbo.py` still wrote committed evidence under `_repair_greenbird/` and `_repair_jumbo/`. Redirected those writes to `_work_r3/` and added the seventeen files to the byte-identity assertion.
- GAP 1: added `prior_week_level` to B0.2 in `green_b02.py` with the `prior_day_level` sweep-and-fail template, both sides, one reference lifecycle per level per week, level objects recording period kind, scope, source calendar, week convention, high/low, period end and known_at per `wiki/prior-day-week-month-levels.md`. The shared prior-period loader supplies RTH windows only, so the scope is recorded as `rth_0930_1600` and the full-session weekly candle is a named limitation with operand `full_session_prior_week_window` rather than a silently different number. `prior_month_level` stays out of B0.2.
- GAP 2: registered `RR-16-tdo-retest` as a literal confirmation variant on `asia_box`, `prior_day_level` and `prior_week_level`. `values.reclaim_mode` always keeps `five_minute_close`, so the close-through mode is never replaced; `confirmation_mode` becomes `tdo_retest` with the retest time and price only when a held retest of the True Day Open follows the reclaim inside the registered window, and `tdo_required` follows it. The window is unstated in the source and is registered as a bounded 120 minutes. Failure reasons: `no_retest_in_window`, `retest_broke_through`, `tdo_unavailable`.
- Supplemental run: added `--branches` to `run_adapter_populations.py`. Because the manifest lists only the selected coverage ids, the run id is a different hash and no job file of the main root is touched. The supplemental root covers `prior_week_level` (added after the main run) plus `asia_box` and `prior_day_level` (whose recorded confirmation mode changed with GAP 2). The producer and the A05 gate merge the roots by coverage id, supplemental superseding main.
- 2026-09-11 is not a member of the frozen classified session list (which ends 2026-09-03), so the author example of that date has no replay row. Its tdo_retest fixture is a unit fixture built from the published numbers (AS.L 29,045, PDL 29,040, TDO 29,059.50), not a market replay.
- F3: the "before" arm is the unchanged `receipts.py` from HEAD, imported from a temporary package; before, after and a warm re-run of before are timed back to back in one detached process, with the load average recorded at both ends because the box is shared.
- Supplemental run relaunched: `run_adapter_populations.code_identity()` now carries a `scan_code_sha256` over `source_adapters/` and `families/`, because the runner's own hash does not move when a scan does, so a re-run would have reused the manifest hash, the run id and the checkpointed job files of a run produced by different scan code. The first supplemental attempt `32eb6ba2524104cb` was stopped at 1,340 of 1,742 dates and is superseded: it ran before `values.reclaim_mode` was added and before the jumbo range guard became the shared frozen-list guard. It has no RUN_COMPLETE.json, so every consumer skips it.
- The supplemental branch set is the set of branches whose scan code moved after the main root `1019e6c09ee54609` closed: `prior_week_level` (new), `asia_box` and `prior_day_level` (the tdo_retest variant), and `JJ-TBR:branch:internal_rotation` (its location stage marked `evrange=data_unavailable` from `day > TAPE_LAST` (2026-08-19); the shared guard moves that boundary to the frozen list, which changes the sessions of 2026-08-20..2026-09-03). Every other branch's job files were produced by bytes that are unchanged.
- Derived-cache leak (coordinator defect, fixed this pass). Eight empty entries (row_count 0) were written under `/workspace/data/derived/phase1-event-time-v2` between 10:13 and 10:21. Decoded, every one is an RTH window 09:30-16:00 for instrument_id 17 on 2025-11-18, 2026-05-29, 2026-07-31, 2026-08-28, 2026-09-02, 2026-09-10, 2026-09-11, 2026-09-14. Instrument 17 is the placeholder id of the synthetic `Tape` fixtures in the P15-16A unit tests, and the dates are the prior session or prior-week days of fixture dates, so the writer was not the runner, the replay tool or the summary: it was the unit tests. Any adapter path that reaches `HistoricalFeatures.prior` on a synthetic tape asks the shared cache for a window nobody has, and the cache writes an empty entry. The frozen-list guard could not have stopped six of the eight, because those dates are members of the list; the missing invariant was that no test may build a derived window at all.
- Fix: `tests/rule_discovery/conftest.py` installs the cache write guard for the whole session (autouse, session scope), the same guard `init_run` installs in the runner and `load_market` installs in the replay tool, so a cache miss raises `CacheWriteBlocked` and the adapters record a named omission instead of writing. `build_market_view` and `load_source_market` already refuse a non-member date through `require_native_session` before any build.
- New test `test_A05_every_after_tape_example_replays_without_building_a_window` monkeypatches `event_cache.build_event_window` and `event_time.build_event_window` to raise, then replays every author example whose date is outside the frozen list (inside_tape false or not a session), asserting each returns a reason and no window was built. It also pins the calendar endpoint: 2026-09-03 is in and 2026-09-11 is out.
- The eight entries were deleted after re-reading each manifest and confirming `row_count == 0`. `find /workspace/data /workspace/sources -type f -newer $SCRATCH/briefs/p15-16a-integration-opus-implementer.md` now reports 0 files.
- Supplemental roots: `32eb6ba2524104cb` (3 branches) is superseded and incomplete (stopped at 1,340 of 1,742 dates, no RUN_COMPLETE.json, so every consumer skips it). The receipt uses `823273eba4b9ec50` only: 4 branches, `GB-FAIL:branch:prior_week_level`, `GB-FAIL:branch:asia_box`, `GB-FAIL:branch:prior_day_level`, `JJ-TBR:branch:internal_rotation`.
- Receipt reissued once: the first issued attempt carried a round-2 sentence in REPORT.md limitations that still read "jobs_declared = 39 x 1742 = 67938". The corrected full-history root carries 40 branches (69,680 job files) and the supplemental root 4 more over the same dates (6,968), so the sentence now states 76,648 job files over 41 distinct coverage ids across both roots. The earlier attempt `7f7929dfcac0d7ae/attempt-0001` is preserved, as every attempt and run root is; it is superseded by the reissued one, whose run id differs because the producer bytes it pins changed.
- F3 revised on the orchestrator's instruction. The serial "before" arm I had launched (unchanged receipts.py from HEAD, imported from a temporary package) ran 5h40m at 12% CPU on a box whose load average never fell below 10, and was killed rather than allowed to block the receipt. The "before" side is now the driver's round-2 output `$SCRATCH/verify-16a-integration-r2/verify_before_P15-09.json` (unchanged module, ok false, 39 raw failures, 3702.7 s under load) with its module copy `receipts_before.py` beside it. `_work_r3/verify_after_p15_09.py` runs only the memoized verifier over the same closed attempt 250e5aea146f3516, serially, and writes the wall time, the equality of the distinct (code, path, detail) sets against that before-file, and the top 30 cumulative profile entries to `_work_r3/PROFILE_VERIFY_P15-09.txt`.
- Container OOM. The pod's memory limit is 77.3 GB (cgroup v1 `memory.limit_in_bytes`); the host's `free` is not the pod and was misleading. The cgroup OOM killer fired at 16:56:47Z and killed every Python process, including the second and third receipt-production runs (the third at 7.3 GB, 56 minutes in, during the author-example replay, which is why `produce.log` ends at "author example replay failed"). Nothing launched before 16:57Z survived; every earlier in-flight output was rechecked rather than trusted. Receipt production was restarted as a single process, with `/usr/bin/time -v` recording peak RSS, only after `cat /sys/fs/cgroup/memory/memory.usage_in_bytes` showed at least 12 GB free, and with nothing of mine running beside it.
- Code set: `tests/rule_discovery/conftest.py` is now enumerated in CODE_PATHS beside the `test_p15_16a*` modules. It installs the derived-cache write guard for the whole session, so it is part of what those tests assert and the receipt must pin its bytes; without it the fix for the empty-cache-entry defect would sit outside the code identity. The receipt was re-issued for that reason.
- Peak memory: main run root `1019e6c09ee54609` peak worker RSS 3.82 GB over 12 workers (wall 7,413 s); supplemental root `823273eba4b9ec50` peak worker RSS 1.09 GB (wall 1,674.8 s); receipt production peak child RSS 14.73 GB (wall 3,780.5 s), measured with `resource.getrusage(RUSAGE_CHILDREN).ru_maxrss` in `_work_r3/run_produce.py` and recorded in `_work_r3/produce.rss.json`.
- Verifier rejection, both defects fixed at the cause. (1) `_unpinned_owned_paths` (receipts.py:524-542) pins every regular file under an owned directory, while the producer enumerated owned directories by extension, so `families/.gitkeep` was never in CODE_SNAPSHOT. `_repo_files` now takes an optional suffix and `_owned_dir_files` enumerates owned directories with no filter (produce_p15_16a.py:88-121); both owned directories in TASK_GRAPH P15-16A `owns` (source_adapters/, families/) go through it, and `_unpinned_owned_paths` over the new CODE_PATHS returns []. (2) A06, A07, A08 and S01 carried `test_nodeids: []` with command indices 0 and 1, which are not audit commands, so `_check_evidence_matrix` (receipts.py:1533) rejected them. Each now binds to a test of its own id: `test_A06_command_exit_codes_hashes_and_coverage_reconcile`, `test_A07_evidence_matrix_resolves_every_key`, `test_A08_silent_failure_probes_are_assigned_to_their_own_ids` and `test_S01_missing_substituted_and_malformed_artifacts_are_rejected`. A08 and S03 also got their own evidence selectors instead of borrowing A06's and S01's.
- The bound tests exercise the producing code, not the issued attempt: the producer runs this suite before it writes the attempt it is issuing, so a test that reads issued attempts cannot see the receipt it supports and re-judges frozen history instead (the first attempt at this fix did exactly that and failed on the rejected attempt `8228d638a16d71bd` after 57 minutes of pytest). A06 checks that `run_cmd` records the real exit code and a log digest that matches the bytes, with a failing command as the negative control, then reconciles branches x dates against RUN_COMPLETE and the job files on disk for the main root `1019e6c09ee54609` (69,680) and the supplemental root `823273eba4b9ec50` (6,968), and against the declared `job_count_all_roots` (76,648). A07 runs the contract's own `_check_evidence_matrix` over the matrix `write_matrix` writes into a stand-in attempt and requires zero failures; its negative control clears A06's node ids and reproduces the verifier's exact rejection, `INVENTORY: passing check has no test_nodeids or audit command`. A08 takes the nine assurance cases from TASK_GRAPH, requires each row to name a test of its own id that resolves in this file, and its negative control flips S01/S03 from accepted-limit to pass only when the probes stage has run. S01 replaces a source-string assertion with three real mutations of a bound receipt (removed artifact -> ARTIFACT_MISSING, substituted bytes -> ARTIFACT_HASH, unparseable JSON re-pinned -> JSON_PARSE). `test_code_snapshot_pins_every_file_under_an_owned_directory` guards defect (1) with the extension filter as its negative control.
