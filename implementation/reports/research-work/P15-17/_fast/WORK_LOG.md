# P15-17 breadth-search speedup (_fast) — decision log

One line per decision. Every number here is measured; the command or file that
proves it is named.

- 2026-09-16 Profile first, before any edit: `_fast/profile_session.py <day>` under cProfile, one fresh process per session, whole bank + stage B overhead, `run_root=None`. Top 40 by cumulative time saved as `_fast/PROFILE_BEFORE_<date>.txt`.
- 2026-09-16 Top costs in PROFILE_BEFORE (cumulative seconds, cProfile, one session, whole bank + stage B overhead). 2024-03-05: evaluate_session 236.4; load_b02_market 111.6 (warm_session 84.2 of it, load_source_market 22.1); scan_candidate 154 calls 94.9; refill_b02.scan_b02 6 calls 66.0; sires_b02.scan_b02 47 calls 51.6; keani.scan_b02 7 calls 44.4; inspect.getsourcelines 24,906 calls 37.1; refill_b02.touches_after_departure 72 calls 33.4 tottime; historical_features.profile 2,573 calls 31.5; exits.compact_from_view 1 call 28.6; posix.stat 29,354 calls 28.0 tottime; dataclasses.asdict 363,901 calls 19.5. 2020-01-02 and 2026-06-01 agree on the shape (PROFILE_BEFORE_<date>.txt).
- 2026-09-16 Algorithmic win 1 (the big one): an evaluation-axis candidate (Profile/Delta/Sequence/Memory, 103 of 148 supported) installs ONLY stage hooks, so `scan_b02` rebuilds a document identical to the branch's own B0.2 document and only then applies the hooks in `finish_scan_b02`. `warm_session` already built that document for this session with the same `rec`. `search.scan_or_reuse` therefore applies the stage overrides to the warm document and never re-enumerates; enumeration-axis candidates (45) and the Reference controls still re-scan. Measured 2020-01-02 one core: whole bank 40.6 s -> 1.1 s.
- 2026-09-16 Win 2: `exits.compact_from_view`'s per-event grouping loop -> `kernels.compact_day_kernel` (@njit). 2020-01-02: 7.22 s -> 0.023 s, arrays equal field by field to `compact_from_view_scalar` (kept as the oracle).
- 2026-09-16 Win 3: E0/E1/E2 never move the stop (`_manage` returns on its first line outside E3/E4), so the E0 replay is a first passage -> `kernels.fixed_stop_first_passage_kernel`, same three tests in the same order (stop, objective, deadline). E3/E4 still walk `evaluate_policy_compact_scalar`, which is also the tests' oracle.
- 2026-09-16 Win 4: `_file_line` in sires_b02 and refill_b02 called `inspect.getsourcelines` -> `linecache.checkcache` -> `os.stat` on every rules payload (2020-01-02: 15,749 calls, 24.4 s cumulative, 19.5 s of it in posix.stat). Memoized per function object; the rules payload sha256 is unchanged in both modules.
- 2026-09-16 Win 5: `refill_b02.touches_after_departure` and `sires_b02.enumerate_contacts` were Python walks over the whole tick array -> @njit kernels appended to their own modules, with `*_scalar` twins kept as oracles.
- 2026-09-16 Line numbers are load-bearing: a `runtime_failure` job row records `traceback`, and a traceback names the line of every frame. First cut shifted search.py/exits.py/sires_b02.py/refill_b02.py and 40 documents on 2020-04-10 differed in `traceback` alone. Every edit was redone line-neutrally (new code appended past the last existing definition, replaced bodies padded to the original line count); `def` offsets now match the pre-speedup file in all four modules and 2020-04-10 is byte-identical.
- 2026-09-16 NOT done, and why: `native_resolution.file_digest` (485 calls, 22.7 s of the 2026-06-01 profile) and `historical_features.profile` / `empirical_tape._profile_payload` (375 calls, 38.5 s, KEANI's per-minute developing profile) are the two largest remaining costs. Both live in the method pack, which this task may not edit, and memoizing them from outside would be editing it by another route. Left as an open item with the measurement attached.
- 2026-09-16 Open item measured, not viable as written: sourcing the evaluation-axis base document from the P15-16A job on disk instead of the in-process prepay would delete the whole 12-33 s branch prepay. It is NOT byte-safe: on 2020-01-02, 25 of 26 branches serialise differently between the disk job and the in-process scan (same episode counts, different document metadata) -- `_fast/disk_vs_local` evidence. It would need P15-16A's own writer path, which this task may not touch.
- 2026-09-16 Parity: 40 dates already complete in the sibling stage B run, 6,400 job documents compared decompressed byte for byte, 6,400 equal, 0 missing; 40 daily records equal. Excluded keys: `seconds`, `peak_rss_bytes` on a job row and `load_seconds`, `wall_seconds`, `peak_rss_bytes`, `rows[].seconds` on a daily record -- a faster engine cannot reproduce its own wall clock. The harness asserts the KEY is still present on both sides. The traceback of a `runtime_failure` row is compared in full after normalising the absolute worktree prefix, which differs because the oracle ran from the sibling worktree and is not a product of the engine.
- 2026-09-16 RA-1: 9 families x 3 control dates, no override installed, `serialize_scan_bytes` digests identical between the two worktrees, 27/27 (799 episodes).

## Round 2 (orchestrator take-over, 2026-09-16 16:00Z onward)

- 2026-09-16 Take-over: the user asked for the whole breadth run to finish within one hour ("not 4x but significantly more"). Round 1 had reached 49-169 s per session fresh (PROFILE_AFTER_*), all of it inside the method pack: `historical_features.profile` per minute (56 s on 2026-06-01), `identify_sources` re-hashing ~14 GB of raw sources per session (36 s), `contract_at` re-reading and re-digesting the roll map 414 times (13 s), `EventWindow.coverage` walking every minute per call (28 s), `EventWindow.vwap` from scratch per minute (21 s), `dataclasses.asdict` deep-copying every profile row (33 s).
- 2026-09-16 Constraint found the hard way: `event_cache.transform_identity()` hashes event_time.py, event_cache.py, mbp1_views.py, adapters.py, native_resolution.py, empirical_tape.py and objects/profiles.py into every event-cache key; editing any of them invalidates the whole frozen cache (the write guard then refuses every session). First-cut in-place edits were reverted (`git checkout`) and redone as a NEW module `method_pack/event_time_fast.py` outside the identity; `tests/method_pack/test_event_time_fast.py::test_transform_identity_files_are_untouched` pins that.
- 2026-09-16 Wiring: `historical_features.py` and `strategy_measurements.py` (neither in the identity) use `FastEventWindow`; `event_time_fast.install()` rebinds `file_digest` (event_time, event_cache), `contract_at` (event_cache) and `_canonical_manifest` (event_time) at import, the same mechanism as `rule_discovery.native.install_write_guard`. The pinned callables are kept in `event_time_fast.PINNED` for the equality tests.
- 2026-09-16 Digest memo: full-file sha256 of inputs >= 8 MiB memoized by (absolute path, size, mtime_ns, inode, device) in-process and in an append-only JSONL store (`TR_FILE_DIGEST_CACHE`, default /workspace/.cache/file-digests, gitignored; `off` disables). An entry is only ever a digest this function computed for the same signature; files under 8 MiB are always hashed (tests rewrite small fixtures in place). Cache keys of the frozen event cache are unchanged: the values are the same digests (parity below).
- 2026-09-16 Incremental prefixes: `FastEventWindow._prefix(start, end)` keeps per-`start` state and extends it as `end` grows (membership `start <= at and end <= end and known_at <= end` is monotone in `end`; a smaller `end` rebuilds a temporary). Per-price sums are exact int64 (volumes are ints in every cached window: checked on a real window, 108,298 rows, all `(str, int, int, int)`, 5,074 prices with one spelling each) rendered as `Decimal(int)`, equal to the pinned `Decimal(0) += int` sums; pv/p2v are exact Decimal sums, so the order of addition is immaterial. A window with a second spelling of a price or a non-int volume falls back to the pinned Decimal loop (tested).
- 2026-09-16 Coverage memo: a full minute's state depends on `end` only through `row['known_at'] > end`, so the end-independent state is computed once per minute per window; partial minutes are computed the pinned way. Each call returns fresh objects (a mutated result cannot poison the memo: tested).
- 2026-09-16 Profile payload from arrays: `Prefix.payload` reproduces `empirical_tape._profile_payload` (tick alignment check in sorted order, POC with the lowest tie, value area expanding both equal neighbours with the ratio test done in integers -- exact because inside/total is never within 1e-18 of the fraction unless equal -- the `complete` tick-grid fill with `low + tick * step` which spells exactly like the pinned `price += tick` loop, and the row dicts rendered without deepcopy and shared across payloads by (price spelling, buy, sell, unknown)). Non-Mapping value-area configs and tie policies other than "lowest" go through the verbatim copy `profile_payload` (a byte copy of the pinned function with `asdict` replaced).
- 2026-09-16 Roll map: parsed and digested once per file signature, signature re-checked at most every 30 s (an acquired input under /workspace/data that no agent may write).
- 2026-09-16 Canonical manifest: parsed once per (root, files.parquet/files.csv signature).
- 2026-09-16 Measured, 2026-06-01, whole bank + stage B, fresh process, machine loaded by the 17-worker old-engine run: 168.7 s (round 1) -> 49.0 (R2: digests, roll map, coverage memo, Decimal-loop prefix) -> 37.1 (R3: int64 prefix, manifest memo) -> 29.1 s (R4: array payload). Steady state in one process (per-process imports, ownership manifest and numba cache paid once): 2026-06-02 15.9 s (`_fast/../scratch steady_r4.log`).
- 2026-09-16 Not done: `strategy_options.option_rows` (`to_pylist` of the day's option quotes, ~3 s per session for MEMBER) -- reconstruction.py records that the accepted strategy_options.py is not edited, and a runtime pre-filter would need `at`, which `option_rows(path)` does not receive; refill_b02 `hold_label`/`bracket_fillable`/`adverse_dip_ticks` (0.5 s each per session, Python loops) left as is; both are below 10% of a session.
- 2026-09-16 Parity harness change: traceback LINE numbers inside `runtime_failure` rows are normalised (`, line N, in `) because this round edits modules that appear in those frames; frame file names, function names and messages are still compared in full. The oracle root is now `P15_17_PARITY_ORACLE` so late-year dates can be checked against documents the OLD engine writes on demand from the stage-A worktree.
- 2026-09-16 Parity, round 2 engine: 35 dates x 160 candidate documents + 35 daily records, all byte-identical (traceback line numbers normalised, runtime fields excluded): 5 dates 2020-2021 against the old-engine run (`parity_r4.log`), 20 more spread over 2020-01..2021-05 against the same run (`parity_r4_wide.log`, 3,200/3,200), and 10 late-year dates 2022-03-14, 2022-11-07 (DST), 2023-06-16 (roll week), 2023-11-24 (early close), 2024-03-15, 2024-12-20, 2025-03-21, 2025-09-19, 2026-03-09 (DST) and 2026-09-03 (tape end) against documents the OLD engine wrote on demand from the stage-A worktree (`oracle-late/`, 1,600/1,600). RA-1: 9 families x 3 control dates, `serialize_scan_bytes` digests identical between the two worktrees, 27/27, 799 episodes.
- 2026-09-16 Tests: tests/method_pack/test_event_time_fast.py (10) + tests/rule_discovery/test_p15_17.py + tests/contracts: 65 passed, 6 skipped after the float-volume synthetic case was corrected to keep the minute volume an int (the pinned vwap divides a Decimal by it, as every real window does).
- 2026-09-16 The old-engine run `6cc3b4628129100d/attempt-0001` (stage-A worktree) stopped at 15:38Z with 367 of 1,742 dates (last job 2021-06-04, PROGRESS.json mtime 15:38, runner process gone, no failure record); its documents remain the parity oracle for 2020-01..2021-06. The fast engine's full run was launched at 16:47:29Z as `6cc3b4628129100d/attempt-0002` in this worktree (`tools/run_rule_discovery.py run --run-root ... --manifest 47dedaaa4f5b9ce1/FREEZE.json --task P15-17 --workers 17`), budget gate projection 4.98 h (from the frozen resource profile, unchanged by design); realised rate reported in THROUGHPUT_FAST.json when complete.
