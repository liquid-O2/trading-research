# Handoff prompt (paste everything below the line into a new session started in /workspace)

---

You are taking over an unfinished job in the repository `liquid-O2/trading-research` at `/workspace`. Read `/workspace/AGENTS.md` first and follow it. Then read the memory index the harness loads (`MEMORY.md`) and these memory files in full: `no-candidate-inflation.md`, `sires-is-a-combined-method.md`, `selection-and-context-direction.md`, `source-passes-2026-09-18.md`, `fidelity-audit-state.md` (long; its last ten bullets are the current state), `runpod-container-limits.md`.

## The job, in the owner's words

Implement the traders' BASE strategies faithfully on NQ futures: Jumbo and Green Bird first, then Sires (with Saint, Member, Keani as worked examples of his combined method). No upgrades, no exit work (exits, stops and targets are parked; one simple baseline exit only; entries are judged on MAE/MFE). The owner has waited two days and is out of patience. Do not report progress as "done" and do not come back with excuses; come back with the three numbers below or with a specific blocker.

## What "done" means for a strategy (the owner's bar, not negotiable)

1. SETUPS A DAY on the list the strategy would actually TRADE (the executed list): about what the author trades, two or three, five at most. "85 a day is insane, 28 is insane too." Never raise ticket recall by lifting caps, gates or cycle limits.
2. EVERY dated ticket of the author on that traded list, within ten points on the right bar (decision-scored tickets by their bar). Green Bird's own documented mistake (GB-2026-04-28 09:45) is the only accepted exception. "26/30 and 17/19 isn't acceptable."
3. The FAKE-TICKET rate on the same traded list beside it (tickets moved 40 minutes either way on their own day and side, priced at the market). A real rate is evidence only beside its fake rate.
4. The selection must be ADAPTIVE: "his policies are not the same for every day, for every regime, and for every condition." The day-and-moment read (day type, overnight behaviour and size, range size, what is already swept, news tier, volatility, time window) decides which play, clock and level are live today. It comes from the author's own stated conditions in the sources, not from a fit to thirty tickets.
5. Context comes from the SOURCES, page by page, not from the owner's examples. Every read carries the time it is known; nothing may use information after the decision (no fill at a bar's extreme, no gate on later bars, no stop from later bars, no feature known later than the row). A result that looks too good is a look-ahead or a scoring bug until proven otherwise (this session found six such defects, the last one a target on the wrong side of the entry scored as an instant win).

## Where the work is

- ONE working tree: `/workspace/.worktrees/p15-next`, branch `phase15/next`, commit `4d15b024`. (`/workspace/.worktrees/phase15` on `phase15/objective` is behind it; fast-forward it when green, then main.) Python: `/workspace/implementation/.venv/bin/python`. Run everything from `/workspace/.worktrees/p15-next/implementation` with `PYTHONPATH=src OMP_NUM_THREADS=2` (without PYTHONPATH you test another checkout). Scratch: `X=/workspace/.scratch/a537e734`. Never write under `/workspace/data` or `/workspace/sources`. One ticket replay needs up to 14 GB; the cgroup limit is 83 GB; run heavy replays one at a time. Background shells start in `/workspace`: use absolute paths; kill by explicit PID.
- READ FIRST, in full: `$X/context-pass/JUMBO_CONTEXT_PASS.md`, `$X/context-pass/GREENBIRD_CONTEXT_PASS.md`, `$X/context-pass/SIRES_COMBINED_METHOD.md` (page-by-page source passes with quotes, tape checks, the adapters' misreads with line numbers, each author's own frequency and selection statements). Also `planning/phase-1-5/CONTEXT_INVENTORY_2026-09-18.md` and `planning/phase-1-5/COMPLETION_STATUS_2026-09-18.md`. Page-marked text of all 35 discretionary PDFs is in `$X/src-text/`.
- Adapters: `implementation/src/trading_research/research/rule_discovery/source_adapters/` (`jumbo.py`, `jumbo_context.py`, `green_b02.py`, `session_levels.py`, shared selector `trade_selection.py`). Tickets: `planning/phase-1-5/AUTHOR_EXAMPLES_2026-09-17.json` (records corrected this session keep the first-recorded value and the evidence beside it; do the same).

## Tools that already exist (use them; do not rebuild them)

- `tools/policy_lab.py`: scans each ticket day ONCE (cache under `$X/lab/cache`, keyed by the adapter files' content) and then measures any traded-list policy in seconds: trades a day, tickets on the traded list, fake tickets on it. Example: `tools/policy_lab.py --family GB --cache $X/lab/cache --out $X/lab/out.json --fake $X/lab/fake_40.json,$X/lab/fake_-40.json --show "name" --policies '{"name": {"green_b02.CONSTANT": value}}'`. Policies are module constants of `green_b02`, `jumbo`, `trade_selection`.
- `$X/gb_inspect.py` (run with PYTHONPATH=src from `implementation`): for every Green Bird ticket, the same-side opportunities of its clock in time order and which one is HIS. Write the Jumbo twin.
- `select_session_trades(..., windows=[(start, end, cap)], objective_ends_session=, earliest_fill=, trace=[])`: per-clock caps, lock-out, earliest-fill choice, and a trace of why each candidate was not traded. It refuses wrong-side stops and targets and keeps coincident levels on a trade as its `confluences`.
- `tools/replay_jj_gb.py` (full ticket replay, `--only`, `--examples`, `--overrides`), `tools/placebo_examples_jj_gb.py --shift N`, `tools/fidelity_guard.py --out DIR --placebo [--pin]` (pins every ticket flag AND the fake-ticket rate; refuses a pin without placebo; the current pin is the older 0918e run and must be rewritten only from an accepted run), `tools/day_context_features.py` (day and moment reads with known-at times), `tools/build_grading_dataset_v2.py` (ticket versus candidates with features; Jumbo's is built at `$X/jj-grading-ctx/rows.jsonl`), `tools/fit_selection.py`, `tools/run_jj_gb_population.py`.

## The measured state (2026-09-18 night)

- Traded list today: Green Bird median 5.5 trades a ticket day, 4 to 6 of 19 tickets, fake rate 0.03 to 0.07. Jumbo median 4 a day, 13 of 30 tickets (fake rate not yet measured on the traded list). Both are far from the bar.
- The scanners DO find his trades: on 11 of 19 Green Bird tickets his trade is the first, second or third same-side opportunity of its clock. The misses come from SELECTION: (a) the list takes whatever fires first on either side, and that early mechanical trade then holds the position or locks the day out before his trade (2026-08-28: a 09:31 long paid and the lock-out blocked his 10:05 short); (b) Green Bird's day read names his play on 3 of 19 tickets (it says `asia_fade` nearly every day), so there is no working day-level selection for him; Jumbo's read names his play on 20 of 30 (20 of 25 outside London); (c) failures are detected on one-minute bars against a dozen references, so something fires within minutes of every clock opening, while Green Bird's stated trigger is the five-minute close back through the level and Jumbo waits for the 09:40 to 09:50 window and a signature (9 of his 16 morning tickets are 09:38 to 09:55; the list enters at 09:02 to 09:06 and 09:31 to 09:35 and at 02:02 in London).
- Fixed and committed this session: Green Bird's prior-day lines (Globex day to 17:00), prior-week lines (full weekly candle), weekly gap alive all week, session lines live through New York, bias recorded not gated, two ticket records re-dated with tape evidence; Jumbo's value-area layer of the range (06:00 to 09:30, reproduces his printed lines to the tick), London box closes 02:00 and is under an hour long from 2026, SessionStat minimum average, absorption without its average is unknown, news-week table and NQ versus ES reads, day reads with known-at times; the selector and scoring fixes listed under Tools.
- Known failing test: `tests/rule_discovery/test_p15_16a_plausibility_saint.py` (Member: pass rates 0.73 to 0.80 outside the bound, confirmation never fails). Saint's population was implausible because of wrong-side targets; that defect is fixed, the population is not re-run.

## What to do, in order

1. GREEN BIRD. Build his adaptive selection from the source pass (section A rows 17 to 31, section B): per clock, which references are live and which play the day calls for (sweep-and-fail day: the 9 to 10 box edge after 10:00 with the five-minute close; continuation day: the pullback into discount, the pocket, VWAP; the open: the 09:30 manipulation or a daily or weekly line; overnight only as his overnight shift; the afternoon only when the same failure appears), confluence first ("More confluence = higher rating"), one setup one strike, lock-out after HIS trade pays. Replace or repair `session_read`'s primary play so it is a running read of five-minute closes at the edges. Iterate in the policy lab until all 18 tickets are on a traded list of five or fewer a day, then measure the fake rate, then the density on 60 sessions spread over 2021 to 2026.
2. JUMBO. Same method: the open cycle and the 09:40 to 09:50 reversal window as separate one-trade clocks, signature entries where he waits for them, pre-open trades only for the documented pre-open plays, London not at the box close, the day read choosing the play (it is right on 20 of 25), the remaining misreads of the Jumbo pass section C, the missing context of its section B.
3. Run `fidelity_guard.py --placebo`, pin only an accepted run, run the unit and native tests, fast-forward `phase15/objective` and main, push.
4. Then Sires as ONE combined method following `SIRES_COMBINED_METHOD.md` section 3 (map frozen at 09:25, leniency side and objective per level, gamma regime picks the layer, arrival at a marked level, opposition fails, own side paid, retest entry, structural stop, graded reads), two to four setups a session; repair Saint and Member per its section 4; re-run their populations.

## Working rules the owner enforces

Report honestly and briefly; never say a strategy is done without the three numbers on the traded list. Keep a heartbeat if the owner runs `/loop`. Commits to the branch are fine; pushing main is authorized after the guard passes. If you delegate to a subagent, put every instruction in its initial brief: a subagent treats later messages from you as untrusted text and ignores them.
