# Phase 1 measurement specification

Status: proposed implementation contract, derived from the completed wiki. No command, schema migration or application code has been implemented in this session. The [PRD](PRD.md) defines the outcome; mechanism pages own source definitions and named variants. This spec supplies only the shared seam needed to compare them. The later [closed user rules](QUESTIONS_RESOLVED.md) and the variant grids below supersede earlier Q01–Q23 definition-blocker wording, including references in the unchanged wiki, PRD, tickets and PHASE.md.

## One interface

The proposed `phase1 measure` entry point accepts either `--slice NN` or `--all`, with `--verify`. A slice performs its definition/coverage/computation/report path. It may reuse evidence whose input, definition, code and population fingerprints still match. `--all` verifies the dependency closure and all required family/variant reports. No service, plugin framework, worker fleet or dashboard is required. Choose concrete code locations from the existing checkout during the later execution task.

The command always prints this header and one row per variant, including blocked/deferred rows:

```text
family | variant | n | faithful disagreements | experiment status | report path
```

`family` is Jumbo, day-type, auction-order-flow, CVD-SMT, volatility or options-data. `variant` includes mechanism identity and definition version. `n` is the mature eligible outcome unit specified in that report, never an unlabeled mix of days, zones and touches. `faithful disagreements` counts unexplained discrepancies against the independent expected-result evidence. `report path` identifies a real generated report for that row. Planned report location: `reports/phase-1/<slice>/<variant>.md`, with adjacent structured measured records only as needed. These report files are future execution outputs, not created by this planning task.

## Minimal records and ownership

| Record | Required fields / ownership |
|---|---|
| Definition / variant | mechanism_id, family, source path/page/post/line, source-rule or user-hypothesis or experiment, formula/parameters, units, clock/bar construction, required inputs, definition version, closed resolution IDs, variant-grid version, disposition |
| Population | instrument/contract, calendar/day boundary, observed start/end, complete/partial/missing sessions, warmup, eligible rule, source vintage, family-native and matched-overlap membership, discovery/confirmation assignment |
| Object / version | stable object_id, parent/version, family/variant, native and optional mapped price, anchor/bounds, creation/origin time, known_at, confirmed_at, invalidated_at, supersession, state, input lineage |
| Feature snapshot | object/session ID, observation time, feature cutoff, values/units, source-known times, missing/unknown flags, definition version; all inputs must be available by cutoff |
| Event / path | event_id, object/version, approach, trigger/touch/break times, rearm state, source horizon, first-passage offsets, ordered outcomes, max excursion, censor time/reason, same-bar ambiguity and eligible denominator |
| Label | source/experimental taxonomy, label-known time, primitive evidence, unmatched/unresolved state, label version; completed-day labels remain outcomes |
| Comparison | faithful and named variant IDs, population/metric/unit, eligible sessions, event/touch n, estimates/uncertainty, paired differences, years/conditions, density/width/start-distance controls, source disagreements, experimental conclusion |
| Evidence | input and definition fingerprints, planned variant set frozen before confirmation, independent examples, measured report paths, coverage exceptions and exact command/result |

The producing slice owns its definition and records. Later slices consume them by identity and as-of time; they do not duplicate or rewrite an earlier producer’s rules. Raw rows remain immutable. A shared helper is justified only when the next slice needs it; do not build all producer layers before the first report.

## Time, source fidelity and event order

Follow [the measurement contract](wiki/measurement-contract.md) and each mechanism’s source rule. A source’s backdrawn origin is not first availability. Completed HTF values, final profile shape, future no-retest, current-day total volume and next-day OI must not become earlier features. Completed/current/developing states have separate versions. Stop using a future-informed source display as a predictor; preserve its literal formula and disagreement evidence under a diagnostic identity.

OHLC can establish that both sides traded without establishing order. Retain unknown order or explicit bounds. Trade-level ordering is used only where observed; no invented path, high-first tie default or hindsight fill. Bars formed from activity remain within the frozen clock definition, with boundary handling specified. Different data sources cannot double-count the same executions.

[Q01](QUESTIONS.md) is Closed — variant grid. Use the shared grid below for all family comparisons. Source-explicit signatures retain separate names and source-event counts, alongside the same common outcome grid. Keep n for sessions, created objects, eligible triggers and mature touches separately. Nonreturning breaks, unconfirmed absorption, untouched nodes, dojis, equality and no-break sessions remain visible.

## Populations and causal inputs

| Family/input | Honest starting evidence, subject to actual later coverage query |
|---|---|
| Minute price/range | NQ/ES/YM from 2010-09; RTY observed2017-07. Do not trust earlier partition labels. |
| NQ order flow/BBO | NQ MBP-1 observed2020-01-01T23:00 onward. Separate NQ tape starts2021-09; use each schema’s own real coverage. |
| Sister futures | ES/YM/RTY trades from 2020; ES MBP-1 is partial. Keep contract/roll identity and missingness. |
| Options | CME futures-options definitions/stats/trades/OHLC from 2020; no acquired option MBP-1 established. Theta daily2016/NDXP2018, scoped intraday2020; exact DTE/strike limits remain in the wiki. |
| Cash/ETF/volatility | QQQ/SPY minute from 2018; NDX/SPX cash daily. Volatility indices and VX curve are daily unless acquisition proves otherwise. |

The [inventory page](wiki/inventory-and-availability.md) and [option scope](wiki/options-chain-availability.md) own exact dates and limits. Source extrema are not a gap-free audit. Later slices run the cheapest decisive real-data query for their coverage. Requested pulls, empty September markers and superseded vendor overlaps do not establish data. Roll maps/release times must be valid as of use; do not select a pre-open contract from that day’s completed volume.

## Discovery, experiments and confirmation

Use the longest honest sample per family. Pre2024 history is discovery where available. Reserve2024 through each family’s last complete2026 observation for frozen confirmation. A family starting later cannot invent earlier training history; use source-fixed definitions or an explicitly reserved chronological split and report that limitation. Never force all families onto the narrowest common options sample.

Before confirmation, freeze source variants, named experiments, finite parameter grids, outcome definitions, population rules and uncertainty method in the report’s evidence record. Fixed-grid clock experiments use the wiki’s proposed15-minute neighbors within60minutes, with an explicit distance penalty toward the source clock; preserve the published clock as the benchmark. The exact penalty strength is a discovery-only comparison parameter, not a daily learned box. Activity-bar thresholds use past information. Test named changes and a small predeclared set of combinations, not an unbounded Cartesian search.

Compute descriptive means/quantiles, event rates, time-to-event and conditional tables. HAR means lagged1/5/22 RV inputs; no forecast is trained. Experimental day/state labelers are deterministic definitions. No fitted Context classifier, learned location/reversal selector, HMM, touch grader, performance-selected supertrend, or trade/P&L optimization runs in Phase 1.

Report full native samples and matched overlaps. Use session-clustered95% intervals and paired session differences where applicable; show each year and support, dependence and multiple-comparison limits. Small/nonsignificant/worse results are measured results. A winner claim requires the frozen metric comparison and stability evidence; it cannot use confirmation to retune. Density/width/age/starting distance accompany level and node comparisons.

## Verification and pass semantics

Each ticket starts with an independent expected example, then computes real discovery data and a report. Include decisive cases for its named risk: ambiguous order, no event, incomplete horizon, unreleased input, invalidated object or source-formula discrepancy. Do not write broad tests that merely mirror code. Before the confirmation pass, show the frozen choices and independent expected results. Reuse accepted results while their fingerprints remain valid.

Status values distinguish `measured-better`, `measured-null`, `measured-worse`, `measured-inconclusive-support`, `blocked-data`, `deferred-P2`, `deferred-P3`, and `unidentifiable-MBP1`. Definition status is Closed — user lock or Closed — variant grid. Benchmark rows identify their source or disclosed-approximation status separately. Reports never label a planned or skipped computation as measured.

Successful verification requires complete mandatory rows, valid inputs/time/denominators, no unexplained faithful disagreement and all named in-scope comparisons measured on their declared sample. It does not require an improvement or matching a source claim percentage. Explicit wiki deferrals for later models and unidentifiable all-depth/identity claims are valid dispositions. An unsupported auxiliary source such as VOLI may retain a documented unavailable benchmark and a named available proxy; it cannot silently satisfy a required acquired-input comparison. The P-zone benchmark is the disclosed approximation. Implement the named CVD/node variants below; Q01–Q23 do not prevent a ticket or the global pass. Missing mandatory measurement evidence still prevents global success.

The all-family command must exit nonzero for required data failures, stale evidence, omission, leakage or an unexplained error against the declared benchmark. On failure, preserve the exact exception/result, input identities and evidence; reassess before another repair. No automatic retry/repair loop. There is no wall-clock kill. The accepted pass command is the scientific stop rule, with the source/raw-data/archive invariants intact.

## Explicit exclusions

No application code is written now. No raw sources/data/archive changes, bundled-code execution, workers, trades, purchases or excluded-product/contradiction-lint review. Later implementation uses Grok through Cursor under the selected project model and execution rules, one smallest complete slice at a time. Phase 2/3 scope remains solely [the one-page handoff](wiki/deferred-context-location.md).

## Closed-definition variant grids

This section implements the latest user closure. Values introduced here are disclosed measurement variants, not asserted unpublished source internals. Preserve explicit source constructions, then measure the named interpretations and upgrades. Do not add question rows or definition-blocked ticket states. Freeze the selected finite construction grid before confirmation. Use the same outcome grid below for every family; retain actual code/data dependencies and missing-observation flags.

### Shared outcome grid — Q01

For a point, set lower=upper=the level; for a zone use its known-at bounds. Record approach side at first arrival. Distances below are native price ticks. The same named profiles and horizons apply to every family; report instrument/tick size and zone width.

| Profile | Touch tolerance | Break distance beyond far edge | Rejection distance beyond approach-side edge | Rearm departure on approach side | Break confirmation |
|---|---:|---:|---:|---:|---|
| G1 exact | 0 | 1 | 2 | 2 | Observed trade |
| G2 buffered | 1 | 2 | 4 | 4 | Observed trade |
| G3 sustained | 1 | 4 | 8 | 8 | One completed 1-minute close |

Evaluate each profile at 1, 5, 15, 30 and 60 minutes after touch, plus the applicable session end: 18 common profile/horizon variants. Touch means first observed entry into the tolerance-expanded bounds from the recorded approach side. Break means the declared confirmation reaches the far-edge threshold. Reject means the approach-side reward threshold is reached before that break. Hold means no confirmed break through the complete horizon; hold and rejection may overlap and are reported separately. Acceptance is the G3 confirmed break followed by one or two complete 1-minute closes beyond its break threshold, named `accept-1` and `accept-2`.

Rearm requires the declared departure, then a fresh entry; a continuous dwell is one touch. Retain first-touch and rearmed-touch populations separately. A gap over the entire zone is `gap-through`, not an observed touch. If OHLC cannot establish first-passage order, record order-unknown rather than choosing a winner. Incomplete horizons are censored, not holds. Source-specific reaction claims receive their own event labels in addition to this common comparison grid.

### Widths, range coordinates and P-zone approximation — Q02, Q04, Q05

`W69 = high(06:00–09:00 NY) − low(06:00–09:00 NY)` and `WpriorRTH = prior RTH high − prior RTH low`, with prior RTH 09:30–16:00 NY. These are the locked width-table inputs. Store both in points. The named `prior-RTH-width-ratio` variant is W69/WpriorRTH; a zero denominator is unavailable. Report its bins [0,.3), [.3,.5), [.5,.8), [.8,1.2), [1.2,∞), and an `upper-inclusive-boundaries` variant. Ratio bins are not relabeled as the source’s price-percentage claims.

All 1.33/1.66 range extensions use W69. Keep two explicit coordinate variants: `range-origin` has upper=L69+k·W69 and lower=H69−k·W69; `beyond-edge` has upper=H69+k·W69 and lower=L69−k·W69, for k in {1.33,1.66}. These are extensions, never P-zones.

**P-zone benchmark: `disclosed-distance-approximation`.** The original formula is unavailable and is not a pass dependency. At 09:00 NY use the completed 6–9 midpoint as anchor. From the last 500 available completed 09–12 sessions, calculate nonnegative upper and lower excursions from each session’s corresponding anchor. Freeze nearest-rank percentile boundaries 50/75/90/95/99 at creation. On each side, adjacent boundaries form four distance bands; log the actual history count and warmup. These are disclosed approximation tiers, not a claim to recover original T1–T4. Source support is summarized in [the existing P-zone page](wiki/jumbo-p-zones.md).

Measure named upgrades: `history-60` versus `history-500`; `09:30-open-anchor` with a 09:30–12 historical horizon; `vol-scaled` distances using current available 6–9 GK/RV scale divided by the historical median of that same available scale; `similar-vol-tercile` historical filtering; `formation-volume-filter` above the prior-60 median versus no filter; and `VP-node-snap` to the nearest already-known node within one current band width versus no snapping. Historical filter thresholds use past data. Missing scale/profile inputs mark that variant’s availability, not a new question. Compare no pre-touch retirement versus retirement on a completed 1-minute close beyond the band’s far edge; all outcomes still use G1–G3. No learned Location selector is introduced.

### EV range and open-location switch

[EV range](wiki/ev-range-expected-move.md) and the [open-location switch](wiki/jumbo-day-classes.md#2026-open-location-switch) are required slice 51 records and `--all` report rows. They extend the existing records; the command and printed header stay unchanged. Reuse range, value and class records from the dependency closure of tickets 01 and 09. EV approximations have their own mechanism identity, separate from P-zones, extensions and SessionStat.

| Record extension | Matching columns |
|---|---|
| EV construction | ev_object_id, ev_variant, anchor_time/price, ev_known_at, AM_start/end, lookback_n, history_cutoff, estimator/quantile, scale_kind/value/known_at, ev_lower/upper, ev_midpoint, eq69_object_id/price |
| Open-location snapshot | rth_open_time/price, prior_RTH_high/low, prior_VAH/VAL, profile_variant/known_at, H69/L69/W69, WpriorRTH, overnight_balance_variant/value, open_vs_value, open_vs_range, open_vs_69, open_location_cell, switch_variant/state/known_at, rvol_variant/value/known_at, rvol_above_average |
| EV reaction | ev_side, first_touch_at, time_to_touch, tag_vs_prior_value/range, target_id, target_return_at, overshoot_ticks, common_grid_id, reject/hold/break, censor_reason, order_unknown |
| Conditional path comparison | formation_clock, outcome_start/end, open_location_cell, conditioning_cutoff, eligible_n, high_only_n, low_only_n, both_n, neither_n, break_order, OR_reference_minutes/known_at, A_direction, OR_edge_return, mature_n, claim_id |

**Disclosed EV grid.** For `ev-am-mean-60`, create the envelope at the first observed 09:00 NY price A. From each of the last 60 complete 09:00–12:00 sessions use that session's own 09:00 anchor A_i and excursions u_i=max(H_i−A_i,0), d_i=max(A_i−L_i,0). Freeze upper=A+mean(u), lower=A−mean(d); insufficient history is warmup. Compare `ev-am-q50/q75/q90-60` using nearest-rank side-specific quantiles. These are disclosed approximations, not recovered Jumbo internals.

For `ev-prior-RV/GK/YZ-scaled`, normalize each historical excursion by its preceding complete RTH's volatility in price units, then multiply the mean normalized excursion by the current preceding complete RTH's scale. RV is sqrt(sum of squared 1-minute log returns); GK is sqrt(max(0,0.5·log(H/L)^2−(2·log(2)−1)·log(C/O)^2)); YZ is the existing 20-session Yang–Zhang daily scale through the prior RTH. Multiply each dimensionless scale by its corresponding AM anchor price. Zero/missing scales are unavailable. Reuse existing volatility definitions/evidence when present; these named comparisons do not add blocking tickets. Keep ev_midpoint=(upper+lower)/2 and eq69=(H69+L69)/2 as distinct return targets. All variants use G1–G3 and the shared horizons, plus the 12:00 AM endpoint; no break-even or P&L model is added.

**Disclosed switch grid.** At 09:30, independently classify the open as below/inside/above/on-boundary/unavailable against prior RTH value, prior RTH range and completed 6–9. Use strict inside with boundary separate. `open-location-only` labels inside both `mean-reversion-hypothesis`, outside both on the same side `continuation-hypothesis`, and all other combinations mixed/boundary/unavailable. `outside-aligned-volume` additionally requires the open beyond the 6–9 edge on that side and RVOL>1. The pre-open RVOL variant uses 09:00–09:30 volume divided by the mean for the same clock over the prior 60 complete sessions. A separate developing RTH RVOL variant uses 09:30-to-cutoff volume against the same elapsed clock over those sessions; its 09:35/09:45 snapshots only condition subsequent paths, never the whole earlier A-period. Store the continuous values and eligibility even when a hypothesis flag fails.

Report in-value and in-range double-break expectancy separately and jointly, preserving all cells, width/balance strata and source horizons. Reuse ticket 06's named 05:00–09:00 versus 06:00–09:00 comparison on matched dates; the locked W69/WpriorRTH inputs remain unchanged. For the July 9 76% claim, report A-period 09:30–10:00 open-recross and directional-close outcomes separately from return to a frozen 5m/15m OR edge after its 09:35/09:45 availability. The source long example concerns the OR low; the short/high mirror is a named experiment. Post-OR outcomes cannot be described as observed no-return for the entire A-period. Keep RVOL-conditioned denominators separate; neither 76% nor a high small-range double-break rate is a pass target or trading edge.

### Session-fail boxes and fail-back

[Session-fail boxes](wiki/session-fail-boxes.md) extends the existing records and outcome tables. Ticket 06 owns completed boxes and reference/location records. Ticket 09 owns the common sweep/fail-back versus hold-outside labeler. Ticket 51 consumes those records beside EV range and the open-location switch. Existing dependencies and command shapes stay unchanged. These are required comparisons under `--all`, with no new question or ticket.

| Record extension | Columns |
|---|---|
| Box / line provenance | object_id, parent_object_id, variant_id, source_file/section, source_rule_tag, clock_tag, parameter_origin, timezone, formation_start/end, known_at, eligible_after, high/low, reference_price, reference_role |
| References / confluence | prior_day_variant/id, TDO_price/known_at, cash_open_price/known_at, Friday_settlement/known_at, Sunday_open/known_at, NWOG_lower/upper, swept_reference_ids/times, destination_id |
| Golden-pocket location | impulse_parent_id, impulse_start/end_price/time, impulse_known_at, orientation_variant/value, pocket_lower/upper, pocket_contact_at, location_known_at |
| Sweep / confirmation | sweep_event_id, swept_edge_or_line_id, approach_side, sweep_at, sweep_depth_ticks, return_at, confirmation_variant, confirmation_reference_id, close5_bar_start/end/value, fail_back_at, hold_outside_until, state_known_at, a_plus_sweep_label |
| Path comparison | common_grid_id, confirmation_variant, observation_cutoff, horizon_end, touch/fail_back/hold_outside, high_only/low_only/both/neither, path_order, opposite_edge_visit_at, destination_touch/fill_at, overshoot, eligible_n, mature_n, no_sweep_n, boundary/unknown/censor_reason |

**Clocks and availability.** Use `America/New_York`, including DST. Form ranges on [start,end), freeze at end and count subsequent outcomes only after that cutoff. `GB-NYAM` is 09:00–10:00; `GB-10-11` is 10:00–11:00. A 09:45 event is ineligible for GB-NYAM. `GB-hour` is [t−60 minutes,t), frozen at each whole-hour cutoff t. `GB-hour-15m-cutoff` samples the same completed 60 minutes at 15-minute cutoffs as a named upgrade. The cadence is a disclosed construction choice. `GB-Asia` is 20:00–00:00 and `GB-London` is 02:00–05:00; both retain `clock_tag=INFERRED`. Keep their stated source tags separately. Existing clock-grid neighbors remain named experiments. Never merge overlapping GB and Jumbo objects by clock alone. Jumbo 6–9, prior-day rails and Jumbo London TBR/1.33/1.66 keep their existing definitions.

**Lines and locations.** TDO is the first observed print of the 00:00 ET minute. The cash-open line is the first observed print of the 09:30 minute. Store the actual observation time. A minute-bar-open substitute is a separately tagged `INFERRED` observation proxy. A missing boundary is not a late opening price. NWOG bounds are min/max(Friday settlement, Sunday 18:00 ET Globex opening print). Both must be available as of use; missing settlement stays unavailable. The settlement endpoint is a user lock, distinct from the attachment's Friday-close wording. NWOG carries `reference_role=destination`; record near-edge contact and far-edge/full-gap fill without generating an entry. Equal endpoints are a zero-gap case.

`GP-NYAM` and `GP-Jumbo69` use the completed parent range's height W=H−L. The 50.0–61.8% fractions are **HIS WORDS**. The range-height reconstruction and 6–9 transfer are disclosed location variants. In `extrema-order`, the first occurrences of the final low then high define an up impulse; high then low defines a down impulse. Both times are known only after formation. Up-pocket bounds are [H−0.618W,H−0.5W]; down-pocket bounds are [L+0.5W,L+0.618W]. Same-bar order without trade evidence is unknown, not an inferred direction. Zero width is unavailable. A later extreme cannot revise an earlier pocket. `reference_role=location` never supplies a failure or entry by itself.

**Failure and hold variants.** A sweep is an observed strict breach by at least one native tick of an already known edge or line. Equality is touch, not sweep. Record the approach side and every swept reference independently. `sweep-fail-print` requires a later observed price strictly back inside the box; for a line it must cross back to the pre-sweep side. `sweep-fail-close5` requires the first qualifying completed ET-aligned 5-minute bar to close strictly inside. Its close must follow both object availability and the sweep. `close5-through-TDO` and `close5-through-PDL` require a sweep plus an observed crossing of the named line in the breach direction, followed by a completed 5-minute close back through it. Keep box return and line confirmation times separate. A line already on the desired side does not count as a new cross-through.

`hold-outside-print` requires all observed post-sweep prices to remain strictly beyond the breached edge through the complete horizon. `hold-outside-close5` requires all completed post-sweep 5-minute closes to remain strictly beyond it. It needs at least one eligible close. A print return can coexist with a close-based hold; keep the variant IDs separate. Boundary closes, missing observations, unresolved event order and incomplete horizons cannot count as confirmed holds. This outside-hold statistic is distinct from the shared grid's zone-hold statistic.

Use these confirmation variants alongside the unchanged G1–G3 touch/reaction grid and its horizons. Preserve first and rearmed events. `a_plus_sweep_label` is true only when a sweep has been observed, false for an observed no-sweep population and unknown when coverage is insufficient. It is a sweep-only label, not the source's full grading system. Its known_at is the sweep time; a later failure has a later label-known time.

Compare fail-back, hold-outside and path class by clock, prior value/range, width, open-location state and golden-pocket location. Match cutoffs and future horizons across Jumbo and GB; keep full native coverage separately. In particular, compare completed GB-NYAM with Jumbo paths observed from after 10:00, not Jumbo's entire 09–12 outcome. Store the PDH + Asia-high + London-high sweep sequence and subsequent NWOG visit as confluence and destination events. The separate 09:30 open reaction may occur before GB-NYAM is available. No later box failure may enter a pre-open feature snapshot. Source percentages and fleet P&L are not pass targets.

### Remaining closed construction grids

Each row supplies the missing choices for the existing Q ID. Coded source variants remain intact. Change one construction axis at a time from the declared baseline, plus explicitly frozen combinations; this is not an unrestricted Cartesian search. All rows use the shared outcome grid.

| Closed ID | Named variants to measure |
|---|---|
| Q02 day labels | `width-terciles` and `width-quintiles` from prior-60 W69/WpriorRTH observations define narrow/extended bands. `balance-containment` uses 50%/70% of the 6–9 span inside prior RTH; retain the continuous overlap too. `purge-touch` and `purge-strict-break` record prior rail contact versus breach before the observation time. Judas uses first break then return/reversal; the two single-break classes retain extended versus narrow/already-purged evidence. Keep source scenarios as separate flags and emit overlap/unmatched labels rather than inventing exclusivity. |
| Q03 | Keep literal NY/source-exchange clocks. `ETH-18-to-17` and `RTH-0930-to-16` are explicit populations. London source clocks 00–00:30, 03–03:30 and liquidity 03–07 remain distinct; measure fixed-grid neighbors under the existing clock regularization. Preserve literal 0700–0501 and add `0700-one-minute-anchor`. Weekly variants are literal individual Mon/Tue session and `Mon-Tue-union`. |
| Q06 | `mean`, `median`, linearly recency-weighted mean, and exponential mean with 20/60-session half-life; `minimum-average-lower-quartile` and `minimum-rolling-mean-5` proxies. Compare last 60 completed versus current-inclusive source histories; open versus completed-range-edge anchors. Label proxy formulas explicitly. |
| Q07 | Prior-60 width terciles/quintiles; shallow pullback ≤25%/50% of completed early range; test/hold thresholds from G1–G3; close-only versus wick recross. Store each source day/open signature, snapshot at 30/60/120 minutes, and overlap/unmatched states. Final labels remain later outcomes. |
| Q08 | Rows of 1/2/4 native ticks; POC ties choose lower/upper/nearest profile midpoint. VA fractions 40/68/70% with source alternating-bin and highest-adjacent-volume expansion. Anchors: RTH, overnight, prior day/week and confirmed dealing range. Nodes use local maxima/minima after 1/3/5-row smoothing, with 10%/25% adjacent-peak prominence; a two-sided LVN needs a supported peak on both sides. Shelves use ≥50%/70% of the associated peak; ledges use adjacent-volume drops ≥50%/70%. TPO uses 15/30/60-minute periods, one-period interior single prints, 2/3-row tail excess, and single-row/flat-multiple-period poor-extreme variants. |
| Q09 | Separate generic re-entry, hold-inside, open-outside/two-period-inside, and older-POC/minor-node failed auctions. Balance establishment uses two/three completed 30-minute periods with overlapping value. Significant volume is ≥1.5×/2× prior-14 mean. Instant return is within 1/5 minutes. Two-period-inside variants require each period’s close inside versus its entire range inside. Use G holds/retests and report each denominator separately. |
| Q10 | Emit joint counts and trigger-conditional counts side by side. Gap references are prior RTH close, prior ETH close and settlement when available. Full fill is touch of that reference; half fill is touch of the midpoint between current open and reference. Include either/both/individual edge and no-fill outcomes. |
| Q11 | Trade-price versus HLC3 VWAP; ETH/RTH/fixed-06/new-high/new-low anchors; confirmed 3/3 and 5/5 swing anchors and timestamped source event anchors. Dispersion variants are centered volume-weighted SD, uncentered RMS, median-centered MAD and empirical residual quantiles. Bands 1/2/2.5/3 retain separate identities. |
| Q12 | Delta peaks: greatest positive, greatest negative magnitude, greatest absolute and highest-price nonzero row. Anchors: RTH/overnight/week/confirmed dealing range. Intrabar POC flip is lower→upper half or lower→upper quartile (and mirrors), known at transition; require 0/1 subsequent complete bar for confirmation. Protected extremes use confirmed swing plus close beyond the opposite swing, with price-only versus directional-delta agreement; future no-retest is only an outcome. |
| Q13 | Same-price ask/bid ratio 3.5×/4.5×; diagonal ask(p)/bid(p−tick) and its mirrored sell test at 3×/4×. Run lengths 2/3 rows. Zero opposite volume: positive-only qualifies versus zero-row excluded. Minimum row volume: any positive versus prior-session row-volume median. These are distinct variants, not one inferred350% rule. |
| Q14 | Print sizes 30–60 inclusive, ≥30 with visual size capped at60, ≥75 London, ≥100 NY, and prior-history top35%. Individual prints versus same-side/same-price 100ms/1s aggregates. Range bars span40ticks versus40points. Body/wick labels become available at bar completion. |
| Q15 | Body/range ≤0.006 versus ≤0.6; volume ≥1.5× prior-14 versus current-inclusive-14 mean. Keep three same-price BBO renewal events, observed renewal across three price levels where best-book visits permit it, and three-tick price displacement separately named. Opposite price reward uses2/3/4ticks; stage windows1/5/15minutes. Declining aggression, depleted renewal and huge-delta/no-progress remain different signatures. |
| Q16 | CVD reset at ETH/RTH or rolling window; own median over prior20/50/200 completed observations, plus expanding-session median. Compare raw CVD-minus-median and volume-normalized differences. No comparison of price with CVD units. |
| Q17 | Same-side print clusters within1/5seconds and1/3/5ticks; bounds are print min/max versus one-tick padding. Catalyst is first qualifying print versus confirmed cluster edge. Departure/rearm and touch results use G1–G3. Memory variants use prior same-session tests, prior-day tests and one/five-session half-life decay. OFM retains failed-start, release, return, renewal and subsequent control change as separately timestamped stages. |
| Q18 | Sample 1s/5s/1m. Define high/low effort and displacement using lagged50th/75th/90th and complementary percentiles. B: two-sided flow and low displacement; A: high aggression/low progress with observed renewal; D: high directional progress; E: renewal decline followed by edge breach; W: quote withdrawal without matching prints. Emit multi-label state flags, plus first-onset and fixed A/D/E/W/B precedence variants for exclusive transition tables. These are MBP-1 proxy labelers, not trained models. |
| Q19 | Keep each literal Pine sweep/CISD variant. Add source-text alternatives: wick breach/close-back versus entire body back inside; opposing-run open versus body/wick envelope; scans of10/20bars; close versus wick far-edge invalidation; arithmetic versus logarithmic midpoint. Formation and confirmation times stay separate. |
| Q20 | Trade participant proxies: size buckets1–9/10–29/30–59/60–99/100+, and prior-session size terciles. OHLC participant proxies: bar-volume terciles and volume/body-activity terciles from prior50 completed bars; apply the sourced signed-bar-volume increment within each bucket. They do not identify traders. Gamma CVD variants sum signed option quantity×multiplier×gamma, or that quantity×multiplier×gamma×spot²×0.01; use as-of Greeks, quote/tick signing and separate sign scenarios. Resets: ETH/RTH/rolling. Retain plain trade and OHLC CVD, so all five constructions are measured. |
| Q21 | NQ trade-triggered prior-window extrema compared with as-of ES trades; OHLC NQ/ES/YM and NQ/ES/YM/RTY. Scales1/5/15/60minutes plus source-confirmed HTF pivots. Trade comparator age limits1/5/30seconds; OHLC matching permits0/1 completed bar. Compare native ticks and prior-volatility-normalized excursions. IOD/RFZ/node arrivals match object kind, formation/session scope and side, with0/1/5-minute lag bins; count local nonarrival and use G horizons for response. |
| Q22 | Price futures options with Black-76 and cash/ETF options with disclosed European versus American-exercise approximations as named pricing scenarios. Use contract expiry/settlement/multiplier and ACT/365 time; rates/carry inputs retain availability. Exposure signs: unsigned, call-positive/put-negative proxy, and aggressor-flow scenario, never observed dealer inventory. Expiry buckets0DTE/1–7/8–14/15–60 days. Signing: quote test, tick test, unknown; OI weights: last available static versus signed-volume revision capped at25%/50% of max(prior OI,1). Missing release time stays as-of-unavailable; one/two-session lag assumptions are explicitly labeled proxies. Mapping: prior-session median price ratio versus additive basis, with native versus mapped events kept separate. |
| Q23 | Benchmark disclosed per-strike OI/scenario-gamma local peaks, with the screenshot retained as qualitative evidence. Compare 1/3/5-strike smoothing, top1/top5/all local peaks, one-strike-neighbor bounds versus half-height bounds, gross-exposure-normalized strength and0/1/5-minute persistence requirements. Retire on disappearance versus completed-close breach; retain versioned merges, invalidations and untouched nodes. No proprietary reconstruction or user decoding is required. |

Missing implementation definitions discovered later follow this same rule: add the named variants here, record their construction and available inputs, and measure them. Do not reopen Q01–Q23, add new QUESTIONS.md rows, or hold a ticket for the user to decode a source.
