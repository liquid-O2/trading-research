# Closed definition rules

These user instructions close Q01–Q23. They supersede earlier definition-blocker wording without rewriting the wiki, PRD, tickets or PHASE.md.

- **Width table:** uses the 6–9 H−L and prior RTH H−L.
- **Extensions:** 1.33/1.66 are from the 6–9 range. They remain extensions, separate from P-zones.
- **P-zones:** the original formula is unavailable. Benchmark = disclosed approximation; add and measure upgrades. Do not ask the user to decode unpublished Jumbo internals.
- **Touch/reject/hold/break:** use a named variant grid, with the same grid across families.
- **Ambiguous PDF thresholds:** use named variants from the source text, then measure. Do not ask the user.

Every existing row in [QUESTIONS.md](QUESTIONS.md) is marked **Closed — variant grid** or **Closed — user lock**. No new questions or tickets are added. If another definition is missing, specify its named variants in [SPEC.md](SPEC.md#closed-definition-variant-grids) and keep the ticket unblocked by that definition. Missing observations and limitations remain explicit; variants are not claims to have recovered an undisclosed formula.

The SPEC addition supplies only missing measurement definitions. This change does not start implementation, measurement runs or workers.
