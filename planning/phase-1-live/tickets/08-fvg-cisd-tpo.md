# 08 — FVG, CISD/blocks, TPO and AMT labels

**Outcome.** Three sourced families that are not inside ticket 07 print on F: first-presented FVG / body gaps; Jumbo/Pine sweep-CISD-blocks; TPO geometry plus AMT open/day labels. IB remains the ticket-02 comparison row.

**Wiki.** `wiki/fvg-body-gaps.md`, `wiki/sweep-cisd-blocks.md`, `wiki/tpo-ib-auction.md`.

**Definition → compute → pass.**
1. `gap.fvg.first.clock` on 1-minute NQ; `gap.body.adjacent`; HTF and trade-empty comparison rows.
2. `block.sweep.tbr.3m` (TBR three-candle); `cisd.fractal.literal` (Open Source Fractal as coded, causal replay); wick/body/close and envelope upgrades.
3. `value.tpo.rth.30m` (POC, single prints, excess, poor extremes); `label.amt.open.30m`; `label.amt.day`. 80% claims as separate denominators, not one pooled rate.
4. Do not write any of these ids onto GB session-fail pages or as GB edges.
5. Pass command: `... run_phase1_objects.py report --family gap --family block --family tpo`

**Acceptance criteria.**
- Each family has at least one `measured` row with `n` and the shared grid.
- No GB-NYAM / TDO / NWOG id is redefined here.
- `range.ib` is not re-homed; it stays ticket 02.
- AMT labels and Jumbo `day_type` printed side by side; unmatched stays unmatched.

**Blocked by.** 01 (bars), 02 (clocks), 03 (Jumbo labels for the agreement matrix).
**Out of scope.** SMT; GB fail-boxes; promoting OR/IB to the main box.
