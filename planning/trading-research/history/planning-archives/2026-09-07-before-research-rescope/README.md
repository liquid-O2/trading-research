# Archived trading plan — 2026-09-07

The user requested a simpler active research program covering statistics, Context and Locations, with equal depth for every family. This archive preserves the preceding plan and review artifacts byte for byte. The rebuildable Python environment and caches are excluded.

The readable frozen reference remains at `/workspace/planning/trading-model` because existing implementation evidence binds its absolute paths and hashes. It no longer governs the active work sequence. The active replacement is `/workspace/planning/trading-research/PLAN.md`.

See [MANIFEST.json](MANIFEST.json) for every archived file and hash. The compressed archive is [trading-model.tar.gz](trading-model.tar.gz). This planning archive is distinct from the excluded failed implementation under `/workspace/archive`.
